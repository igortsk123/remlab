# CODE-REF allowlist: пути, которые audit НЕ считает за claim о коде (иллюстрации, примеры,
# планируемые файлы). По одному пути на строку; пусто/# — игнорируются. Точное совпадение с токеном
# ИЛИ префикс (строка с хвостовым / гасит всё под ней).
# Внешняя research-папка ML-экспериментов (вне репо remlab, /home/pakar/mltest) — не репо-код:
mltest/
# tools/ намеренно в .gitignore («не часть проекта», кроме memory-audit.mjs для CI) — ссылки на
# локальные скрипты (migrate/trace/session/metrics) не репо-claim; в CI-checkout их нет. Продуктовый
# код (app/, lib/, infra/, db/) остаётся под проверкой CODE-REF.
tools/
components/CalcForm.tsx
# ADR-0042 (мебельный трек): планируемые файлы (код ещё не написан) + внешний URL API Гдеслона:
db/init/007-products.sql
db/init/008-sets.sql
services/planner-solver/
api.gdeslon.ru/
data/planner/composition.ts
# Э0 prod-layout-engine: пути ВНЕШНИХ репо (ProcTHOR/Infinigen/Holodeck) в guides/layout-mined-rules.md —
# это цитаты чужого кода с URL, не claim о нашем дереве:
procthor/
ai2holodeck/
src/infinigen_examples/
src/infinigen/
example_solver/
node_impl/
annealing.py
trimesh_geometry.py
# Файлы АПСТРИМА Hunyuan3D-2.1 (живут в docker-образе /opt/hunyuan, не в нашем репо) —
# ссылки на них в памяти это цитата чужого кода, а не claim о нашем дереве:
hy3dpaint/
hy3dshape/
