# Исходники первичной сборки банка (июль 2026) — архив

Файлы, из которых `/memory-init` 2026-07-01 собрал первую версию банка (`_intake/_processed/` по
конвенции кита). Перенесены сюда 2026-09-05 (аудит памяти): папка `_processed/` была в `.gitignore`,
а файлы — в git, единственная копия. Актуальные версии: `docs/tech-spec-ts-stack.md`,
`docs/cjm-ux-v0.2.md`; `goal.md` и `deploy-and-decisions.md` — только здесь (история).
`source:` в сгенерированных доках может по-прежнему указывать на `_intake/_processed/…` — это ссылка
на провенанс, читать здесь.
