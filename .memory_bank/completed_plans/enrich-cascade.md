---
workstream: catalog
slug: enrich-cascade
title: К2 — каскад обогащения 0→1→2→3 с качеством и версиями
status: completed
created: 2026-08-05
updated: 2026-08-05
completed: 2026-08-05
---

## Цель
Обогатить пул гостиной (26 114 товаров) по каскаду «сперва бесплатно, потом дёшево, дорого —
только спорным», записать результат в `product_enrichment` с версиями и системной оценкой
качества, и платить дальше только за дельту.

## Источник задачи
Этап К2 мастер-плана [[MASTER-catalog-ai]] после К1 (дельта, [[catalog-delta-lifecycle]]) и К3
(выбор модели, [[golden-sample]]). Модель выбрана числами: `gpt-5.6-luna`.

## Скоуп — что входит
- **Уровень 0 (`rules0.py`, бесплатно):** роль по `category_path`, цвет и материал по словарям,
  форма, бренд и серия, качество размеров (`полные / частичные / нет`), флаги трудной карточки.
  Всё, что вытягивается регулярками, модели не отдаём — она только проверяет и дополняет.
- **Уровень 1 (`enrich.py`, `gpt-5.6-luna`):** текст карточки → строгая JSON-схема К3.
  Фотографию не отправляем. Массовый прогон — Batch API (−50%).
- **Уровень 2 (заготовка):** vision по миниатюре только по флагам (нет описания И общее название,
  либо противоречие текста и правил). Включается, когда замерим, сколько таких карточек.
- **Уровень 3:** эскалация на `gpt-5.6-terra` для позиций с низким качеством.
- **`enrichment_quality` считает система:** согласие уровня 0 и уровня 1 по роли, покрытие полей,
  флаги трудности, правдоподобие размеров. Не «уверенность модели о себе» — она завышена.
- **Версии в каждой записи:** модель, версия промпта, версия схемы, `enrichment_version`.
  Смена версии не пересчитывает каталог: перегоняем только затронутое.

## Скоуп — что НЕ входит
- Индексы кандидатов и переключение сборщика комплектов на обогащение (К4).
- Обучение классификатора поверх эмбеддингов (К6).
- Массовый vision-прогон: сперва цифра «сколько карточек его реально требуют».

## Файлы к изменению
- [ ] `tools/scout/rules0.py` — НОВЫЙ: бесплатный уровень
- [ ] `tools/scout/enrich.py` — НОВЫЙ: каскад, качество, версии, Batch
- [ ] `tools/scout/002-enrichment-payload.sql` — индексы по полям payload (если понадобятся)
- [ ] `.memory_bank/core/catalog.md`, `domain/catalog-enrichment.md` — результаты

## Критерии приёмки
- Прогон по пулу завершён, `product_enrichment.payload` заполнен, версии проставлены.
- Доля позиций с низким качеством (эскалация) < 25%.
- Факт по деньгам сверен со сметой (≈16 $ пакетом за пул).
- Повторный запуск не тратит ни копейки: считается только дельта по хешам (проверка К1).

## Риски
1. Batch выполняется до 24 часов — сессия может закончиться раньше; поэтому `enrich.py --fetch`
   умеет забрать результат позже по id задачи.
2. Строгая схема не проверяет диапазоны — числа валидируем в коде.
3. Модель может «уверенно» назвать роль, которой нет в тексте — ловим сверкой с уровнем 0.

## Верификация
`enrich.py --sample 200` (синхронно, ~1 $) → таблица качества; затем `--pool --batch` и
`--fetch <id>`; выборочная ручная сверка 30 карточек.

## Completion summary (2026-08-05)
Каскад построен и прогнан по всему пулу: **26 147 из 26 147 обогащены**, среднее качество 0.83,
ниже порога 0.65 — 2 268 (8.7%) при допустимых 25%. Факт по деньгам: **17.22 $** за 25 947
товаров пакетом (смета была ≈16 $) — 140.4M токенов входа и 5.3M выхода, 0.66 $ за 1000 товаров.
Повторный запуск стоит НОЛЬ: `--pool` пропускает всё, что обогащено этой версией.

Уровень 0 (`rules0.py`) бесплатно закрывает цвет у 82.2% пула и материал у 72.1%.
Уровень 2 (картинка) не понадобился: карточек «нет описания И общее название» всего 36 из 26 147.
Уровень 3 отработал по 45 позициям, где сильной модели есть с чем работать (текст и размеры на
месте, спорна роль или подтип) — из 2 284 слабых остальные 2 143 слабы из-за дыр в размерах,
которых никакая модель не восстановит.

Уроки: (1) два первых замера показали не ошибки модели, а дыры схемы — в списке ролей не было
бра, шкафа, штор, камина, статуэтки, и модель честно отвечала «другое», а система считала это
расхождением: эскалация 36% → 6% после расширения; (2) «первые N товаров» для замера не годятся —
пул отсортирован по магазину, и первые 60 оказались светильниками одного поставщика, берём ровным
шагом; (3) эскалацию нужно адресовать причине: сильная модель не восстанавливает размеры, поэтому
из 2 284 слабых она полезна ровно 45 (0.45 $ вместо 23 $ на всех).

## Статус на 2026-08-05, 19:55
Уровни 0 и 1 построены и замерены. Пул (25 947 товаров) отправлен пакетом четырьмя частями
(id — `tools/scout/enrich-batch-id.txt`), фоновый опрос и загрузка результата —
`enrich_wait.sh` (лог `~/scout-backups/enrich-wait.log`). Замер на 200 товарах ровным шагом
по пулу: качество 0.83, эскалация 6% (было 36% до расширения списка ролей).
Осталось: дождаться пакетов, сверить факт по деньгам, при нужде прогнать уровень 3 по
позициям с качеством < 0.65.
