---
workstream: estimate/lab
slug: e-companions-checklist
title: Страница расчёта: сопутка галочками отдельным блоком, иерархия заголовков, вкладка «Расчёты материалов»
status: completed
created: 2026-07-31
updated: 2026-07-31
completed: 2026-07-31
---

## Цель
Три правки по фидбеку владельца: (1) вкладка «Материалы» в /lab → «Расчёты материалов»;
(2) на странице расчёта видно, что она внутри «Моей лаборатории» (крошка заметнее, заголовок
скромнее); (3) сопутствующие товары (клей, грунтовка, шпатели…) убрать из позиций расчёта —
показывать отдельным блоком с галочками «Не забудьте».

## Источник задачи
Владелец (2026-07-31, по живой странице /e/…): сопутка «не к месту» среди позиций; крошка
слишком незаметна рядом с жирным заголовком; вкладку переименовать.

## Скоуп — что входит
1. **Вкладка**: «Материалы» → «Расчёты материалов» (`app/lab/page.tsx`). «Ремонт»/«Дизайны»
   не трогаем (владелец просил только материалы).
2. **Иерархия на `/e/[id]`**: крошку сделать заметной строкой обычного размера —
   «🧪 Моя лаборатория / расчёт» (ссылка акцентным цветом), заголовок «Расчёт обоев» — меньше
   и спокойнее (сейчас самый жирный элемент страницы «перебивает» принадлежность к лаборатории).
3. **Сопутка галочками**:
   - `lib/calc/to-estimate.ts` — при сохранении БОЛЬШЕ НЕ добавлять сопутку в позиции
     (позиции = только расчёты по комнатам);
   - `/e/[id]` — легаси-позиции сопутки (note «сопутствующее…») скрыть из списка
     (данные не удаляем — обратимо, старые расчёты очищаются сами);
   - новый блок-карточка «Не забудьте» с чекбоксами по виду материала (`COMPANIONS[kind]`),
     отметки живут в браузере (localStorage), на сервер не пишутся;
   - блок показывается только у расчётов с `meta.kind` (у ремонта своей сопутки нет).
4. Тесты: обновить unit `calc-to-estimate` (сопутки в позициях нет); e2e «Клей обойный» остаётся
   видимым (теперь в блоке галочек) — проверка не ломается.

## Скоуп — что НЕ входит
- Удаление сопутки из БД (скрываем отображением — обратимо).
- Сопутка со ссылками/ценами (будущая монетизация «наши подборы» — отдельно).
- Мёртвые `CalcForm`/`createFromCalc` (follow-up прежнего плана).

## Файлы к изменению
- [x] `app/lab/page.tsx` — ярлык вкладки
- [x] `app/e/[id]/page.tsx` — крошка/заголовок, фильтр легаси-сопутки, блок галочек
- [x] `components/estimate/CompanionChecklist.tsx` — НОВЫЙ (клиентский, чекбоксы + localStorage)
- [x] `lib/calc/to-estimate.ts` (+ `lib/estimate/label.ts`: estimateKind) — убрать сопутку из позиций
- [x] `tests/unit/calc-to-estimate.test.ts` — обновить ожидания
- [x] e2e: комментарий у «Клей обойный» (остался видим — в блоке галочек)

## Критерии приёмки
- [x] В /lab вкладка называется «Расчёты материалов»
- [x] На `/e/[id]` принадлежность к лаборатории читается сразу; заголовок не «перебивает»
- [x] В позициях расчёта нет клея/грунтовки/шпателей — ни в новых, ни в старых сохранённых
- [x] Блок «Не забудьте» с галочками: отметки переживают перезагрузку страницы
- [x] Типы/линт/тесты/сборка зелёные; e2e в CI зелёный; прод обновлён

## Definition of Done — память
- [x] ADR-0040 (сопутка = чек-лист-подсказка, не позиции расчёта); `core/estimate.md`; `/memory-check`, audit чисто

## Лог выполнения
- 2026-07-31 — план создан (draft); «деплой»; откат: тег `rollback-pre-companions` (52cd38e)
- 2026-07-31 — реализовано, прод 4bed228; смоук по живому расчёту владельца: сопутки в позициях
  нет, блок галочек виден, «Клей обойный» ровно 1 раз; «Расчёты материалов» в /lab
- 2026-07-31 — completed: /memory-check, audit чисто

## Completion summary
Сопутка перестала быть позициями (calcToItems), легаси скрыта фильтром (БД не тронута),
блок галочек CompanionChecklist (localStorage); крошка «Моя лаборатория / расчёт» заметной
строкой, h1 24px; вкладка «Расчёты материалов». Побочный эффект: пустой расчёт (0 позиций)
больше не сохраняется — раньше сопутка «спасала» пустое сохранение (это фикс, не регрессия).

### Уроки (ОБЯЗАТЕЛЬНО)
Без отклонений.

## Follow-up work
- [ ] Сопутка «наши подборы» со ссылками (монетизация, М1 v1.1) — когда дойдём
