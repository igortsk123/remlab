---
workstream: launch
slug: launch-p3-calc-ux
title: П3 UX калькуляторов — VizCta скрыт, LeadCard-баннер без дублей, подсказки, новый расчёт, тап-зоны
status: completed
created: 2026-07-28
updated: 2026-07-28
completed: 2026-07-28
---

## Цель (зонт launch-prep П3, пп. 11, 12, 15–18, 24)
1. VizCta скрыт за `NEXT_PUBLIC_SHOW_WIP` (не удалять). 2. LeadCard — заметный баннер, у плитки ОДНА
на комнату (сейчас дублируется под стенами и полом). 3. Ошибка чтения ссылки — заметнее (danger, жирно).
4. Единая подсказка «что дальше» в липком Итоге: площадь 0 → «заполните размеры»; есть площадь, нет
цены → «укажите цену». 5. «?»-подсказки у «Раппорт» (обои) и «Шов» (плитка). 6. Кнопка «Новый расчёт»
+ очистка черновика после сохранения (save возвращает id, клиент чистит localStorage и переходит).
7. Крестики удаления зон: тап-зона ≥44px, контрастнее (danger hover).

## Файлы
CalcBuilder (флаг VizCta, sticky-hint, «Новый расчёт»), RoomPanel (одна LeadCard в конец комнаты),
LinkAutofill (error-стиль), MaterialParams (tip у Раппорт/Шов), NumField+help, useCalcProject (clear),
ResultView + app/calc-actions.ts (return {ok,id} вместо redirect; клиент чистит и переходит),
FloorEditor (крестики), globals.css (.lead-card заметнее, .icon-del).

## Критерии
- [x] Прод: VizCta не виден; LeadCard одна на комнату и заметная; ошибки чтения видно
- [x] Липкий Итог подсказывает следующее действие во всех 4 видах
- [x] «?» у раппорта/шва работают (hover/тап)
- [x] «Новый расчёт» очищает черновик; после сохранения черновик очищен, переход на смету
- [x] typecheck / lint / test / build + e2e зелёные; авто-деплой

## Лог
- 2026-07-28 — создан, реализация (зонт)
- 2026-07-28 — реализовано, гейты зелёные, выкат авто-деплоем

## Completion summary
(1) VizCta за NEXT_PUBLIC_SHOW_WIP. (2) LeadCard одна на комнату (в конец панели; у плитки не
дублируется), баннер заметнее (2px терракота + плотнее фон). (3) Ошибка чтения ссылки — danger, 600.
(4) Липкий Итог: «→ заполните размеры» / «→ укажите цену товара…» во всех видах. (5) «?»-подсказки
у Раппорта и Шва (help-tooltip). (6) saveCalcEstimate возвращает {ok,id}; клиент чистит черновик
(clearProject) и переходит на /e/<id>; кнопка «новый расчёт» с confirm у вкладок комнат.
(7) Крестики зон — .icon-del (44px, danger hover).
