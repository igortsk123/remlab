---
workstream: launch
slug: launch-p5-links2-core
title: П5 Чтение ссылок 2.0 (ядро) — аудит+фикс цены, чистый текст для LLM, пометки авто/вручную, товар-строка
status: completed
created: 2026-07-28
updated: 2026-07-28
completed: 2026-07-28
---

## Сделано (зонт launch-prep П5, ядро)
- **Аудит на реальных ссылках (прод):** обои miroboev — размеры/раппорт ✓, но цена 2.1 МЛРД (жадный
  regex склеил артикул R210139 с ценой 2650 через пробел) → ФИКС: пробелы в числе цены только как
  разделители троек + санити ≤10 млн (+2 теста). Плитка stroypark: 200×200, 735.93 ₽/м² ✓.
  OZON — unreachable (антибот, ожидаемо). `OPENAI_API_KEY` на проде ЕСТЬ и проброшен в контейнер ✓.
- **Чистый текст для LLM:** `htmlToText()` — script/style/noscript вырезаются целиком (раньше JS-код
  попадал в bodyText и зашумлял 12k-срез для ИИ и regex-эвристики). Используется в parseProductHtml и route.
- **Пометки авто/вручную:** `lib/calc/auto-fields.ts` (applyAutoSpec/manualKeys, 3 теста) +
  `room.autoKeys`/`floorAutoKeys`; новая ссылка стирает ТОЛЬКО авто-значения, ручные живут; ручная
  правка снимает бейдж. UI: бейдж «авто» у полей MaterialParams (16 точек).
- **Строчка о товаре:** после чтения ссылки — «Нашли: <название>» (видно, что данные с этой ссылки).

## Follow-up (П5b, не блокирует запуск)
- [ ] Прокси-фолбэк добычи HTML (РФ резидентский из VPN-проекта) — нужен адрес/доступ от владельца (в `.env` прода).
- [ ] Расширенный аудит по списку магазинов (Леруа/Петрович/ОБИ…) + отчёт владельцу.
- [ ] (опц.) last-resort «вставьте текст со страницы».
