---
workstream: estimate/lab
slug: lab-hub-tabs
title: «Моя лаборатория» — вкладки для всех сохранений (материалы · ремонт · дизайны · мой стиль)
status: completed
created: 2026-07-31
updated: 2026-07-31
completed: 2026-07-31
---

## Цель
Превратить `/lab` из плоского списка смет в центр всех сохранений сайта с **вкладками**:
«Материалы» (сохранённые расчёты), «Ремонт» (тизер «Скоро», позже — расчёты source=remont),
«Дизайны» (пока заглушка-тизер), + карточка «Мой стиль» (результат игры /styles, начнём сохранять).

## Источник задачи
Владелец (2026-07-31): в лаборатории должен быть весь сохраняемый функционал сайта, включая то,
что в разработке. Ответы на вопросы: структура — вкладки; дизайны — пока просто заглушка;
раздел «Ремонт» показывать тизером «Скоро»; добавить «Мой стиль» из игры.

## Скоуп — что входит
1. **Вкладки в `/lab`** (серверные ссылки `?tab=` — прямые ссылки и «назад» работают, клиентский
   JS не нужен; решение по ходу, вместо клиентского LabTabs): `Материалы` · `Ремонт` · `Дизайны`.
   - **Материалы** (по умолчанию): текущий список смет `source !== "remont"` + удаление +
     «+ Новый расчёт» (как сейчас, без регрессий).
   - **Ремонт**: если есть сметы `source === "remont"` — список (тот же вид карточек);
     если нет — тизер-карточка «Скоро: сколько стоит ремонт» со ссылкой на витрину `/calc/remont`
     (там уже стоит ComingSoon). Тизер показывается всегда; префикс «Скоро:», CTA и бейдж
     «скоро» на вкладке — только при выключенном `SHOW_WIP` (иначе врали бы при включённом разделе).
   - **Дизайны**: заглушка-тизер «Скоро: дизайн вашей комнаты по фото». Если у сессии уже есть
     проекты (`projects`) — под тизером строка «Уже создано: N комнат → /rooms» (не терять
     доступ к существующему; это текущая строка-счётчик, переехавшая во вкладку).
   - На вкладках-тизерах бейдж «скоро» на самой вкладке, чтобы не выглядело как битая пустота.
2. **«Мой стиль»** — маленькая карточка над вкладками (общая для всех):
   - `/styles` (StyleQuiz) по завершении игры сохраняет результат: server action
     (`app/styles-actions.ts`) пишет стиль в cookie-сессию — лёгкая таблица
     `style_results (session_id pk, style, updated_at)` (миграция `db/init/006-...sql`) —
     БЕЗ новой инфраструктуры, тот же паттерн session_id.
   - В `/lab`: если стиль есть — «Ваш стиль: <название>» + кнопки «пройти заново» (/styles)
     и «дизайн в этом стиле» (ведёт на витрину дизайна); если нет — «Узнайте свой стиль → игра».
3. **Бейдж-счётчик** `/api/lab/count` — оставить как есть (только сметы); дизайны/стиль на
   счётчик не влияют (тизеры — не «сохранения»).
4. Метрика: событие `lab_tab` (параметр tab) через существующий `lib/analytics.ts` — серверное,
   при рендере с явным `?tab=` (клики по «Ремонт»/«Дизайны» и прямые заходы; дефолтная
   «Материалы» без `?tab` не трекается) — понять, ищут ли люди закрытые разделы.
5. Тесты: unit на фильтрацию смет по source; e2e-smoke `/lab` (вкладки переключаются,
   пустые состояния видны) — в существующую e2e-сетку.

## Скоуп — что НЕ входит
- Полноценные карточки-превью дизайнов в /lab (владелец: «пока не важно») — отдельная задача,
  когда откроем AI-дизайн.
- «Мои заявки» (лиды) — владелец не выбрал; кандидат в follow-up.
- Аккаунты/логин (auth — по-прежнему анонимная cookie-сессия, интерим).
- Пагинация/фильтры/переименование смет.
- Редирект `/rooms` → `/lab` — НЕ делаем (дизайны в /lab пока заглушка, /rooms остаётся
  рабочей страницей существующих проектов).

## Файлы к изменению (факт)
- [x] `app/lab/page.tsx` — вкладки-ссылки `?tab=`, панели, событие lab_tab
- [x] `components/lab/EstimateRows.tsx` — НОВЫЙ: список смет (вынос из старой /lab)
- [x] `components/lab/LabTeaser.tsx` — НОВЫЙ: тизер WIP-раздела
- [x] `components/lab/MyStyleCard.tsx` — НОВЫЙ: карточка «Мой стиль» / CTA на игру
- [x] `lib/estimate/lab-split.ts` — НОВЫЙ: раскладка смет по source
- [x] `app/styles-actions.ts` — `completeQuiz` (Zod + upsert + track; бывш. trackQuizCompleted)
- [x] `components/StyleQuiz.tsx` — вызов completeQuiz
- [x] `db/schema.ts` + `db/init/006-style-results.sql` — таблица `style_results`
- [x] `modules/style/repository.ts` — НОВЫЙ: get/upsert по session_id (память-фолбэк)
- [x] `lib/analytics.ts` — тип события `lab_tab`
- [x] `app/globals.css` — стили `.lab-tabs`/`.lab-tab`/`.style-swatch`
- [x] `deploy.sh` — 006-sql в запасной ручной путь (авто-деплой берёт все `db/init/*.sql` сам)
- [x] тесты: `tests/unit/lab-split.test.ts`, `tests/unit/style-repo.test.ts`, e2e smoke `/lab`
- НЕ создан `components/lab/LabTabs.tsx` из черновика — вкладки серверными ссылками (см. лог)

## Задачи
- [ ] Вкладки + разнос смет по source (материалы/ремонт), тизеры «Скоро» (Ремонт, Дизайны)
- [ ] Строка «Уже создано: N комнат» во вкладке Дизайны (при наличии projects)
- [ ] Таблица style_results + repo + server action + вызов из StyleQuiz
- [ ] Карточка «Мой стиль» в /lab (оба состояния)
- [ ] Метрика lab_tab; типы/линт/тесты/сборка зелёные
- [ ] Push в main → авто-деплой; smoke на проде

## Критерии приёмки
- [x] `/lab`: три вкладки; «Материалы» = прежний список без регрессий (удаление работает)
- [x] Вкладка «Ремонт»: тизер при пустоте; смета source=remont (если появится) отображается
- [x] Вкладка «Дизайны»: тизер; при наличии комнат — ссылка «Уже создано: N» на /rooms
- [x] Игра /styles сохраняет стиль; /lab показывает «Ваш стиль: …»; повторная игра обновляет
- [x] `?tab=remont` открывает нужную вкладку по прямой ссылке
- [x] Lint / typecheck / build / тесты проходят; e2e в CI зелёный (после фикса 25c3e09)
- [x] Файлы вне scope — не задеты (globals.css/deploy.sh добавлены в скоуп фактом, см. список)

## Definition of Done — память (без этого `completed` запрещён)
- [x] Memory Bank обновлён: `core/estimate.md` (или `core/user-flow.md`) — /lab с вкладками;
  `decisions.md` — ADR (вкладки, тизеры WIP, сохранение стиля); `project-state.md`
- [x] Новая область? Нет — /lab уже описан в user-flow/estimate; style_results → `core/data-model.md`
- [x] «Уроки» заполнены; отброшенное → `core/lessons.md`
- [x] `/memory-check` выполнен, audit «чисто»

## Лог выполнения
- 2026-07-31 — план создан (draft); ответы владельца: вкладки; дизайны — заглушка;
  ремонт — тизер «Скоро»; + «Мой стиль»
- 2026-07-31 — «деплой» от владельца (макет отменён, сразу в прод); точка отката — git-тег
  `rollback-pre-lab-hub-tabs` (02237f6); по слову «откат» — revert до него + push
- 2026-07-31 — реализовано; вкладки — серверные ссылки `?tab=` (не клиентский стейт: работают
  прямые ссылки/«назад», ноль JS); обнаружено: /styles и /start тоже за витринами SHOW_WIP →
  бейджи «скоро» зависят от флага; сохранение стиля: `completeQuiz` (бывш. trackQuizCompleted)
- 2026-07-31 — коммит 4c57fd4 + фикс e2e 25c3e09 (регистр «Скоро: сколько…», клики в nav
  вкладок); CI зелёный; авто-деплой применяет db/init/006 сам (deploy.yml прогоняет все *.sql)

## Completion summary
Реализовано и в проде (25c3e09, версия подтверждена /api/health): /lab — вкладки-ссылки
Материалы/Ремонт/Дизайны с тизерами «Скоро» у WIP-разделов, карточка «Мой стиль»
(style_results + completeQuiz из игры), событие lab_tab, тесты (2 unit + e2e smoke),
миграция 006 применена авто-деплоем. Упрощено: вкладки серверными ссылками вместо клиентского
компонента (меньше кода, работают прямые ссылки). Отложено: превью дизайнов в /lab (владелец:
«пока заглушка»), «Мои заявки» (follow-up). Verify-агент: функциональных расхождений нет.
Откат: тег `rollback-pre-lab-hub-tabs`.

### Уроки (ОБЯЗАТЕЛЬНО)
Почти без отклонений; одна грабля: e2e-локатор по тексту упал на регистре («Скоро: **с**колько
стоит ремонт» vs /Сколько…/) и мог бы совпасть со ссылками шапки → текстовые локаторы писать
регистронезависимо (/…/i) и скоупить к контейнеру (getByRole("navigation", …)). → в core/lessons.

## Follow-up work
- [ ] «Мои заявки» (история лидов «найдём дешевле») — отдельным планом, если владелец захочет
- [ ] Полноценные превью дизайнов в /lab — когда откроем AI-дизайн (М5)
- [ ] Учитывать дизайны в бейдже-счётчике — вместе с превью

- 2026-07-31 — completed: прод 25c3e09, verify чисто, /memory-check выполнен, audit чисто
