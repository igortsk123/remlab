---
workstream: mesh-pipeline
slug: mesh-node-health-breaker
title: Автоснятие битой ноды по подряд-сбоям + задания не теряются молча
status: completed
created: 2026-09-01
updated: 2026-09-01
completed: 2026-09-01
---

## Цель
Нода, которая систематически валит задания ПО СВОЕЙ ВИНЕ, сама выводится из пула и
переаллоцируется (3 сбоя подряд); её задания возвращаются в очередь, а не исчезают.
Плюс — устранить причину простоя оплачиваемых нод между пачками.

## Источник задачи
Владелец 01.09 по итогам замера скорости: «аллокейт надо таких нод как №2 после 3 подряд,
делай системно», «повторяться не должно», «почему когда локально отключаю ремонт ноды тоже
стоят». Продолжение `mesh-dynamic-node-pool` (in_progress) — та же семья отказов.

## Диагноз (проверено по коду и журналам, 01.09)
1. **Нода не выводится из пула.** `ssh_run.worker()` уводит ноду ТОЛЬКО по `transport_failed`
   (`tools/scout/salad/ssh_run.py:404`), и то лишь на 60-секундный cooldown — без `reallocate`.
   `input_failed` считается вердиктом по товару, нода продолжает брать задания:
   `35b10e39` отдала 30 таких за прогон.
2. **Задание закрывается молча.** `Jobs.summary()` (`ssh_run.py:340-349`) нетерминальным
   считает только `transport_failed` → `input_failed` попадает в `terminal_prefix` →
   `batch_show` двигает курсор (`batch_show.py:287`). `apply_repairs.py` сканирует только
   скачанные `manifest.json` (`apply_repairs.py:144`), а `input_failed` возникает ДО создания
   манифеста (`worker.py:150`) — то есть приёмка такой отказ физически не видит.
3. **Второй, независимый источник потери.** `batch_show` берёт `total = len(SAMPLE['jobs'])`
   = **1465** (`batch_show.py:242`), а `ssh_run` разворачивает seeds и фильтрует роли:
   1515 → **1503** mesh-eligible (проверено скриптом; отброшено 12 ковров, 25 SKU имеют по
   3 seed). Конвейер остановится на 1465 — **последние 38 заданий не будут запрошены никогда.**
4. **Простой 06:41→07:19 (38 мин) — не ремонт, а OOM.** Три локальных шага убиты earlyoom на
   машине с ~11 ГБ: `apply_repairs.py` 6.95 ГБ, `orient_worker.py --run --limit 200 --vlm`
   8.6 ГБ, `topview_render.py` 8.86 ГБ. Перед смертью загнали машину в своп — отсюда десятки
   минут. Работу они НЕ доделали. Приёмка — 1 из 7 последовательных шагов между пачками
   (`batch_show.py:289-300`), всё это время `ssh_run` не запущен: ноды тёплые
   (`--keep-alive`), оплачиваемые и без заданий. Цена паузы ≈ **5,7 GPU-часов**.
5. **Надпись «ремонт» в логе — устаревшая.** Супервизор стартовал 05:33:37, ревёрт ADR-0143
   закоммичен 05:57: ярлык шага живёт в памяти старого процесса, сам `apply_repairs.py`
   вызывается подпроцессом и уже новый. Отключение ремонта сработало.

## Критическая оценка (свой разбор + Codex, `_intake/codex/codex-node-breaker-answer.md`)
Совпало: нода не выводится, задания теряются, курсор двигать нельзя, простой лечится
отделением локального consumer от удалённой генерации.

**Codex опроверг три моих вывода — проверил, он прав:**
- **«30 SKU потеряны» — неверно.** 30 — это число неудачных ПОПЫТОК на ноде, а не потерянных
  товаров. Факт: 43 строки `input_failed`, 39 уникальных SKU, из них **10 не имеют ни одного
  успеха** нигде в журнале. Масштаб потери — ~10 SKU, а не 30. Точнее журнал сказать не
  позволяет: в нём нет `seed`, `job_key` и полного instance id (`ssh_run.py:391`).
- **`input_failed` нельзя считать виной ноды целиком.** Разбор ошибок: 21 ENETUNREACH,
  8 timeout, **7 HTTP 404/410 (мёртвое фото — вина товара)**, 6 SSL EOF, 1 DNS. Порог
  «3 подряд `input_failed`» переаллоцировал бы здоровую ноду за три мёртвых ссылки подряд.
  Классифицировать надо по коду ошибки, а не по статусу.
- **Счётчик в памяти процесса не сработает.** Каждая пачка — НОВЫЙ процесс `ssh_run`, серия
  2+2+2 никогда не дойдёт до трёх. То же и с бюджетом пересадок: `_CULL_LOG`
  (`batch_show.py:99`) живёт в памяти, у родителя и у ребёнка журналы разные.

Codex нашёл сверх этого (проверено): статус `failed` — все 4 случая это
`ValueError: пустой файл: source.jpg`, дефект кода (`worker.py:178` → `:212` → `storage.py:30`),
а не вина ноды; `warmup()` ставит `warm=True` даже после `warmup_error` (`worker.py:102`) —
`probe_warm` примет сломавшуюся при прогреве ноду; после `reallocate` замена греется 25–35 мин,
а `STALL_S`=1800 может закрыть остаток «нет живых нод» ровно перед прогревом.

**Где я не согласился с Codex (и почему):** он предлагает сразу ввести контракт
`disposition/fault_scope/phase/error_code` в воркере. Воркер живёт в docker-образе на нодах —
это пересборка и перезалив образа (ADR-0137, часы). Текст ошибки УЖЕ приезжает в результате и
уже пишется в журнал, поэтому классификацию делаем на стороне супервизора: тот же эффект без
пересборки образа. Контракт в воркере — следующим шагом, когда образ и так будет пересобираться.

## Скоуп — что входит
- Классификация отказа по коду ошибки: вина ноды / вина товара / общая инфраструктура.
- Долговечный (межпроцессный) счётчик подряд-сбоев на инстанс + порог 3 → снятие + `reallocate`.
- Долговечный общий бюджет пересадок (один на оба повода: медленная тяга и битая нода).
- Задание с виной ноды возвращается в очередь на другую ноду и не двигает курсор.
- Единая плоская нумерация заданий: `total`, `--skip`, `--limit` из одного списка (1465 → 1503).
- Возврат ~10 потерянных SKU в очередь.
- Постобработка перестаёт умирать в OOM (иначе ориентация и топ-вью не доделываются).

## Скоуп — что НЕ входит
- Контракт `disposition/fault_scope` внутри воркера (нужна пересборка образа) — следующий шаг.
- Полный вынос постобработки в фон параллельно генерации — отдельный план (см. follow-up).
- Апгрейд DEV-VM по памяти — решение владельца.

## Файлы к изменению
- [x] `tools/scout/salad/ssh_run.py` — классификация ошибки, возврат задания в очередь,
      счётчик на инстанс, снятие + reallocate, `summary()` не двигает курсор по вине ноды.
- [x] `tools/scout/salad/batch_show.py` — `total` из плоского списка; `reallocate(iid, why)`
      как общая функция; бюджет пересадок — в файл, а не в память.
- [x] `tools/scout/mesh_queue.py` — повтор пишется в существующий `mesh_jobs.retry_wait`
      (готовая машина состояний, не заводить второй источник правды).
- [x] `tools/scout/salad/worker.py` — пустой `source.jpg` (статус `failed`) и `warm=True`
      после `warmup_error`.
- [x] `tools/scout/salad/topview_render.py` — 4 процесса → 1–2, микропачками.
- [x] `tools/scout/orient_worker.py` — `--limit 200` → 20, каждая микропачка отдельным процессом.
- [x] `tools/scout/salad/tests_pool.py` — тесты классификации, порога и бюджета.

## Задачи (порядок = приоритет)
- [x] P0. Плоская нумерация заданий: `total`/`skip`/`limit` из одного канонического списка.
- [x] P0. Классификатор ошибки на супервизоре: ENETUNREACH/EHOSTUNREACH/DNS → вина ноды;
      HTTP 404/410 → вина товара (терминально); timeout/SSL EOF/5xx → неизвестно, повтор на
      другой ноде, но счётчик ноды НЕ растёт.
- [x] P0. Вина ноды → задание в очередь (множество `tried` уже есть), курсор не двигается.
- [x] P0. Вина товара → терминально, но с долговечной записью причины (иначе теряется
      диагностика, как сейчас с `bad_cutout`).
- [x] P0. Вернуть ~10 потерянных SKU (собрать из `mesh-run-progress.jsonl`, свериться с
      наличием манифеста, поставить в очередь).
- [x] P1. Счётчик подряд-сбоев на `(group, instance_id)` в долговечном файле с TTL; порог 3
      (env `MESH_NODE_FAIL_STREAK`); обнуление на `ok`/`cached`/вердикт по товару.
- [x] P1. Порог достигнут → пометить ноду `retired` под `nodes_lock`, вернуть задание,
      **отпустить lock**, только потом звать `reallocate` (сеть до 30 с — под локом нельзя).
- [x] P1. Предохранители: общий бюджет 2/тик и 6/час в файле; не снимать, если тёплых нод
      останется < 2; один и тот же класс ошибки на ≥3 разных нодах за 5 мин → это общая сеть,
      пауза раздачи вместо выкашивания пула.
- [x] P1. `STALL_S` учитывает прогрев замены после reallocate (иначе снятие ноды приводит к
      «нет живых нод» и закрытию остатка).
- [x] P1. Проверить, что `reallocate` разрешён для инстанса в состоянии `running`
      (сейчас зовётся только для `downloading`, `batch_show.py:193`).
- [x] P1. `warm=True` после `warmup_error` → нода не берёт задания.
- [x] P1. Пустой `source.jpg` → не попадает в комплект (статус `failed` исчезает как класс).
- [x] P2. Память постобработки: топ-вью 1–2 процесса, ориентация пачками по 20, каждая —
      отдельным процессом; ограничить шаг через `systemd-run -p MemoryMax=3G`.
- [x] Тесты: 3 подряд вины ноды → снята; 2 вины + `ok` → счётчик обнулён; 3 подряд 404 →
      нода НЕ снята; рестарт процесса между 2-м и 3-м сбоем → счётчик выжил; общая сеть →
      бюджет не даёт выкосить пул; исчерпание попыток → задание в `retry_wait`, не в тишину.

## Критерии приёмки
- [x] Нода с 3 сбоями «по вине ноды» подряд снимается и переаллоцируется; в логе видно почему.
- [x] Три подряд HTTP 404 ноду НЕ снимают.
- [x] Счётчик и бюджет переживают перезапуск процесса между пачками.
- [x] Ни один отказ по вине ноды не двигает курсор `done`.
- [x] Конвейер запрашивает 1503 задания, а не 1465.
- [x] После прогона: SKU «в плане, но без меша и без места в очереди» = 0.
- [x] `orient_worker`/`topview_render` завершаются штатно, пик RSS каждого < 3 ГБ.
- [x] Lint / тесты проходят; нет отладочного вывода; файлы вне scope не тронуты.

## Definition of Done — память (без этого `completed` запрещён)
- [ ] `core/mesh-pipeline.md`: классификация отказов, автоснятие ноды, плоская нумерация
- [ ] ADR в `decisions.md`: «вина ноды vs вина товара» по коду ошибки; порог 3; бюджет пересадок
- [ ] `core/lessons.md`: смерть шага в OOM маскируется под «медленно»; счётчик в памяти
      процесса бесполезен там, где процесс перезапускается каждую пачку; число попыток ≠
      число потерянных товаров (30 против 10)
- [ ] `/memory-check` выполнен, audit «чисто»

## Лог выполнения
- 2026-09-01 — план создан (draft) по итогам замера скорости за час
- 2026-09-01 — разбор Codex, три вывода исправлены, добавлена потеря 38 заданий на `total`
- 2026-09-01 — «деплой»: статус in_progress, конвейер поставлен на паузу перед правками
- 2026-09-01 — реализовано и закоммичено (0134f17), стенд пула 17/17 зелёный, main запушен

## Completion summary
Отказ прогона классифицируется по ТЕКСТУ ошибки (`node_health.classify`), а не по статусу:
вина ноды / вина товара / непонятно. Вина ноды и «непонятно» возвращают задание в очередь и не
двигают курсор; 3 сбоя подряд по вине ноды снимают ноду из пула и переаллоцируют её. Счётчик
серии и ОБЩИЙ бюджет пересадок — в файле под flock, потому что каждая пачка это новый процесс.
Задание, побывавшее на двух нодах и всё равно упавшее, уходит в спул повторов с причиной и
счётчиком попыток (потолок 3), спул разбирает `batch_show.drain_retry_spool`. План прогона
считается одним плоским списком (1465 → 1503): последние 38 заданий больше не теряются.
`recover_lost.py` вернул 16 уже потерянных товаров. Постобработка перестала умирать в OOM:
топ-вью 4→2 процесса, ориентация микропачками по 20. `probe_warm` не берёт ноду с
`warmup_error`. Стенд пула — 17 случаев, зелёный. Коммит 0134f17, main запушен.

Отложено осознанно: контракт `disposition/fault_scope` внутри воркера (нужна пересборка образа —
правка `worker.py` про пустой `source.jpg` вступит в силу тогда же) и вынос постобработки в фон
параллельно генерации (это и есть главный резерв скорости: 38 мин простоя × 9 нод ≈ 5,7 GPU-часов
за одну паузу). Проверка автомата на живом прогоне — после волны соседней сессии.

### Уроки (ОБЯЗАТЕЛЬНО)
- «Умер по OOM» и «медленно работает» снаружи неотличимы. Три шага постобработки не тормозили, а
  НЕ ДОДЕЛЫВАЛИ работу — их убивал earlyoom после того, как они загоняли машину в своп. Смотреть
  `journalctl | grep SIGTERM` ДО того, как чинить логику шага.
- Число попыток ≠ число потерянных товаров: 30 отказов на ноде дали 16 потерянных SKU. Считать
  потери по журналу прогона нельзя (нет seed и полного id инстанса) — судить по дереву мешей.
- Счётчик, который копится МЕЖДУ пачками, нельзя держать в памяти: пачка = новый процесс, серия
  2+2+2 не дойдёт до порога. Та же ошибка была у бюджета пересадок (родитель и ребёнок вели
  разные журналы) — мой первый вариант плана содержал ровно её.
- Один статус ошибки может означать противоположные вещи: `input_failed` — это и мёртвая сеть
  ноды, и HTTP 404 на фото. Порог «3 подряд input_failed» выкинул бы здоровую ноду за три мёртвых
  ссылки. Классифицировать по коду ошибки; 404 доказывает, что сеть жива, и обязан ОБНУЛЯТЬ серию.
- Надпись в логе идущего процесса врёт про свежий код: ярлык шага «ремонт» остался в памяти
  супервизора, стартовавшего за 24 минуты до ревёрта. Сверять время старта процесса с коммитом.
- Свой же новый код надо перечитывать диффом: первая версия спула дублировала на GPU задания,
  которые и так возвращались сами (курсор их не пропускал). Поймано на самопроверке, не тестом.

## Follow-up work
- [ ] Разделить удалённый producer и локальный consumer: постобработка идёт параллельно
      следующей пачке (Codex проверил безопасность: `receiver.py:167` публикует `complete.json`
      последним, `drain.sh:23` копирует только такие каталоги — нужен один файловый lock)
- [ ] Контракт `disposition/fault_scope/phase/error_code` в воркере при следующей пересборке образа
- [ ] Журнал прогона: `run_id`, `job_key`, `seed`, полный instance/machine id
