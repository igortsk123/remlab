---
workstream: calc-materials
slug: calc-leadcard-highlight
title: «Найдём дешевле» — отдельной выделенной карточкой (под каждым товаром)
status: completed
created: 2026-07-22
updated: 2026-07-22
completed: 2026-07-22
---

## Цель
Вынести Шаг 2 («Найдём этот товар дешевле») из карточки ссылки в ОТДЕЛЬНУЮ визуально ВЫДЕЛЕННУЮ карточку.

## Источник задачи
Владелец: выделить этот блок отдельной карточкой и выделить визуально. 2 карточки при плитке
стены+пол (под каждым товаром) — подтверждено «да, логично».

## Скоуп
- [ ] `components/calc/LeadCard.tsx` (нов.) — карточка «Найдём этот товар дешевле» (подстрока + кнопки
      Телеграм/MAX/почта → `LeadModal`); принимает `kind`, `url`. Стиль `.lead-card` (акцентная рамка + лёгкая заливка).
- [ ] `components/calc/LinkAutofill.tsx` — убрать Шаг 2 (divider + «найдём дешевле» + боты + модалка);
      оставить Шаг 1 (ссылка + автоподгрузка + параметры). Убрать импорт `LeadModal`, TG/MAX, стейт modal.
- [ ] `components/calc/RoomPanel.tsx` — после каждого `LinkAutofill` рендерить `<LeadCard>` с его url
      (стены → `productUrl`, пол → `floorProductUrl`).
- [ ] `app/globals.css` — `.lead-card` (border `--accent` + tint).

## Скоуп — что НЕ входит
- Логика лида/ботов/модалки/парсинга — не меняем, только переезд.

## Критерии приёмки
- [ ] «Найдём этот товар дешевле» — отдельная выделенная карточка под блоком ссылки; кнопки/модалка работают.
- [ ] Плитка стены+пол → две карточки (под каждым товаром).
- [ ] `typecheck && lint && test && build` + e2e зелёные.

## Выкатка
typecheck/lint/test/build → commit → push → `./deploy.sh` → smoke + прод + VPN. Миграций нет.

## Лог
- 2026-07-22 — план + реализация (деплой).

## Completion summary
«Найдём этот товар дешевле» вынесено из LinkAutofill в отдельную выделенную карточку
`LeadCard` (`.lead-card`: акцентная рамка + заливка), рендерится в RoomPanel под каждым блоком ссылки
(стены → productUrl, пол → floorProductUrl). LinkAutofill оставил только Шаг 1. typecheck/lint/test(69)/build зелёные.
