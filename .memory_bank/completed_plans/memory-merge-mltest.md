---
workstream: memory
slug: memory-merge-mltest
title: Слияние — перенести mltest в репозиторий целиком (код, знания, секреты) и закрыть дыру «не знаю, что у нас есть»
status: completed
created: 2026-08-04
updated: 2026-08-04
completed: 2026-08-04
---

## Цель
Перенести в Memory Bank знания из **соседнего проекта `/home/pakar/mltest`** (замер комнаты по
фото, fit-check, подбор товаров, визуализация замены) и закрыть системную дыру: доступы и
наработки, лежащие вне репозитория, для агента «не существуют».

## Источник задачи
Владелец 2026-08-04: ключ fal.ai лежал в `/home/pakar/mltest/.env`, агент про него не знал и
спросил владельца «есть ли доступ». Далее: «mltest по сути это наш проект — он начинался с того,
что фото комнаты делали и там пытались мерить мебель… прошлые эксперименты в контекст памяти
разнести, это важно», «спланируй полностью слияние памяти».

## Что обнаружено (инвентаризация 2026-08-04)

**Расположение:** `/home/pakar/mltest` — ВНЕ репозитория (репо: `/home/pakar/igor/remlab`),
артефакты писались в `/home/pakar/remlab-temp/`. В памяти упомянут одной строкой в
`_kit/code-ref-ignore.txt` как «внешняя research-папка», содержимое не переносилось.

**Что там сделано (по коду):**
| Файл | Суть |
|---|---|
| `run_auto.py` | Полностью автоматический замер: вход только фото + модель телефона + рост + потолок; FOV выводится из модели телефона |
| `cv_refine.py`, `geometry_solver.py` | Масштаб по листу A4 + solvePnP; OpenCV уточняет геометрию вокруг seed'ов от Gemini |
| `run_plan.py` | **План пола сверху на геометрии пола (A4+solvePnP), без depth** — «надёжный результат без depth-хрупкости»; SegFormer ADE20K для пола/стен/окон |
| `run_f2.py`, `run_f3.py` | Иерархия «что на чём стоит»; само-проверяющий замер |
| `fit_check.py` | Детерминированный fit-check на Shapely: «влезет / велик на N см / что убрать» |
| `product_match.py`, `run_match.py` | Подбор РЕАЛЬНЫХ товаров, которые влезут в зону (цена, магазин, ссылка) |
| `run_viz.py` | Ф4 «вот так впишется»: стереть объект (LaMa) + вставить товар в ВЫЧИСЛЕННОМ масштабе. Принцип: «масштаб/позиция — детерминированный движок (истина); картинка — убеждалка» |
| `taxonomy.py`, `object_catalog.py`, `sizes_prior.py` | Таксономия классов и справочник типовых размеров предметов; само-проверка размеров |
| `gdino_test.py`, `openai_vision_name.py`, `openai_translate.py` | Grounding DINO + vision-именование объектов, дешёвый перевод ярлыков |
| `.env` | **FAL_KEY** (баланс $9.03 на 2026-08-04) и OPENAI_API_KEY |

**Пересечения с сегодняшней работой:** принцип «геометрия — истина, генерация — убеждалка»,
fit-check на Shapely, план сверху без depth, вставка товара в вычисленном масштабе — всё это
переоткрывалось заново 2026-08-02…04.

## Шаги

**Ш0. Доступы (сделать первым).** Внести в `core/access-and-integrations.md` все реальные доступы,
значения — в `_secrets/ACCESS.md`. Инвентаризация нашла 30 ключей, из них **12 не в реестре**
(fal.ai — внесён 2026-08-04; DaData, Magento, почтовые ящики и БД соседних проектов — пометить как
«не наши, не трогать»). Инструмент: `node tools/access-inventory.mjs` (новый, в git).

**Ш1. Tier 2 — новый доменный док `domain/room-measurement.md`.** Перенести: конвейер замера
(A4+solvePnP → сегментация → план пола), что работает и что хрупко (depth), справочники
(таксономия, каталог размеров), fit-check, подбор товаров, схему визуализации замены. С пометкой
`source: external:/home/pakar/mltest` и ссылками на конкретные файлы.

**Ш2. Tier 1 — сводка `core/room-measurement.md`** (≤3 КБ) со ссылкой `tier2:` вниз, чтобы тема
попала в decision tree INDEX и была видна на старте сессии.

**Ш3. Уроки.** В `core/lessons.md` — только то, что меняет решения: план сверху надёжнее строить
на floor-геометрии, чем на depth; масштаб/позиция считаются детерминированно, генерация не имеет
права их менять; LaMa-стирание + вставка в масштабе как рабочая схема «вот так впишется».

**Ш4. Связь с текущими планами.** Отметить в [[llm-layout-planner]] и [[prod-layout-engine]],
что fit-check и «план без depth» уже реализованы в mltest и переиспользуются, а не пишутся заново.
В `anti-patterns.md` — что переоткрытие уже сделанного случилось из-за отсутствия реестра.

**Ш5. ПЕРЕНОС КОДА В РЕПОЗИТОРИЙ** (решение владельца 2026-08-04: «всё вместе, к нам перезалить,
включая секреты»).
- `mltest/*.py` → `services/room-measure/` (сохранить структуру и имена, добавить README с
  назначением каждого файла и командой запуска).
- Артефакты прогонов (`*.jpg`, `*.npy`, `cache/`, `tmp/`, `venv/`, `weights/`, `yolov8s-world.pt`
  27 МБ) в git НЕ тащим — они воспроизводимы; в README указать, где их взять/как получить заново.
  Исходные фото комнат (`room*.jpg`) — перенести, они входные данные экспериментов, не мусор.
- `requirements.txt` собрать из фактических импортов (torch, transformers, opencv, shapely, ultralytics).
- Пути `/home/pakar/mltest` и `/home/pakar/remlab-temp` в коде заменить на относительные от репо.
- Исходный каталог не удалять до подтверждения, что перенесённое запускается; потом заменить
  README-указателем (правило memory-discipline: две копии = дивергенция).

**Ш5б. Секреты.** `mltest/.env` → значения в `_secrets/ACCESS.md` (вне git), в
`core/access-and-integrations.md` — только указатель. Ключ `FAL_KEY` уже внесён 2026-08-04.
Проверить, что после переноса код читает ключи из общего места проекта, а не из своего `.env`.

**Ш6. Правило против повторения.** В `.claude/rules/memory-discipline.md` — перед словами «у нас
нет доступа/этого не делали» прогонять `tools/access-inventory.mjs` и проверять реестр интеграций;
раз в неделю (или на `/memory-check`) — сверка реестра с фактическими ключами на машине.

## Completion summary (2026-08-04)
Сделано: код перенесён в `services/room-measure/` (18 скриптов, справочники, фото комнат;
пути переписаны на репозиторий, `/tmp/room-measure` для артефактов, requirements собран);
веса/venv/кэш оставлены на месте и добавлены в `.gitignore`; в старой папке — README-указатель.
Знания: `domain/room-measurement.md` (Tier 2) + `core/room-measurement.md` (Tier 1, в decision tree).
Доступы: fal.ai в реестре и `_secrets/ACCESS.md`, чужие ключи соседних проектов помечены как чужие;
`tools/access-inventory.mjs` показывает «всё найденное учтено». Правило инвентаризации — в
`.claude/rules/memory-discipline.md`. Уроки 57–58.
**Не сделано:** запуск `run_plan.py` из репозитория не проверялся (нужен venv с torch ~2 ГБ) —
проверить при первом реальном использовании.
**Уроки:** наработки вне репозитория для агента не существуют; план сверху надёжнее на геометрии
пола, чем на карте глубины.

## Критерии приёмки
- [ ] `node tools/access-inventory.mjs` показывает 0 доступов «не в реестре» (чужие — помечены как чужие)
- [ ] Тема «замер комнаты» видна в decision tree INDEX и находится за один drill
- [ ] В `domain/room-measurement.md` перечислены все компоненты mltest с назначением и статусом
- [ ] `services/room-measure/` запускается из репозитория (проверен хотя бы `run_plan.py` на сохранённом фото)
- [ ] В git не попали веса моделей, venv и кэш; `.gitignore` дополнен
- [ ] `/home/pakar/mltest` заменён README-указателем на новое место
- [ ] `/memory-check` чистый

## Definition of Done — память
- [ ] ADR (реестр доступов + судьба mltest); `/memory-check`; audit «чисто»
