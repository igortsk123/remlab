---
tier: 1
topic: decisions
scope: ADR-лог — архитектурные решения с обоснованием и влиянием
tier2: "../docs/DECISIONS.md"
updated: 2026-07-22
importance: high
source: manual
status: stable
source_of_truth: canonical
last_verified: 2026-07-12
review_after: ""
---

# Decisions — ADR-лог

> Полные ADR с контекстом/последствиями — в `docs/DECISIONS.md`. Здесь — короткая навигация по решениям.

## [2026-07-01] Стек проекта
**Решение:** TypeScript (strict) end-to-end: Next.js (App Router, full-stack) + Drizzle + Zod (контракты на границах) + Inngest (durable jobs) + внешний инференс (Vertex/fal/Replicate за провайдер-интерфейсом) + YooKassa + Sentry + PostHog + Vitest/Playwright + GitHub Actions.
**Почему:** соло + не-кодер → один рантайм и сквозные типы убирают класс ошибок, которые владелец не читает.
**Альтернативы:** Python+TS два рантайма — отвергнуто (два деплоя, рассинхрон).
**Влияет на:** `core/architecture.md`, структуру `/modules`, `/contracts`.

## [2026-07-01] Self-host на exit-fi вместо Vercel — ADR-0001 (docs/DECISIONS.md)
Изолированный docker-compose на Hetzner exit-fi. Причина: свой сервер, контроль, дёшево. Fallback — Vercel+Supabase.

## [2026-07-01] БД в контейнере postgres:17+pgvector — ADR-0002
Не Supabase Cloud (полный self-host Supabase не влезает рядом с VPN). Auth/Realtime — интерим (anonymous id + polling), позже отдельным ADR.

## [2026-07-01] TLS Let's Encrypt TLS-ALPN-01 на :443 — ADR-0003
Домен GoDaddy A→сервер напрямую; :80 закрыт. Caddy авто-выпуск/продление на :443.

## [2026-07-01] Щедрые лимиты памяти контейнеров — ADR-0004
app 1G / pg 1G (shared_buffers 128MB) / caddy 128M — защита VPN-ноды от OOM.

## [2026-07-01] Дисковая гигиена/автоочистка + swap 4G — ADR-0005
Ротация логов Docker, scoped-очистка образов, weekly timer, ротация бэкапов, df-watchdog, swappiness=10.

## [2026-07-01] Образы под linux/arm64 — ADR-0006
exit-fi оказался ARM (aarch64). Собираем кросс-сборкой buildx `--platform linux/arm64` + binfmt. `deploy.sh` делает это сам. Обычный amd64-билд не запускается на сервере.

## [2026-07-01] Провайдер ИИ = Gemini для обеих задач (M0) — ADR-0007
**Решение:** и генерацию картинок, и анализ фото делаем через Google Gemini одним ключом:
картинки `gemini-3.1-flash-image` (Nano Banana 2), текст/зрение `gemini-flash-latest`. Провайдер за
интерфейсом (`lib/providers/`), сменный под задачу. Детали доступа — `core/access-and-integrations.md`.
**Почему:** ключ Gemini доступен и проверен; Nano Banana 2 силён в editing/consistency по эталонному
фото (наш кейс «перерисовать комнату, сохранив её»); анализ фото у всех стоит доли цента — не рычаг;
OpenAI-ключа в наличии нет. Одна интеграция = проще.
**Альтернативы:** OpenAI GPT-Image (дороже ~$0.167 hi; выше в бенчах text-to-image, не в editing) /
Imagen 4 Fast ($0.02, слабее в editing) — отложены, подключаемы через сменный провайдер.
**Влияет на:** `core/access-and-integrations.md`, `lib/providers/*`, экономику M4/paywall (резолюция).

## [2026-07-01] Каркас Stage 1: in-memory store + Zod как источник формы — ADR-0008
**Решение:** для каркаса Stage 1 источник правды по данным — Zod-контракты (`/contracts`, тип через
`z.infer`), рантайм-хранилище — за интерфейсом `ProjectRepository` с in-memory реализацией
(`modules/store/`). Модель упрощена до одного агрегата `Project` (вместо 11 таблиц спеки §4).
Настоящий Postgres/Drizzle подключается на шаге деплоя заменой реализации Repository.
**Почему:** локальной БД нет, CI должен быть герметичным/быстрым, миграции без живой БД дают дрейф.
Абстракция даёт рабочий проходимый flow сейчас и чистую замену на PG позже без правок вызывающих.
**Альтернативы:** поднимать Postgres в CI/локально сразу — отложено (лишняя связность на этапе каркаса).
**Влияет на:** `modules/store/`, `contracts/*`, план деплоя (там PgRepository + миграции).

## [2026-07-01] Продуктовые решения Stage 1 (владелец) — ADR-0009
**Решения владельца:** (1) дизайн — тёплый **japandi** (крем/беж/greige, дерево, шалфей/терракота);
(2) AI-превью = **перерисовка ФОТО пользователя** (сохраняем геометрию комнаты, меняем стиль/декор),
не картинка-вдохновение; честный дисклеймер «это концепт, проверьте размеры»;
(3) сценарий **«Рассчитать стоимость» — fake-door «Скоро»** (Cost Engine позже, считает движок, не LLM).
**Влияет на:** `app/globals.css` (тема), `modules/visual-generation` (restyle по эталону), `/soon`, `core/user-flow.md`.

## [2026-07-01] Фейковый ИИ-провайдер по флагу для тестов/e2e — ADR-0010
**Решение:** `REMLAB_FAKE_AI=1` → `lib/providers/fake.ts` (детерминированные заглушки в тех же контрактах).
e2e и unit гоняются без ключей и трат; реальный прогон — без флага (Gemini).
**Влияет на:** `lib/providers/index.ts`, `playwright.config.ts` (webServer), CI.

## [2026-07-01] Postgres активирован + авто-деплой через GitHub — ADR-0011
**Решение:** (1) БД — Postgres (`projects` = jsonb-агрегат, `db/schema.ts` Drizzle, `modules/store/pg-repository.ts`).
Выбор реализации в `repo()`: `DATABASE_URL` задан → PG (прод), иначе in-memory (локально/тесты). Миграция —
`tools/migrate.mjs` (идемпотентно) + `db/init/002-projects.sql` (свежая БД); на сервере применяется через
контейнер db (`psql -f`, deploy.sh шаг 5b). (2) Деплой **по умолчанию автоматический через GitHub Actions**:
`.github/workflows/deploy.yml` запускается после зелёного «CI gate» на main, собирает arm64-образ **в раннере**
(не на боевом сервере — VPN-нода) и катит через `deploy.sh` (save|ssh load|compose up|migrate|smoke|rollback).
**Требует (owner, разово):** секреты репозитория `DEPLOY_SSH_KEY` (ssh root@сервер) и `DEPLOY_HOST=89.167.127.0`;
на сервере `/opt/remlab/.env` должен содержать `GEMINI_API_KEY` (+ `POSTGRES_PASSWORD`). Без секретов деплой-джоба
безопасно пропускается. Прямой SSH к проду мной не используется (только через GitHub-автоматизацию).
**Почему:** тяжёлая сборка вне фрагильного сервера; воспроизводимый, откатываемый деплой без ручных шагов.
**Влияет на:** `docker-compose.yml` (GEMINI_API_KEY в app), `deploy.sh`, `.github/workflows/*`, `core/access-and-integrations.md`.

## [2026-07-01] Observability = PostHog (без Sentry на старте) — ADR-0012
**Решение:** аналитика и ошибки — через **PostHog** (бесплатный тариф: 1M событий/мес, EU-хост `eu.i.posthog.com`
для RU-данных). Лёгкий серверный слой `lib/analytics.ts` (`track`/`captureError`) — no-op без `POSTHOG_KEY`.
Sentry отдельно НЕ заводим на старте (PostHog free покрывает и ошибки).
**Почему:** один бесплатный инструмент вместо двух; события + ошибки в одном месте; включается ключом без правок кода.
**Влияет на:** `lib/analytics.ts`, `app/actions.ts`, `app/api/p/[id]/generate`, `.env.example`, `core/access-and-integrations.md`.

## [2026-07-02] Трейсинг AI-пайплайна (подробный лог + «номер генерации») — ADR-0013
**Решение:** сквозной трейсинг каждого прогона пайплайна. (1) **Захват — в слое провайдеров:**
`getImageProvider/getVisionProvider` оборачиваются инструментированно (`lib/providers/traced.ts`) —
любой вызов LLM логирует шаг в активный прогон; прогон открывается в оркестраторе через `runWithTrace`,
контекст — AsyncLocalStorage. Так лог не отстаёт при смене модели/промпта/шага (новый вызов логирует
себя сам). (2) **Данные:** таблицы `generation_runs` (seq = человекочитаемый номер генерации через
sequence), `generation_steps` (модель/промпт/настройки/вход-выход/время/стоимость/ошибка),
`generation_assets` (картинки — **байты на диске/томе**, в БД только ссылка; base64 в БД не пишем).
(3) **Версионирование:** реестр промптов `lib/prompts/registry.ts` (id+version) и реестр пайплайнов
`lib/pipelines/registry.ts` (сценарий = шаги+модель+промпт) — точка расширения под Nano→GPT→ControlNet/SD.
(4) **Сжатие:** сервис **imagor** (Go+libvips, internal-only, как в SUP2 ADR D18) уменьшает фото
(`fit-in 1536`, webp q80) ПЕРЕД LLM → экономия токенов; сохранённый input-ассет = то, что реально ушло.
(5) **Разбор:** скилл `/trace` + `pnpm trace <N>` + admin-роут `/api/trace/<N>` (краткий+подробный+фото);
кнопка «Сообщить о проблеме» (`/api/trace/report`) для пользователей. Ретеншн `TRACE_RETENTION_DAYS=90`.
**Почему:** владельцу нужны сотни тестов пайплайна (сравнение моделей/промптов/настроек по номеру),
быстрый разбор ошибок и пользовательские жалобы по номеру генерации; логирование не должно требовать
переписывания при эволюции пайплайна. **Альтернативы (отклонены):** сторонний LLM-observability (Langfuse
и т.п.) — лишняя внешняя зависимость/данные вне периметра; логирование в каждом модуле вручную — рассинхрон
при смене пайплайна. **Влияет на:** `lib/trace/*`, `lib/providers/*`, `lib/prompts/`, `lib/pipelines/`,
`lib/images/compress.ts`, `db/schema.ts`, `tools/migrate.mjs`, `db/init/003-traces.sql`, `app/api/trace/*`,
`app/p/[id]/preview`, `docker-compose.yml` (imagor+том), `.claude/rules/pipeline-tracing.md`,
`core/observability-tracing.md`. **Решение владельца:** диск для фото, все фазы сразу, ретеншн 90 дн.

## [2026-07-02] Пивот бизнес-модели v0.2 → v0.3 (affiliate-first freemium) — ADR-0014
**Решение:** принят **Master Project Brief v0.3** (`docs/master-brief-v0.3.md`) как мастер-документ с
приоритетом выше v0.2. Ключевые сдвиги: (1) **freemium-граница** — бесплатно полноценный подбор **до N
(3–5) реальных товаров из фидов с открытыми реф-ссылками** (двигатель affiliate, доход ~3%, сеть Гдеслон);
платно — «комната целиком» (все элементы) + сервисный слой (Cost Engine, чек-лист, план, PDF, workspace).
Товары/ссылки БОЛЬШЕ НЕ за paywall. (2) **Три ступени монетизации складываются:** affiliate → paid room pack
(розница ~2 990 ₽; дизайнеры-B2B ~990 ₽/комната от 10) → застройщики «квартира+ремонт в ипотеку» (~5% с чека,
vision/Stage 4). (3) **Product matching** = «генерация → поиск похожего в большой базе фидов по embeddings»
(pgvector), метрика similarity с 1-го дня. (4) **Рынок РФ→UK, locale-agnostic** (валюта/фиды/сети/rates через
абстракции). (5) **Канал** — дешёвый инфо-трафик DIY-кластера (~10 ₽ CPC) + SEO (SSR/SSG); **главная
falsifiable-гипотеза** юнит-экономики (1 покупка/~45 визитов, чек ~30 000 ₽, доход ≈ 2× CAC), kill-критерий
в Stage 0. (6) **Второй вход** «найти похожую мебель по фото» (без генерации). (7) **Сегмент F** — дизайнеры
(B2B-опт, batch/выгрузки, Stage 1.5).
**Уточнения владельца (2026-07-02) — граница free/paid = уровень «что сделать с комнатой» (3 варианта):**
1. **«Освежить без ремонта» — БЕСПЛАТНО:** визуализация + подбор до 3 реальных предметов мебели по
   визуализации (открыто, реф-ссылки, можно заменить). Без материалов/сметы/плана.
2. **«Недорого обновить» — ПЛАТНО ~1 490 ₽** (розница; владелец называл ~1000 — параметр): мебель в любом
   количестве + материалы. Без расчёта стоимости ремонта, без чертежей/дизайнера.
3. **«Ремонт под ключ» — ПЛАТНО 9 900 ₽** (гипотеза, рынок сверен 2026-07-02): мебель + материалы +
   гайды + чертежи + **смета (Cost Engine)** + **услуга живого дизайнера**. Замена дизайнера, но в 5–10×
   дешевле (дизайн-проект комнаты в РФ ~45–100 тыс Москва / ~25–50 тыс регионы).
- Реф-ссылки во всех вариантах (affiliate). Материалы подняты в платные Stage 1 (в брифе были Stage 2).
- **Код-долг:** структура `FREE_VISIBLE=3` верна для вар.1; нужно реальные товары из фидов + реф-ссылки
  (сейчас seed), добавить варианты 2/3, согласовать с `interventionLevel`. «Раздать всё бесплатно» — НЕВЕРНО.
- **Postgres self-host подтверждён владельцем** — «Supabase» из брифа не берём (ADR-0001/0002). Конфликт закрыт.
**Почему:** товарная инфраструктура (фиды Гдеслон) + измеримый DIY-спрос делают affiliate реалистичным
первичным потоком; открытые товары максимизируют клики; paid продаёт объём+сервис, а не доступ к ссылкам.
**Альтернативы (отклонены):** v0.2-граница «товары за paywall» — душит affiliate-двигатель; inpainting
реальных товаров в Stage 1 — сложно/дорого, выбран matching по embeddings. **Влияет на:**
`docs/master-brief-v0.3.md` (новый), `source-of-truth.md`, `CLAUDE.md`, `product_brief.md`, `core/market.md`,
`core/user-flow.md`, `core/data-model.md`, `core/access-and-integrations.md`, `project-state.md`, `INDEX.md`;
код (позже): `modules/ideas` (открыть товары+реф-ссылки), `app/p/[id]/preview`+`/paywall`, новый пайплайн фидов
и affiliate-трекинг. **Открытые параметры (тесты):** N (3/5), цена room pack, порог similarity, лимит генераций.

## [2026-07-02] Git-workflow: всегда коммитить+пушить в GitHub — ADR-0015
**Решение:** по указанию владельца — после каждого завершённого изменения кода Claude **сам коммитит и
пушит в GitHub** (`origin` = git@github.com:igortsk123/remlab.git), не дожидаясь отдельной команды «коммить».
**Почему:** владелец не пишет код и хочет, чтобы история и деплой-артефакты всегда были в удалённом репо
(бэкап, авто-деплой через GitHub Actions, ADR-0011), без ручных напоминаний. **Влияет на:** переопределяет
пункт «commit/push только по просьбе» из `.claude/rules/agent-workflow.md` — теперь коммит+пуш это дефолт
завершения работы. Коммиты — с co-author trailer. **Границы:** ветки фич `feature/<area>`; секреты не
коммитим (только `.env` на сервере); разрушительные git-операции (force-push, reset --hard) — по-прежнему
с подтверждением. **Альтернативы (отклонено):** копить локально и пушить по запросу — владелец теряет из виду
состояние и рискует потерять работу.

## [2026-07-11] Пивот концепции v0.4 «Смета-first» — ADR-0016
**Решение:** владелец утвердил новую концепцию продукта целиком: ядро — «Смета-лист» (расчёт
количеств и стоимости ремонта/материалов → сохранённый список с реф-ссылками → комиссия с покупок,
в т.ч. по ссылкам, которые пользователь принёс сам, через deeplink). Два входа: А «калькуляторы
материалов» (~70–90k показов/мес, ответ-числом сразу) и Б «сколько стоит ремонт» (~52k+, вилка
трёх бюджетов). AI-визуализация по фото и мебельный подбор — ступень воронки («как будет
выглядеть»), а не вход; мастера — партнёрский лид после сметы (не свой каталог). Мастер-план —
`plans/MASTER-cost-first.md` (М0–М7); утверждённый пользовательский сценарий — там же.
**Почему:** ценовой спрос на порядок больше AI-дизайнерского (52k+90k против 15–18k) при клике
7–36 ₽ (против 62–194 ₽ у «под ключ»); интент момента покупки материалов монетизируется рефами
с высокой конверсией («он покупает то, что сам выбрал»); чек материалов 50–200 тыс. ₽.
**Альтернативы (отклонены):** пивот в лидген бригад (красный океан: Яндекс Услуги/Профи, чужой
бизнес-профиль); остаться в мебельном AI-дизайне как ядре (мал спрос, гики-туристы в трафике).
**Влияет на:** `CLAUDE.md` (шапка), `project-state.md`, `product_brief.md`, `core/goals.md`,
`core/user-flow.md`, `core/market.md` (пометки пивота, полная переработка — по ходу М-этапов);
ревизия планов: commercial-master-plan и MASTER-roadmap + 8 sub-планов → архив (таблица «Судьба
прежних планов» в мастере); кампании Директа: Этап 2/4 становятся главными посадочными.
`docs/master-brief-v0.3.md` остаётся исторической базой (мебельная часть = ступень М5).

## [2026-07-12] Сквозная навигация по сценариям + разделы-каркасы — ADR-0017
**Решение:** одна сквозная шапка `components/SiteHeader.tsx` (смонтирована в `app/layout.tsx`) на
ВСЕХ страницах: логотип + «Моя лаборатория» (строка 1); ниже две ВЫДЕЛЕННЫЕ кнопки-калькулятора
«Посчитать материалы» (`/calc`) и «Сколько стоит ремонт» (`/calc/remont`) + лёгкие ссылки Дизайн
(`/start`) · Стили (`/styles`) · Советы (`/sovety`). Узкий экран — полоса разделов прокручивается
вбок (без «гамбургера»); решение владельца — «все разделы на виду, две кнопки выделить». Добавлены
разделы-КАРКАСЫ (контент позже — решение владельца): `/styles` (рабочая игра-карточки «узнай свой
вкус» → стиль → CTA дизайн/смета; статьи по стилям — плейсхолдеры), `/sovety` (плитки-советы —
плейсхолдеры). Личный хаб назван «Моя лаборатория» (`/lab`, на бренд remont-lab) — агрегирует «Мои
сметы» (`/estimates`) и «Мои дизайны» (`/rooms`); полноценный вход по паролю — позже. Главная
(`app/page.tsx`) пересобрана в «о проекте целиком» (герой «ремонт своими руками — хорошо и недорого»
+ витрина всех сценариев), не только калькулятор. Мини-воронка: событие `quiz_completed`
(`lib/analytics.ts` + серверный экшен `app/styles-actions.ts`).
**Почему:** владелец хотел единую верхнюю навигацию по сценариям; две кнопки-калькулятора = денежная
воронка (входы А/Б) — на каждой странице; статьи/советы = будущий SEO-контур (М7), сейчас нужен каркас.
**Альтернативы (отклонены):** всё под «гамбургер» (прячет воронку); все пункты равнозначными (две
главные кнопки теряются).
**Влияет на:** `app/layout.tsx`, `components/SiteHeader.tsx`, `components/StyleQuiz.tsx`,
`lib/styles/quiz.ts`, `app/{page,styles,sovety,lab}/page.tsx`, `app/globals.css`,
`app/styles-actions.ts`, `lib/analytics.ts`, `core/user-flow.md`, `core/architecture.md`.
**Место в мастере:** аддитивный UX-слой; поднимает входы М2/М3 в шапку, выводит хвост М5 (`/start`),
закладывает каркас М7 (Стили/Советы). Ядро сметы М1 не тронуто.
**content-later:** тексты статей/советов; реальные фото интерьеров для квиза (сейчас плейсхолдеры-
палитры); галерея «до/после»; настоящий аккаунт/вход.

## [2026-07-12] Калькулятор материалов v2 — детальный расчёт (серия К0–К6) — ADR-0018
**Решение:** строим детальный калькулятор (мультикомната, проёмы/зоны, полные параметры материалов,
автозаполнение по ссылке, платные хвосты) серией подпланов К0–К6 (`plans/calc-*`, роадмап
`plans/calc-materials-roadmap.md`). Архитектура: (1) рабочее состояние — КЛИЕНТСКОЕ, локально в
сессии (`localStorage`; `contracts/calc.ts` + `lib/calc/*`); сервер/БД трогаем только на «Сохранить
смету» → materialize в `Estimate` (M1, К3). (2) Один расчёт = один вид материала (kind на проекте,
из роута `/calc/[kind]`); смета копит несколько расчётов (владелец 2026-07-12). (3) Формулы — чистые
функции (`lib/estimate/calc.ts`) + golden-тесты. (4) v2 под флагом `CALC_V2`/`?v2=1` до К3, затем промоут в дефолт (флаг снят на К3); прод-дефолт
до К3 — прежний калькулятор. (5) Платёж (К5 YooKassa) и
боты (К6 Telegram/MAX) — отдельные ступени с деградацией без ключей; ПДн — TODO (юрист).
**Почему:** локальное состояние = отзывчивый билдер без БД; флаг = нет регресса между фазами;
чистые формулы+golden = расчёт не врёт. **Альтернативы (отклонены):** серверный state на каждый шаг
(лишние раунд-трипы); один гигантский план (не проверяемо по частям). **Влияет на:** `contracts/calc.ts`,
`lib/calc/*`, `components/calc/*`, `app/calc/[kind]`, `lib/estimate/calc.ts` (К2), серверный
calc-экшен сохранения (К3), YooKassa/боты (К5–К6), `core/estimate.md`, `core/architecture.md`.

## [2026-07-21] UX/копирайт калькулятора обоев — ADR-0019
**Решение:** проход по UX и текстам калькулятора обоев (`/calc/oboi`). (1) **Проёмы у обоев скрыты
из UI** — вместо ручного ввода окон/дверей приглушённая иконка-«окно» с подсказкой; для плитки/краски
проёмы ОСТАЮТСЯ (там вычитаются из площади). Основание: `wallpaper()` в `lib/calc/formulas.ts` и так
игнорирует проёмы (расчёт по периметру × макс. высоте — проёмы идут в запас целых полос), поэтому
поле только путало. (2) **Копирайт параметризован по kind** через `CALC_META` (`lib/estimate/companions.ts`):
добавлены падежные формы `titleGen` («Сколько нужно обоев»), `accYour` («ссылку на выбранные вами
обои»), `accPick` («подберём те же обои»), `work` («материалы для поклейки») — так oboi получает
точный текст владельца, а плитка/краска/ламинат (общие компоненты) остаются грамматичными.
(3) **Склонения единиц** — общий хелпер `lib/format/plural.ts` (`plural`/`pluralUnit`: рулон/рулона/
рулонов, упаковка склоняется, м²/л/шт инвариантны), применён в `ResultView` (Итог) и `RoomPanel`;
дубль `plural` из `app/lab/page.tsx` вынесен туда же. (4) **Порядок хвостов** на `/calc/[kind]`:
Итог → «Также не забудьте» (companions чек-листом с ✓, был «Заодно не забудьте» чипами) → «Найдём
выгоднее» (был «Найти дешевле») → «Как это будет выглядеть». (5) Визуальный divider между размерами
стен и ссылкой на товар; CSS-паттерны `.help`/`.checklist`/`.divider` в `app/globals.css`.
**Почему:** тексты калькулятора писались обобщённо и путали (двоеточие в заголовке, проёмы, «рулон»
без склонения); задача владельца — человечный, «непустой» экран обоев. Формулы и их golden-тесты не
тронуты (только отображение). **Влияет на:** `lib/estimate/companions.ts`, `lib/format/plural.ts`
(нов.), `app/calc/[kind]/page.tsx`, `components/calc/{SurfaceEditor,RoomPanel,LinkAutofill,ResultView,FindCheaper,VizCta,CalcBuilder}.tsx`,
`app/lab/page.tsx`, `app/globals.css`, `core/estimate.md`.

## [2026-07-21] Калькулятор обоев раунд 2: парсинг рулона + лид город/мультитовар — ADR-0020
**Решение:** (1) **Парсинг ссылки обоев** (`lib/calc/link-parse.ts`): ловим размеры рулона в формате
«ширина × длина м» (напр. «1,06*10м»; разделитель `*`/`x`/`х`/`×`, десятичная `,`/`.`) из og-меты и
раппорт «рап+орт … N см/мм/м» из body со снятыми тегами (значение в СОСЕДНЕМ `<td>`; «Рапорт» часто с
одной «п»). **Грабля:** `\b` после кириллической «м» НЕ срабатывает (кириллица вне `\w` в JS-regex) →
вместо `\b` негативный lookahead `(?![а-яёa-z])`. Пустой лейбл-поле «раппорт (см)» без числа не матчим.
(2) **Лид «Найдём выгоднее»** расширен: несколько ссылок на товар (склеиваем в `url` по строке) + город
(новая колонка `leads.city`, аддитивный идемпотентный `ALTER … ADD COLUMN IF NOT EXISTS` в `db/init/005-leads.sql`).
Текст блока по kind через `CALC_META.accYours` («ваши обои»/«вашу плитку»…; бывш. `accPick`).
**Иконка проёмов** → дверь (прямоугольник + точка-ручка), новый текст подсказки. **Почему:** реальная
ссылка магазина (miroboev) отдаёт размеры и раппорт, но парсер их не брал; лиду нужны товары+город для
подбора. **Влияет на:** `lib/calc/link-parse.ts`, `tests/unit/calc-link-parse.test.ts`,
`components/calc/{SurfaceEditor,FindCheaper,VizCta}.tsx`, `app/lead-actions.ts`, `modules/leads/repository.ts`,
`db/schema.ts`, `db/init/005-leads.sql`, `lib/estimate/companions.ts`.

## [2026-07-21] Плитка: пол своей плиткой (стены+пол раздельно) + парсинг крупных страниц — ADR-0021
**Решение:** (1) **Плитка для пола** отдельной плиткой: комната получила ВТОРУЮ спеку `floorMaterial`
+ `floorProductUrl` (`contracts/calc.ts`). Абстракция `computeRoomParts(room,kind)` (`lib/calc/formulas.ts`)
для плитки возвращает части «Стены» (по `material`) и «Пол» (по `floorMaterial`) — каждая своей
ссылкой/параметрами и ОТДЕЛЬНОЙ строкой Итога/позицией сметы; прочие виды — одна часть = `computeRoom`.
`RoomPanel` для плитки: секции «Стены»/«Пол» («+ добавить размеры пола» / «удалить пол»), у каждой свой
`LinkAutofill`+`MaterialParams`. Части подписаны нейтрально — НЕ привязано к «ванной» (любое помещение).
(2) **Парсинг крупных карточек:** лимит HTML в `parse-link` 500 КБ → **2 МБ** (цена stroypark лежала за
500 КБ); цена — допускаем пробел-разделитель тысяч («2 220.00»→2220); размеры «AxBсм» → в мм (×10:
«20х20см»→200×200). **Почему:** пол — частый кейс плитки (санузел/коридор) с ДРУГОЙ плиткой, чем стены;
страницы магазинов >1 МБ резались и не отдавали цену/размер. **Альтернатива (отклонена):** суммировать
стены+пол одной плиткой (неверно при разных плитках). **Влияет на:** `contracts/calc.ts`,
`lib/calc/formulas.ts`, `components/calc/{RoomPanel,ResultView,CalcBuilder,LinkAutofill}.tsx`,
`lib/calc/to-estimate.ts`, `app/api/calc/parse-link/route.ts`, `lib/calc/link-parse.ts`.

## [2026-07-21] Плитка/краска: проёмы по галочке (дефолт — полная площадь) + UX — ADR-0022
**Решение:** для плитки И краски проёмы (окна/двери) теперь вычитаются ТОЛЬКО по галочке «Учесть окна
и двери» (`room.countOpenings`, дефолт ВЫКЛ → площадь полная/gross). Тип проёма (окно/дверь/проём) и
поле «кол-во» убраны — проём = Ширина×Высота (тип на площадь не влияет; count=1, нужно больше — добавить
ещё). Выбор net/gross — в `computeRoom` (краска) и `computeRoomParts` (плитка-стены). Обои — без изменений
(проёмы скрыты, DoorHint). Плюс UX: кнопка «Заполнить» зелёная (`--accent`, как `nav-cta--alt`); зоны пола
«+ площадь»/«- площадь»; вверху калькулятора кнопка-ссылка «Найдём выгоднее» → smooth-scroll к нижнему
блоку (`#find-cheaper`), не дубль формы. **Почему:** раньше плитка/краска молча вычитали проёмы (занижение
без запаса на подрезку/бой); владелец хочет полную площадь по умолчанию и опциональный учёт. **Влияет на:**
`contracts/calc.ts`, `lib/calc/formulas.ts`, `components/calc/{SurfaceEditor,RoomPanel,FloorEditor,LinkAutofill,CalcBuilder}.tsx`.

## [2026-07-21] Калькулятор: дробный ввод (NumInput) + липкая шапка итогов (Заход A) — ADR-0023
**Решение:** (1) **`components/calc/NumInput.tsx`** — стандарт числового ввода: держит «сырую» строку,
принимает «.» и «,» (и промежуточные «1,»/«1.»), наружу отдаёт число, ресинк по внешнему value
(автозаполнение). Заменил сырые `<input>`+`strToNum`/`numToStr` во всех полях (`SurfaceEditor`,
`FloorEditor`, `MaterialParams`); прежний строковый хелпер (num) удалён. Причина: контролируемый round-trip
`numToStr(strToNum())` стирал промежуточный ввод → дробь не набрать. (2) **Липкая шапка итогов**
(`.calc-sticky` в `CalcBuilder`): площадь + стоимость (при цене), живой пересчёт; липнет под сквозной
шапкой через `--site-header-h`, которую публикует `SiteHeader` (ResizeObserver). (3) Автоподстановка
высоты первой стены пустым/новым стенам; (4) группа «Учесть окна и двери» (`.openings-group`) визуально
связывает чекбокс и проёмы. Это ЗАХОД A большого батча; Заход B (скрыть «Найдём выгоднее» + перестройка
блока ссылки: боты/почта-модалка/аккордеон/отдельная карточка) — следом. **Влияет на:**
`components/calc/{NumInput(нов.),SurfaceEditor,FloorEditor,MaterialParams,CalcBuilder}.tsx`,
`components/SiteHeader.tsx`, `app/globals.css`.

## [2026-07-21] Лид переехал в блок ссылки; FindCheaper удалён; модалка почты (Заход B) — ADR-0024
**Решение:** блок «Найдём выгоднее» (`FindCheaper`) удалён; лид-функции (e-mail/боты/согласие) переехали
в блок ссылки `LinkAutofill`, ставший самодостаточным материал-блоком (ОТДЕЛЬНАЯ карточка ПЕРЕД размерами):
текст (+ про e-mail/бота) + «Заполнить» + кнопки Телеграм/MAX (deeplink из env `NEXT_PUBLIC_*_BOT`; если
пусто — кнопка-заглушка без перехода, без «скоро») + «Сообщить по почте» → `LeadModal` (нов.: e-mail с
проверкой маски regex + согласие ПДн, закрытие backdrop/«×»/Esc, лид → `captureLead`). `MaterialParams`
рендерится ВНУТРИ блока, свёрнут по умолчанию, раскрывается «ввести вручную» или после ввода ссылки.
`RoomPanel` перестроен в СТЕК карточек (шапка → [ссылка+параметры] → [размеры] на каждую плитку). Верхняя
кнопка «Если есть ссылка…» и `openSignal` убраны; липкая шапка итогов (ADR-0023) осталась. **Влияет на:**
`components/calc/{LinkAutofill,LeadModal(нов.),RoomPanel,CalcBuilder}.tsx`, `FindCheaper.tsx` (удалён),
`app/globals.css` (модалка).

## [2026-07-21] Блок ссылки: рекламный вид + автоподгрузка без кнопки; роадмап парсинга — ADR-0025
**Решение:** `LinkAutofill` — рекламный вид (крупный заголовок «Добавьте ссылку на …» + выгоды галочками:
заполним сами / найдём дешевле / подскажем что купить) для конверсии (ссылка → реф + лид). Кнопка
«Заполнить» УБРАНА: при вводе/вставке ссылки (debounce 700 мс, если `http…`) — авто-запрос
`/api/calc/parse-link`, показывается крутилка (`.spinner`), затем поля раскрываются с заполненными
значениями; «ввести вручную» раскрывает поля без запроса. Параметры не выдумываем — только найденное.
**Роадмап точного парсинга (владелец, план отдельно):** прямой fetch страницы (Google-поиск НЕ нужен) →
(1) per-site парсеры под популярные магазины (точно/бесплатно), (2) **ИИ-фолбэк на OpenAI (ChatGPT)** для
незнакомых сайтов — отдать текст страницы, вернуть строго нужные поля, только найденное; анти-бот-маркетплейсы
(Озон/ВБ) — ручной ввод. **Влияет на:** `components/calc/LinkAutofill.tsx`, `app/globals.css`; далее —
`lib/calc/link-parse.ts`/новый ИИ-слой (следующий план).

## [2026-07-21] ИИ-фолбэк парсинга ссылок на OpenAI — ADR-0026
**Решение:** первичное извлечение параметров товара — детерминированный regex-парсер (`link-parse.ts`);
если после него пусты КЛЮЧЕВЫЕ поля вида → **дочитываем через OpenAI** (`lib/calc/link-parse-ai.ts`):
`fetch` на `api.openai.com/v1/chat/completions` (без SDK), JSON-mode, модель `OPENAI_EXTRACT_MODEL`
(дефолт `gpt-4o-mini`), `temperature:0`, промпт «верни только явно указанное, null если нет, НЕ выдумывай/НЕ
округляй», обрезка текста ~12k, таймаут 12с. Ответ валидируется (только нужные ключи, число>0) и мёржится
ТОЛЬКО в пустые поля (детерминированный приоритетнее). Нет `OPENAI_API_KEY`/ошибка → тихо пропускаем
(текущее поведение). Ключ на сервере — тот же, что у ads-watchdog (владелец 2026-07-21). **Почему:** regex
под каждый магазин не написать; ИИ читает таблицу характеристик как человек, но без выдумывания. Анти-бот
(Озон/ВБ, страница не скачивается) ИИ не решает — ручной ввод. **Влияет на:** `lib/calc/link-parse-ai.ts`
(нов.), `app/api/calc/parse-link/route.ts`, `docker-compose.yml` (+`OPENAI_API_KEY`/`OPENAI_EXTRACT_MODEL`),
`.env.example`, `core/access-and-integrations.md`.

## [2026-07-22] Плитка: убрать проёмы из UI, инлайн-результаты, «?» штук без размера — ADR-0027
**Решение:** (1) галочку «Учесть окна и двери» и весь ввод проёмов убрали из UI (`SurfaceEditor`) —
теперь у ВСЕХ видов (обои уже без проёмов, добавились плитка+краска). Проёмы всегда идут в запас на
подрезку; площадь стен = gross. Поле `room.countOpenings` и логика `net/gross` в формуле остались
(тесты живы), но UI флаг больше не выставляет. (2) Результат «Площадь/Нужно» теперь рендерится инлайн
под блоком стен (label «Стены») и под блоком пола (label «Пол»), а не единым блоком внизу панели; у
блока пола — заметная кнопка «удалить» (симметрия со стенами). (3) Без заданного размера плитки в
строке «Нужно» вместо площади комнаты показываем «? шт — задайте размер/ссылку»: новый флаг
`CalcOutput.qtyUnknown` (ставит ветка `tile()` без размеров). Для сметы (`to-estimate`) qty остаётся
площадью-fallback — не менялось. **Почему:** проёмы у плитки путали и давали ложную точность; площадь
комнаты в «Нужно» без размера плитки бессмысленна и не мотивирует ввести параметры. **Влияет на:**
`components/calc/SurfaceEditor.tsx`, `components/calc/RoomPanel.tsx`, `lib/calc/formulas.ts`,
`app/globals.css` (удалён `.openings-group`).

## [2026-07-22] Плитка: единицы в СМ, парсинг размера/цены с единицей — ADR-0028
**Решение:** корень «неправильного расчёта» — размер плитки вводили в СМ, а поле было в мм
(60×120 → 2848 шт вместо ~31). (1) **Поля размера плитки в форме — в СМ** («Длина/Ширина плитки, см»),
хранение остаётся в мм (конверсия ×10 на границе UI, `MaterialParams`); контракт/формулы в мм не
трогаем. Ламинат/обои — как были. (2) **Парсер `link-parse.ts` переписан:** размер берём из ТЕЛА по
метке «Размер [(см)] A×B [ед.]» (разделители `x × х *`, дробные), пропуская фильтр-СПИСКИ (за размером
сразу другой размер); эвристика без единицы — оба ≥100 → мм, иначе см. **Цена с ЕДИНИЦЕЙ:**
`руб/м²|кв.м` → `pricePerM2Rub`, `руб/шт` → `pricePerPieceRub`, иначе `pricePerPackRub` (предпочтение
м²>упак>шт). (3) **Контракт** +`pricePerPieceRub`; **формула `tile()`** — стоимость по выбранной
единице. (4) **AI-фолбэк** (`link-parse-ai.ts`): у плитки добавлены поля цены за м²/шт/упак
(`PLITKA_PRICE_FIELDS`), роут зовёт ИИ за ценой только если НИ ОДНОЙ единицы нет. (5) **Форма:** цена +
селектор единицы; плейсхолдеры «0» светло-серым (`input::placeholder`). **Почему:** плитку в РФ
указывают в см и цену за м²/шт — иначе штуки и стоимость считались в разы неверно. Проверено на проде:
Kerama 74×150мм/2246 за м², Керамогранит 600×1200мм/4790 за м². **Влияет на:** `contracts/calc.ts`,
`lib/calc/link-parse.ts`, `lib/calc/link-parse-ai.ts`, `app/api/calc/parse-link/route.ts`,
`lib/calc/formulas.ts`, `components/calc/{MaterialParams,FloorEditor,RoomPanel,SurfaceEditor}.tsx`,
`app/globals.css`.
