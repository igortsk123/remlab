---
workstream: memory
slug: memory-bank-audit-2026-09
title: Аудит Memory Bank 2026-09 — регулярность, дефекты, реструктуризация (ADR-тома, уроки, планы, intake, Tier 0)
status: draft
created: 2026-09-05
updated: 2026-09-05
completed:
plan_kind: sub
owner_decision_required: "Фаза 0 — фокус (D2), согласие на перенос уроков, манифест архивации планов, дата флипа CI-гейта"
---

## Цель
Память проекта — актуальная и корректная на всех уровнях (Tier 0 → core → Tier 2), читаемая за
≤2 перехода; накопленные дефекты (ADR-лог 474 КБ, уроки 217 КБ, 61 открытый план, `_intake/` 22 МБ,
устаревший Tier 0) устранены обратимо, без потери ни одного факта.

## Источник задачи
Владелец, 2026-09-05: «полный аудит мемори банка — пишем ли регулярно; есть ли накопленные косяки;
план исправления; сверить с концепциями шаблона memory-bank-template». План прошёл критику Codex
(сессия проекта) и проработку механики Plan-агентом; одобрен владельцем в Plan Mode 2026-09-05.

## Скоуп — что входит
Фазы 0–8 ниже (память, правила `.claude/rules/`, `docs/README`, проектный аудит `tools/memory-project-audit.mjs`).

## Скоуп — что НЕ входит
Прод, сервер, код продукта; kit-owned файлы (`_kit/manifest.txt`); удаление чего-либо без копии в `archive/`;
переписывание смысла ADR и уроков.

## Файлы к изменению
По фазам (см. Часть 4). Ключевые: `CLAUDE.md`, `.memory_bank/{INDEX,README,source-of-truth,project-state,decisions,anti-patterns}.md`,
`core/lessons.md`, новые `decisions/`, `lessons/`, `_intake/{codex,owner}/`, `plans/*` (манифест), `.claude/rules/*`,
`tools/memory-project-audit.mjs`, `tests/memory-project-audit.test.mjs`.

## Критерии приёмки
- [ ] Кит-аудит и проектный аудит чисто; CI зелёный (смотреть вывод аудита при gate=warn)
- [ ] Хеши/счётчики миграций ADR и уроков сошлись (до == после)
- [ ] Размеры: `decisions.md` ≤40 КБ, `project-state.md` ≤8 КБ, `core/lessons.md` ≤3 КБ, Tier 0 ≤8 КБ
- [ ] Открытых планов ≤25; каждый partial — с причиной и триггером возврата; BROKEN = 0
- [ ] Drill-тест 5/5 за ≤2 перехода; `verify`-агент без MISMATCH по core/*

## Definition of Done — память (без этого `completed` запрещён)
- [ ] `core/*`, `decisions.md`, `project-state.md` обновлены; новая область `core/mesh-pool.md` в decision tree
- [ ] Уроки уборки — в индекс уроков; ADR о решении D2 записан
- [ ] `/memory-check` выполнен, audit «чисто»; лог фаз в `changelog/memory-log.md`

## Лог выполнения
- 2026-09-05 — аудит выполнен (Части 1–3), план (Часть 4) одобрен владельцем в Plan Mode; статус draft — ждёт
  решений Фазы 0 и команды «деплой». Важно: ADR-нумерация к моменту деплоя уже 0179 → дубли получат 0180+.
- 2026-09-05 — **Решения владельца по Фазе 0 получены:** (1) фокус — вариант **A**: мебельный трек первым
  до понятной точки (например, партнёр принял демо), затем возврат к смете; общая цель «Смета-first» не
  меняется → ADR на Фазе 6; (2) дословный перенос уроков в тематические файлы — **разрешён** (Фаза 4b);
  (3) архивация планов по манифесту — **согласие в принципе**, таблицу всё равно показать (Фаза 5);
  (4) строгий режим CI-гейта — включить **сразу после ручного `/memory-check` в конце этой уборки**
  (владелец сам запускает `/memory-check` в конце сессии; гейт живёт в CI и работу в сессии не
  прерывает). Правило общения с владельцем — просто, без терминов, с «зачем» — добавлено в `CLAUDE.md`.
  Ждём команду «деплой».

## Completion summary
[при переводе в completed]

### Уроки (ОБЯЗАТЕЛЬНО; для partial/cancelled — особенно)
[ситуация → пробовали → почему не сработало → правило]

## Follow-up work
- [ ] Фидбек владельцу кита memory-bank-template (список в Части 3 ниже)

---

# Полный аудит Memory Bank remlab — находки и план исправления

Дата аудита: 2026-09-05. Кит: v1.6.0 (remlab = шаблон, md5 всех 18 kit-owned файлов совпадают).
`node tools/memory-audit.mjs` — «✓ проблем не найдено».
**Вывод в одну строку:** пишем регулярно и много; главные косяки — не «не пишем», а «пишем в
неправильные места и не убираем»: Tier 0 устарел на два месяца, ADR-лог стал журналом на 474 КБ,
уроки — архив на 217 КБ без навигации, 61 открытый план, `_intake/` — слепая зона на 22 МБ.

Источники плана: мой аудит по файлам и git → независимая критика Codex (принята там, где
подтверждена кодом) → Plan-агент по механике аудита (`tools/memory-audit.mjs`).

---

## Часть 1. Регулярность записей (ответ на «пишем ли регулярно»)

**Да.** Факты (git, 01.07–05.09):
| Метрика | Значение |
|---|---|
| Коммитов всего / трогают `.memory_bank/` | 1226 / 721 (0.83 на коммит кода) |
| Код-коммиты, закрытые записью в память ≤3 коммитов | 30 из 40 последних (75 %); остальные — сериями, закрыты сводом чуть позже |
| `session-scratch.md` коммитов | 231 (самый частый файл банка) |
| Недель с кодом без записей в память | 0 |
| Хуки SessionStart / Stop(--block) / PreCompact / PostToolUse(Read) + PreToolUse-гард | все на месте, скрипты существуют; CI `memory-audit` зелёный (gate=warn) |

Оговорки: `changelog/memory-log.md` не обновлялся с 17.08 (журнал `/memory-cleanup`, не
`/memory-check` — норма), `/memory-cleanup` не запускался ≥19 дней; `archive/` не пополнялся с 11.07.

## Часть 2. Накопленные дефекты (по убыванию вреда для «быстрого корректного чтения»)

### D1. Tier 0 (CLAUDE.md) врёт о стадии — первое, что читает агент
- `CLAUDE.md:8-9`: «Директ … автопилот dry-run. ⚠️ Ядро сметы (М1–М3) не построено — код-долг v0.4».
  Факт: М1 в проде с 11.07 (`completed_plans/m1-estimate-core.md`), calc-K0…K6 и launch-p1…p7
  завершены. (Автопилот Директа по-прежнему DRY-RUN; ADR-0033 — про права агента, не про рекламу — уточнение 05.09.)
- CLAUDE.md не упоминает то, чем проект живёт весь август–сентябрь: каталог→меши→сеты на Salad,
  демо-планировщик, честность каталога (ADR-0042…0178, ~135 ADR).
- Тот же устаревший приоритет зашит в `INDEX.md:3`, `source-of-truth.md:20`,
  `guides/execution-playbook.md:54`, `.memory_bank/README.md:3` («B2C AI-помощник…», v0.2).

### D2. Расхождение «декларация ↔ фактическая работа» без ADR
- Высший приоритет по доктрине — ADR-0016 + `MASTER-cost-first.md` (`status: active` — значения
  нет в словаре статусов; `updated: 2026-07-11`, не тронут 56 дней).
- Факт: 49 из 61 открытых планов и почти все ADR с 01.08 — мебельный трек (по мастер-плану это
  «хвост М5», а М2–М4 обойдены). 9 планов `MASTER-*` одновременно. ADR-0042 (01.08) обосновал
  мебельный трек, но порядок ступеней никто не переопределил. **Решение владельца.**

### D3. `decisions.md` — 474 КБ, «короткая навигация» стала полным журналом
- Шапка: «Полные ADR — в `docs/DECISIONS.md`, здесь короткая навигация». Факт наоборот:
  `docs/DECISIONS.md` мёртв с 02.07 (8 ADR, их полные тексты есть ТОЛЬКО там), в `decisions.md`
  187 записей ×2.5 КБ, 170 номеров ADR существуют только в нём. `tier2:` — инвертирован.
- Два формата заголовков (95 `[дата] … — ADR-N` и 91 `ADR-N — дата —`); «Стек проекта» без номера;
  ADR-0032 с хвостом в заголовке.
- **7 дублей номеров** (проверено, не 9): 0028, 0135, 0136, 0158, 0159, 0168, 0173 — разные
  решения под одним номером; ссылок на них вне `decisions.md` — 19.
- Canonical-док: `last_verified: 2026-07-12` при `updated: 2026-09-04`. Найти «действующие
  решения по теме X» без grep невозможно. Кит потолка для ADR-лога не задаёт (единственный
  tier-1 без бюджета) — remlab провалился в эту дыру.

### D4. Уроки: `anti-patterns.md` 217 КБ, 47 секций, навигация по датам переноса
- Секции «Уроки 54–60 (перенесены 04.08)» (38 КБ!), «Уроки 303–307 (28.08…)»; 4 разных формата
  записи; уроки 215–224 и 239–252 отсутствуют (утеряны при сводах 12–13.08); дубли номеров
  10, 29, 37, 57, 169. Нумерация — 413 (растёт прямо сейчас).
- `core/lessons.md` — карта диапазонов номеров, а не правила по темам.
- Правило `agent-workflow` Этап 1.2 «свериться с уроками перед планом» на 217 КБ невыполнимо →
  де-факто не выполняется. Кит: COMPRESS уроков запрещён (CLEANUP_POLICY:49) ↔ бюджет Tier 1 3 КБ —
  конфликт кита, в который remlab провалился.

### D5. Планы: 61 открытый, `partial` — кладбище вне аудита
- 33 `partial` (блок «почему пауза» есть у 1), 26 `draft` (18 старше 30 дней, 8 с июля — в т.ч.
  `sub-e0/e3/e4/e7`, `sub-ml-sizes` отменённого мастера Э0–Э8), 12 `in_progress`, 1 `active`,
  2 `cancelled`. `PLAN-STUCK` смотрит только `in_progress`. 3 плана без `title:`, 2 без `created:`.
- 18 слагов планов цитируются `[[slug]]` из content-доков → архивация без переписи ссылок даст
  ~18 BROKEN.

### D6. `project-state.md` — 9.5 КБ при собственной норме ≤8; операционка вместо снимка
- Половина — детали пула Salad (образы, квоты, «PATCH молчит») — это Tier 1/2 темы пула, не «где
  проект». Битая ссылка «см. раздел Концепции» (`project-state.md:91`). Порог: шапка 8 / скилл 10 /
  аудит 12 КБ — три числа.

### D7. `source-of-truth.md` — правка 20.08 без обновления шапки (`updated: 2026-07-11`);
доменная норма расстановки (ADR-0112) внутри доктрины истины; ссылка на мёртвый `docs/DECISIONS.md`.

### D8. `_intake/` — 163 tracked-файла, 22 МБ логов, lifecycle «сырьё → канон → `_processed`» не работает
- 88 файлов `codex-prompts/` + ~50 `codex-*`/`answer-*`/`prompt-*` в корне без индекса; по имени
  цитируются 55, не сведены 94 — неизвестно. `_processed/` в `.gitignore`, но 4 файла tracked
  (2 уникальных). `salad-ssh-key.txt` tracked — публичный ключ (не утечка, но не место).
  51 `.log` (22 МБ, gitignored) — ручные редиректы из сессий; скрипты `tools/` туда не пишут.
- `decisions.md` ссылается на `_intake/…` 19 раз, планы 9, core 2 → intake стал несанкционированным
  Tier 2, при этом исключён из аудита и обеих уборок (антипаттерн M4 в другой папке).

### D9. Frontmatter-гигиена
- `review_after` пуст у 17 из 21 — механизм REVIEW выключен.
- `updated` ↔ git >14 дней: `source-of-truth` (40 д), `core/architecture` (25), `observability`×2 (22).
- `core/layout.md`, `core/lessons.md` без `last_verified/status`; 7 domain-доков без `last_verified`.
- `core/goals.md` и `product_brief.md` — v0.3 с плашкой «ниже — v0.3» (читатель получает отменённое);
  `goals-one-photo-furnish-fit.md` (20 КБ) в корне банка.

### D10. Читаемость Tier 2 и служебных доков
- `INDEX.md` decision tree: пути `../domain/x.md` относительны к `core/` (генератор кита).
- Гиганты без оглавления: `occupancy-rules` 53 КБ, `catalog-enrichment` 41, `demo-planner-ui` 41,
  `layout-mined-rules` 43, `integrations` 31, `wordstat-semantics` 30.
- `guides/` (процесс-доки по README) хранит доменные `living-room-*`; `project-history.md` 43 КБ
  с секциями «вытеснено из снимка 31.08»; `advertising/campaign_state.md` — состояние от 11.07;
  `docs/` — второй полу-банк (tech-spec, brief v0.3, cjm v0.2, DECISIONS) без пометки «что канон».

### D11. Блокнот `session-scratch.md` — дневник, а не «1–2 строки»
120 строк / 15.6 КБ, записи по 5–15 строк; часть уже сведена (ADR-0178, уроки 405–408), но не
усечена. Stop-хук (kit-owned, `session-reminder.mjs:78`) считает блокнот «заполненным» при любом
старом тексте → накопленный scratch делает проверку бессмысленной.

### D12. Авто-память харнесса — 19 файлов, `mirrored:` у 5; проектные факты в банке есть (утечки
нет), но `dev-env-pakardev.md` (90 строк) — смесь проектного и per-user.

### D13. Порог Tier 0 тихо поднят и разошёлся с CI
Stop-хук: `--tier0-max-kb 10`; CI/аудит — 8 (кит). Tier 0 = 7.997 КБ. Ради 8 КБ из `INDEX.md`
вырезаны «Минимум правил» («один факт — одно место») и «Слои памяти» (мост авто-памяти). Always-on
правила без `paths:` — 22 КБ (`memory-discipline` 116 строк при норме ≤60) против ориентира ~10.

### D14. `.claude/rules/codex-adviser.md` без frontmatter (нет `description`; аудит `RULES-FM`
это не ловит).

### D15. Инфраструктурные доки банка застряли на v1.0 (01.07)
`.memory_bank/README.md`: нет `_secrets/`, `_kit/`, `project-history`; lifecycle-поля «опционально»
(с v1.5 их проверяет аудит). `advertising/` — единственная `<area>/` без README. Project-owned
копии потеряли текст шаблона: `memory-discipline.md` (защита от отравления: `external:`
обязателен, `curl | sh`), `agent-workflow.md` (Explore/`/rewind`/`/btw`), `core/_template.md`
(NO-TIER1), `domain/README.md` (lifecycle), `plans/_template.md` (схема урока).

### D16. `gate-mode.txt = warn` с 12.07 без даты флипа (FLEET.md: «warn становится перманентным»);
`memory-log.md` — записи не по порядку; `metrics.log` — 04.08.

## Часть 3. Что НЕ поехало (сверка с концепциями кита)
- Кит v1.6.0 = шаблон: 18 kit-owned файлов байт-в-байт; проверки аудита 1-в-1; скиллы/verify/CI
  идентичны; `_kit/code-ref-ignore.txt` ведётся с обоснованиями.
- Tier 0 8 КБ (0.4 % корпуса); все 26 core ≤3 КБ; NO-TIER1/ORPHAN/BROKEN/PLAN-MISPLACED/BAD-FM/
  CODE-REF — чисто; FROZEN-MEMORY 0.
- Двухслойная память + мост описаны и работают; хуки и CI стоят; блокнот живой (231 коммит).
- Регулярность записи — норма (Часть 1).

## Фидбек киту (не чинится в remlab; отдельный список владельцу кита)
- У `decisions.md` нет потолка и политики ротации; нет проверки уникальности ADR-ID.
- `PLAN-STUCK` не видит `draft`/`partial`/неизвестные статусы → кладбище планов невидимо.
- `RULES-FM` не ловит отсутствие frontmatter у правила.
- Лимит project-state противоречив: 8 (шапка/HEAL) / 10 (skill, anti-patterns) / 12 (audit).
- Запрет COMPRESS для lessons ↔ бюджет Tier 1 3 КБ — конфликт.
- `SECRET` проверяет только `.md`; `_intake/` — слепая зона, и аудит об этом не сообщает
  («чисто» ≠ полный аудит).
- Stop-хук должен проверять свежесть блокнота для текущей сессии, а не непустоту.

---

## Часть 4. План исправления

**Принято из критики Codex (подтверждено кодом):** дублей ADR 7; `updated`/`last_verified` не
синхронизировать (METADATA_SCHEMA:35); публичный ключ — не секрет; автоотмена планов по возрасту
противоречит CLEANUP_POLICY:36; уроки — только дословный перенос и с согласия владельца;
`_processed/` не `git rm --cached` (единственная копия станет локальной). **Отвергнуто:** месячные
тома ADR (берём блоки номеров), ручной индекс intake (генерируем), «9 MASTER → 1» физически
(берём метаданные `plan_kind`/`parent_plan`). **От Plan-агента:** правила аудита для новых папок
(`decisions/`, `lessons/` = content-доки с уникальным `topic`, NO-TIER1 только для `domain/`),
перепись `[[slug]]` при архивации планов в том же коммите, тематические файлы уроков + карта
«номер → файл», 8 полных ADR из `docs/DECISIONS.md` перенести (не только указатель).

**Ограничения исполнения.**
1. **Заморозка:** банк живой (уроки за час выросли с 408 до 413, 7 файлов банка сейчас изменены
   параллельной сессией) — фазы 3–5 только при чистом `git status` и подтверждении владельца,
   что других сессий нет.
2. Kit-owned файлы (md5 в `_kit/manifest.txt`) не трогаем; проектные проверки — отдельный
   `tools/memory-project-audit.mjs`.
3. Каждая фаза — отдельный коммит + тег `mb-restructure-N` (откат = `git revert`/checkout тега);
   массовые переносы не смешивать с содержательными правками.
4. Бэкап первым действием: `tar` `.memory_bank/ .claude/ CLAUDE.md docs/` → `~/backups/`.
5. Дисклеймер: мой прогон аудита в write-режиме уже добавил строку в `plans/README.md` (реестр,
   `mesh-owner-audit`) — штатная регенерация, не правка смысла.
6. План проекта: `.memory_bank/plans/memory-bank-audit-2026-09.md` (draft → in_progress →
   completed → `completed_plans/`); эта страница — его основа.

### Фаза 0 — решения владельца
1. **Фокус (D2)** — три варианта, рекомендую **A**:
   - **A. Временный порядок:** М5 (мебельный трек) раньше М2–М4 до измеримого условия выхода
     (например: демо-планировщик принят партнёром / первые сеты в проде); ADR-0016 в силе.
     Основание — ADR-0042.
   - **B. Стратегический поворот:** мебельный трек — новый портфельный мастер, часть ADR-0016 superseded.
   - **C. Возврат к «Смета-first»:** мебельные планы на паузу, работа к М2–М4.
   Гейтит Фазу 6 и MASTER-часть Фазы 5; всё остальное можно делать до ответа.
2. **Уроки:** разрешить дословный перенос секций из `anti-patterns.md` в тематические файлы
   `lessons/` (иначе — только индекс поверх файла, Фаза 4a).
3. **Планы:** утвердить манифест архивации одной таблицей (Фаза 5), ответ «ок, кроме …».
4. **Гейт CI:** флип `_kit/gate-mode.txt` warn → block — **решено 05.09: сразу после финального
   ручного `/memory-check` этой уборки** (не раньше: иначе владелец ждёт уборки по 10 раз в процессе работы).
5. Уведомление (не решение): дублям ADR даются новые номера; логи уезжают из банка.

### Фаза 1 — инвентаризация и страховка
- Закоммитить untracked/изменённое параллельной сессии (по её завершении) — чистая точка `mb-restructure-0`.
- Бэкап `~/backups/remlab-membank-2026-09-05.tgz`.
- Baseline (скрипт в scratchpad, результат → `changelog/memory-log.md`): 187 ADR-тел, список ID,
  sha256 нормализованного текста каждого тела; число уроков и хеш каждого; все `ADR-\d{4}` по
  банку/docs/коду; `_intake/` ссылки (19+9+2); планы по статусам; `git ls-files _intake`;
  `memory-audit --check --metrics`.

### Фаза 2 — `_intake/` гигиена (решений не требует)
- `_intake/README.md` (scaffold, без шапки; генерирует `tools/intake-index.mjs`): файл · дата (git)
  · вопрос (первый заголовок) · где сведено (grep basename по банку: 55 / 94 не сведены) · статус.
  Правило в шапке: «логи прогонов — не в банк; basename intake-файлов не переименовывать».
- Логи 51 `.log` (22 МБ) → `~/scout-logs/2026-09/` (вне банка; никто из `tools/` туда не пишет).
- `_processed/` (4 tracked, июль): `git mv` → `archive/2026/07/intake-init/` (архивная шапка),
  строку 28 `.gitignore` убрать, папку удалить.
- `_intake/salad-ssh-key.txt` (публичный, нигде не референсится) → `infra/keys/salad-mesh.pub`;
  указатель на приватную пару — `_secrets/ACCESS.md`.
- Вторым шагом (после индекса): `codex-prompts/*` и корневые `codex-*/answer-*/prompt-*/*-referee-*`
  → `_intake/codex/`; владельческие `owner-*`, `dialog-*`, `blind-*`, `self-analysis-*`, `*.json`,
  `*.txt` → `_intake/owner/` — **basename не менять**; `sed` по `.memory_bank/**/*.md` для
  `_intake/codex-prompts/` (21) и `_intake/<имя>` (58); 4 backtick-ссылки на `_intake/*.txt|json` —
  обновить (CODE-REF). Не трогать `session-scratch.md`, `brief/`, `history/` (прошиты в ките).
- Проверка: `git check-ignore`, grep-счётчики путей до/после, аудит чисто.

### Фаза 3 — ADR-лог: индекс + тома по блокам номеров
- Раскладка: `decisions.md` (canonical индекс ≤40 КБ, `tier2: decisions/README.md`) ·
  `decisions/README.md` (scaffold: «номер → том», правило нового ADR) · `decisions/adr-0001-0050.md`,
  `adr-0051-0100.md`, `adr-0101-0150.md`, `adr-0151-0200.md` (текущий). Шапка тома: `tier: 2`,
  `topic: decisions-0101-0150`, `scope: Полные тексты ADR-0101…0150`, `tier1: ../decisions.md`,
  `importance: high`, `source: manual`, `status: stable`, `source_of_truth: historical` у закрытых
  томов (гасит шум CODE-DRIFT), `supporting` у текущего.
- Скрипт `split-decisions.mjs` (scratchpad, dry-run → apply): id — формат B `^## ADR-(\d{4})`,
  формат A — **последний** `ADR-\d{4}` в строке; заголовок → `## ADR-NNNN — YYYY-MM-DD — Название`;
  «Стек проекта» → `ADR-0000`; тела — байт в байт.
- 7 дублей: ранняя запись сохраняет номер, поздней — следующий свободный (на день деплоя, после
  проверки max по банку и блокноту) + строка `Legacy: до 2026-09 шёл под номером ADR-NNNN`.
  19 ссылок вне `decisions.md` — по таблице контекста (уже разобрано Plan-агентом: `core/leads.md:15`
  → бывший 0028-второй, `core/data-model.md:39` → 0136-второй, `domain/demo-planner-ui.md:65/91`,
  `anti-patterns.md:825/830` → 0158/0159-вторые, `core/mesh-color.md:34` → 0168-второй; остальные —
  базовый номер) + `plans/`, `completed_plans/` (~20 мест).
- `docs/DECISIONS.md`: 8 полных ADR → раздел в `adr-0001-0050.md» («Полные тексты из docs/DECISIONS,
  перенос 05.09»); копия → `archive/2026/09/docs-DECISIONS.md`; файл → 10-строчный указатель «legacy».
- Индекс: вводная «как читать» → **«По темам» (10–12 тем → действующие номера, many-to-many)** →
  список по блокам `ADR-N · дата · тема · заголовок · отменяет/отменён ADR-x` (маркеры из regex
  `отменяет|заменяет|откат ADR-\d{4}`, ~9, проверить руками).
- Указатели `docs/DECISIONS.md` → `decisions.md`: `source-of-truth.md`, `project-state.md:91`,
  `execution-playbook.md:54–55`, `CLAUDE.md`, `.memory_bank/README.md:25`, `INDEX.md`,
  `memory-discipline.md:52,63`, `agent-workflow.md:31` («строка → индекс, текст → текущий том»).
- Проверка: 187 тел = сумма по томам; ID уникальны; sha256 конкатенации тел до == после; суммы
  `grep -c` по каждому дублю до == после; `grep -c '^## ' decisions.md` = 0; аудит чисто.

### Фаза 4 — уроки
- **4a (без согласия):** `core/lessons.md` (≤3 КБ) = 8 тем × 2–4 живых правила в строку + путь;
  «критичные сейчас» 5–8; ссылка на карту номеров. В `anti-patterns.md` сверху — тематический индекс
  (тема → номера). Текст секций не трогаем.
- **4b (по согласию, дословно):** `lessons/README.md` (scaffold: `№ → файл`, пропуски 215–224/
  239–252, дубли 10/29/37/57/169 — признать явно, не перенумеровывать; «следующий номер»);
  `lessons/{mesh-salad-pool, catalog-stock, layout-solver, viz-render, demo-planner,
  devops-deploy, memory-process, estimate-ads-leads}.md` (`tier: 2`, `topic: lessons-<x>`,
  `tier1: ../core/lessons.md`). Скрипт `split-lessons.mjs`: разбор 4 форматов записи, тема =
  баллы (заголовок секции ×3, тема ADR-ссылки ×2, словарь ×1), отрыв <2 → ручной проход
  (ожидаю 20–25 %); при смене исходной секции — строка `### из «<секция>»` (провенанс). В
  `anti-patterns.md` остаются §1–7, M1–M5 + указатели (≈13 КБ).
- Правила: `agent-workflow` Этап 1.2 → «`core/lessons.md` + `lessons/<тема плана>.md`»; Этап 2.4 →
  «урок → `lessons/<тема>.md`, номер из README»; `plans/_template.md` — вернуть схему «ситуация →
  пробовали → почему не сработало → правило».
- Проверка: число уроков и sha256 тел до == после; множество номеров без изменений; `core/lessons.md`
  ≤3072 Б; `anti-patterns.md` ≤40 КБ; аудит чисто.

### Фаза 5 — планы: триаж по таблице, манифест → владелец → архив
- Скрипт `plans-triage.mjs`: статус · дни от `updated` · трек · упомянут ли мастером · `[[slug]]`
  из content-доков. Таблица решений:
  | статус | условие | действие | кто |
  |---|---|---|---|
  | in_progress ≤14 д | — | KEEP | авто |
  | in_progress >14 д | по git: сделано → completed; иначе partial + блок «Пауза: почему/когда/чей вызов» | авто, уведомить |
  | partial с блоком «Пауза» | — | KEEP | авто |
  | partial без блока, ≤30 д, в живом мастере | добавить блок (черновик из git/ADR) | владелец подтверждает |
  | partial без блока, мастер архивируется/не упомянут | ARCHIVE `archive_reason: absorbed by …` | **владелец** |
  | draft ≤30 д | — | KEEP | авто |
  | draft >30 д, июльские «остаются (отложены)» по мастеру | ARCHIVE `deferred until <М-этап>` + правка таблицы мастера | **владелец (D2)** |
  | draft >30 д, мебельный, перекрыт позже | ARCHIVE `superseded_by: …` | **владелец** |
  | cancelled (2) | — | ARCHIVE | авто |
  | MASTER-* (9) | 1 портфельный (`plan_kind: portfolio_master`) + под-мастера (`track_master`, `parent_plan`); остальные ARCHIVE | **владелец (D2)** |
- Новые плоские поля (аудит игнорирует неизвестные): `plan_kind`, `parent_plan`, `pause_reason`,
  `resume_trigger`, `review_after`, `owner_decision_required`. `cancelled` — только у реально
  отменённых; отложенные архивируются со статусом как есть + `archive_reason`.
- Применение одним коммитом: `git mv` → `archive/plans/<slug>.md` + перепись `[[slug]]` →
  `` `archive/plans/<slug>.md` `` в 10 content-доках (18 слагов); `title:` у 3 планов, `created:`
  у 2; `plans/README.md` ручная секция «Сейчас в работе» (6–8 строк) + словарь статусов
  (`superseded`, `plan_kind`, правило триажа). `MASTER-cost-first.md`: `status: in_progress`.
- Цель: ≤25 открытых; каждый `partial` — с причиной и триггером возврата; BROKEN = 0.

### Фаза 6 — Tier 0 и «где проект» (атомарно, после D2)
- `CLAUDE.md` стр. 3–9 (стадия, фокус, ссылка на ADR фокуса) — ужать, чтобы Tier 0 ≤8 КБ.
- `INDEX.md` стр. 3–4 + вернуть двумя строками «один факт — одно место» и «два слоя памяти + мост».
- `source-of-truth.md`: § продукта под решение D2; § ADR-0112 → строка-ссылка; шапка
  `updated: 2026-08-20` (дата фактической правки), `review_after`.
- `project-state.md` ≤8 КБ: Фокус сейчас · В проде · Ждёт владельца · Открытые риски · Политики
  (ссылкой); операционка Salad → новая пара `core/mesh-pool.md` (Tier 1 ≤3 КБ) ↔
  `domain/mesh-pool-ops.md` (взаимные `tier2:`/`tier1:`, иначе NO-TIER1).
- ADR о решении D2 (в текущий том + строка индекса); `MASTER-cost-first.md` — статусы М0–М7 по факту,
  таблица под-мастеров, «Судьба прежних планов» по манифесту Фазы 5.
- `.memory_bank/README.md` под v1.6 (`_secrets/`, `_kit/`, `project-history`, lifecycle «проверяет
  аудит», контракт каталогов: `guides/` = процесс/методика, `domain/` = модель предмета,
  `<area>/` = состояние области); `advertising/README.md`; `docs/README.md` («канон — банк; здесь
  история и Tier 2 tech-spec»).
- Tier 0 порог: цель ≤8 КБ (кит и CI); если не влезает — оставить `--tier0-max-kb 10` в хуке и
  зафиксировать ADR (HEAL.md это допускает для кириллицы).

### Фаза 7 — Tier 2, frontmatter, правила, блокнот, авто-память
- Оглавление сверху в доках >15 КБ (6 domain + 2 guides из D10).
- По контракту каталогов: `guides/living-room-inventory-by-area.md`, `living-room-zoning-verified.md`
  → `domain/` (+ пары Tier 1 уже есть: `lr-checklist`, `layout`); `layout-*` остаются в `guides/`.
- `changelog/project-history.md`: заголовки «дата — тема»; `memory-log.md`: записи сверху, лог
  фаз этой уборки.
- `review_after` (+90 д) у canonical и tier-1; `updated` — только где текст менялся после
  (по git); `last_verified` — только по результату `verify` (Фаза 8).
- `core/goals.md`, `product_brief.md` → v0.4 + мебельный трек (v0.3 — строка «история:
  `docs/master-brief-v0.3.md`»); `goals-one-photo-furnish-fit.md` → `archive/2026/09/`;
  `core/goals.md` `tier2:` → `../plans/MASTER-cost-first.md`. `campaign_state.md` — сверить с
  Директом или `status: stale`.
- Правила: `codex-adviser.md` — frontmatter (`description:`); `memory-discipline.md` ≤60 строк
  (детали → `guides/memory-automation.md`) + вернуть строки защиты от отравления;
  `core/_template.md` — NO-TIER1; `domain/README.md` — lifecycle-поля.
- Блокнот: усечь до метки; правило в шапке «≤2 строки; цифры/хроника → `project-history`/ADR»;
  после каждого свода оставлять одну честную строку `- дата — свод выполнен, блокнот чист`
  (Stop-хук kit-owned проверяет непустоту).
- Авто-память: `dev-env-pakardev.md` разделить — проектное уже в банке → `mirrored:`-указатели,
  per-user — как есть.

### Фаза 8 — проектный аудит и верификация
- `tools/memory-project-audit.mjs` + `tests/memory-project-audit.test.mjs` (project-owned):
  ADR-ID уникальны и все в индексе; `draft` >30 д без `review_after` / `partial` без `pause_reason`
  / статус вне словаря; блокнот — записи старше 7 дней; canonical-доки → `_intake/`; секрет-паттерны
  в `.txt/.json` вне `_secrets/`; `_kit/manifest.txt` md5 сходятся. Подключить в Stop-хук
  (`settings.local.json`, после кит-аудита) и шагом в `ci.yml` (kit-owned `memory-audit.yml` не трогаем).
- `verify`-агент (`.claude/agents/verify.md`, режим Б): 4 батча по ~7 доков — все `core/*`,
  `project-state`, `source-of-truth`, `deployment`; MISMATCH → правка; прошедшим — `last_verified`.
- Drill-тест (свежий субагент; только INDEX → core → один Tier 2; код запрещён), 5 вопросов:
  пул нод Salad и кто его гасит · что значит «в наличии» · что из ядра сметы в проде и что такое
  `/go/` · где полный текст перенумерованного ADR и чем отличается от соседа · какие уроки про
  деплой на exit-fi помнить. Критерий 5/5 за ≤2 перехода.
- `/memory-check` → кит-аудит и проектный чисто → `mem:` коммит → push → CI зелёный (смотреть
  вывод аудита, не только «зелёный» при gate=warn) → план → `completed_plans/`.

### Критерии готовности
| Что | Сейчас | Цель |
|---|---|---|
| `decisions.md` | 474 КБ, 7 дублей, 2 формата | индекс ≤40 КБ с картой тем; ID уникальны; тома дословно |
| Уроки | 217 КБ, навигация по датам | `core/lessons.md` — живые правила по 8 темам; карта номер→файл |
| `project-state.md` | 9.5 КБ, операционка | ≤8 КБ, 5 разделов |
| Открытых планов | 61 (32 partial без причин) | ≤25; каждый partial с причиной/триггером |
| `_intake/` | 163 tracked, ключ, 22 МБ логов | индекс; `codex/` и `owner/`; логи вне банка |
| Tier 0 | стадия устарела на 2 месяца | правда о фокусе, ≤8 КБ, инварианты на месте |
| Аудит | кит «чисто» при всём выше | кит + проектный чисто; CI; drill 5/5; хеши миграций сошлись |

### Что НЕ делаем
Не правим kit-owned файлы; не удаляем без копии в `archive/`; не сжимаем и не переписываем уроки;
не меняем смысл ADR; не архивируем планы без утверждённого манифеста; не переименовываем
intake-файлы; не трогаем прод и сервер (это только память).
