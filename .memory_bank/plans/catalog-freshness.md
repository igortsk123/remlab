---
workstream: furniture
slug: catalog-freshness
title: Свежесть каталога — наличие, битые ссылки, автозамены в сетах
status: partial
created: 2026-08-02
updated: 2026-08-17
completed:
---

## Цель
Сеты не «умирают»: товары в сетах всегда в наличии и со рабочими ссылками; пропавшие
заменяются автоматически из запасных (alternates v2), витрина и /go/p/ не ведут в 404.

## Источник задачи
Владелец 2026-08-02: в Сете 1 «наличия нет / несуществующие ссылки». Проверка: из 14 позиций
1 удалена (404 у магазина при СЕГОДНЯШНЕМ фиде), 3 «нет в наличии», плюс наша бага декода
URL из goto= (%21 и др. не разворачивались). Фид = снимок, тухнет за часы.

## Факты
- Фиды YML: у товара есть `available` (читаем в in_stock), у фида — дата генерации; ссылки
  выгрузок ПОСТОЯННЫ, но НЕ сохранены у нас (дыра — только API-токен в ACCESS.md).
- XML API search — живой, точечная перепроверка возможна.
- tvoydom: «нет в наличии» в шаблоне страницы — ЛОЖНЫЙ маркер; наличие по полям остатков
  в инлайн-JSON. nonton/gipfel: текстовый маркер честный.

## Ручные шаги владельца (блокер Ф1)
- [ ] Прислать заново постоянные ссылки выгрузок (7 подключённых магазинов; идеально — все 14
  из кабинета /export_files/) → сохраню в `_secrets/ACCESS.md` (вне git), больше не потеряем.

## Скоуп
**Ф1 Немедленное (после ссылок):**
- Фикс декода: полный `urllib.parse.unquote` для url из `goto=` (view lr_roles / выгрузки).
- Перекачать фиды → перезалить products (available → in_stock, date фида → лог) →
  пересобрать sets2 c фильтром in_stock=true (утверждённые составы — через freeze-механику).
**Ф2 Health-цикл сетов (разведка, cron на DEV-VM):**
- Ежесуточно по товарам активных сетов (~300): (а) in_stock из свежего фида;
  (б) HTTP-статус карточки; (в) домен-специфичное наличие (nonton/gipfel — маркер,
  tvoydom — поля остатков; 2–3 с интервал, HTML-only).
- Пропал → автозамена из alternates (та же роль, ±15% цены) + лог замен; alternates пуст →
  пересбор слота compose2. Отчёт владельцу при заменах в утверждённых сетах.
**Ф3 Прод (порт в gdeslon-catalog Ф1/living-room-sets Ф5 — уже заложено):**
- feed-resync ежесуточный systemd; `/go/p/` отдаёт 302 только на in_stock (иначе — страница
  «товар закончился» с заменой); sets-health в ресинке.
**Ф4 Опция:** точечная перепроверка спорных через XML API search.

## Скоуп — что НЕ входит
Постбэки; прод-код (Ф3 — в существующих планах); скрейп всех 87 тыс. (только товары сетов).

## Критерии приёмки
- [ ] 100% ссылок активных сетов отвечают 200 и «в наличии» после цикла Ф2
- [ ] Убитая позиция заменяется автоматически ≤ суток, с логом
- [ ] Ссылки выгрузок сохранены в ACCESS.md; декод URL — без остаточных %XX

## Definition of Done — память
- [ ] `core/furniture.md` + `domain/integrations.md` (фиды: date/available/ссылки) обновлены;
  уроки → `core/lessons.md`; `/memory-check`; audit чисто

## Лог выполнения
- 2026-08-02 (вечер) — КРИТИЧЕСКИЙ фикс: upsert load3 молча откатывался целиком (алиас p vs
  products в ON CONFLICT; psql без ON_ERROR_STOP прятал ошибку) — ежедневная свежесть фактически
  не писалась. ON_ERROR_STOP=1 во всех psql scout. Попутно: description из фидов в products
  (27k), стиль-скоринг новинок добавлен шагом в refresh_daily.sh (дельта по кэшу).
- 2026-08-02 — план создан (draft) по живой проверке Сета 1 (1 труп, 3 OOS, декод-бага, ложный маркер tvoydom)
- 2026-08-02 — «деплой». Владелец прислал ссылки: все 7 выгрузок опознаны и СОХРАНЕНЫ в ACCESS.md
  (nonton=777e580d, h-f-l=4255a360). Ф1 ГОТОВО: load3.py — upsert 87 635 тов., direct_url
  (полный unquote; mnogomebeli/divanboss SPA → обрезка до живой серии), last_seen;
  снятие исчезнувших из фида (сегодня 0 — состав совпал). ФАКТ: available в выгрузках НЕ
  проставляется (все true), OOS-товары в фиде числятся живыми → наличие ТОЛЬКО по карточкам.
  Ф2: health.py (домен-правила, автозамена в sets2 из alternates, отчёт; v1 не трогаем) —
  запущен; refresh_daily.sh + cron 09:40 и @reboot (guard раз в сутки) — стоит.
- 2026-08-02 — Ф2 ПЕРВЫЙ ПРОГОН: 311 позиций, 17 мёртвых (5.5%); v2 — 13 автозамен из
  alternates + 3 добора прямым подбором (сет2 кресло — живой замены в полосе НЕТ, слот
  открыт); v1 (утверждённые) НЕ менялись — 11 проблемных позиций в отчёте владельцу
  (health-report.json). sync_metrics.py в цикле (цены/размеры/fp/тиры); первый прогон дал
  178 «тир-выбросов» — полосы ±20% слишком строги для декора, калибровка — TODO.

## Completion summary

### Уроки (ОБЯЗАТЕЛЬНО)

## Follow-up work
- [ ] Постбэки Гдеслона (подтверждение покупок) — отдельный план

> 2026-08-17 (/memory-check): статус in_progress→partial — работа поглощена сводами MASTER-zones-v2…v7 (ADR-0098…0107); остаток — по мастеру.
