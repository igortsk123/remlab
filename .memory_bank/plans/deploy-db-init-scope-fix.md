---
workstream: infra/deploy
slug: deploy-db-init-scope-fix
title: Автодеплой чинится — каталожные миграции убраны из прод-инициализации, smoke снова работает
status: draft
created: 2026-09-05
updated: 2026-09-05
completed:
---

## Цель
Вернуть «Deploy prod» в зелёное и восстановить реально работающий smoke-тест с авто-откатом
(с 01.09 он не выполнялся ни разу).

## Источник задачи
Владелец: «были ошибки на гитхаб заливки, возможно там давно код неактуальный — проверь».
Проверка показала: заливка кода исправна (`main` == `origin/main`, CI gate зелёный, прод на
последнем коммите). Падает workflow **Deploy prod** — с 2026-09-01T13:56Z ни одного зелёного
прогона (проверено по 100 последним запускам).

## Диагноз (доказан логом job 101256447530)
```
psql:/docker-entrypoint-initdb.d/008-mesh-binding.sql:14: ERROR: relation "products" does not exist
```
- `db/init/*.sql` = схема **боевой** БД. Деплой заливает их `scp db/init/*.sql` и применяет
  циклом `psql -v ON_ERROR_STOP=1` (`.github/workflows/deploy.yml:85,97`).
- `008-mesh-binding.sql` и `009-photo-origin.sql` (добавлены 01.09) — **каталожные** миграции:
  `alter table products`, `alter table asset_revisions`, view `product_mesh_state` поверх
  `mesh_demand`. Ни одной из этих таблиц на проде нет и быть не должно — каталог живёт только на
  дев-БД `remlab-devdb` (контракт прямо записан в `tools/scout/db_migrate.py:4-6`).
- Проверено на проде: `\dt` → 12 таблиц, `products` отсутствует. Приложение эту таблицу нигде не
  читает (grep по `app/ lib/ db/` пуст) → боевая база не пострадала, чинить надо расположение файлов.
- Почему это не поймали раньше: ручной `deploy.sh` перечисляет SQL-файлы явно (001–007) и не
  падает; глоб есть только в GitHub-workflow.

## Последствие, которое опаснее красного значка
Падение происходит **после** `docker compose up -d` — образ выкатывается, прод не отстаёт. Но шаг
«Smoke-тест + откат при провале» после провала предыдущего шага **пропускается GitHub-ом целиком**:
с 01.09 никто не проверяет, что сайт после выкатки отвечает, и **авто-откат на `:prev` отключён молча**.

## Скоуп — что входит
- Перенос двух каталожных миграций в `tools/scout/` (там их применяет `db_migrate.py`).
- CI-гарда: применение `db/init/*.sql` к чистому postgres — ловит ЛЮБУЮ поломку прод-инициализации,
  не только эту (в т.ч. повторное применение — идемпотентность).
- Smoke-тест/откат в деплое выполняется даже если предыдущий шаг упал.
- ADR + обновление сводок памяти.

## Скоуп — что НЕ входит
- Правки боевой БД (не нужны — таблиц каталога там нет и не должно быть).
- Переезд control-plane таблиц (`mesh_demand`, `asset_revisions`) из runtime-DDL `mesh_queue.py`
  в миграции — отдельный долг, вынесен в follow-up.
- Ручной `deploy.sh` (не падает; трогаем только при необходимости синхронизации).

## Файлы к изменению
- [ ] `db/init/008-mesh-binding.sql` → `tools/scout/006-mesh-binding.sql` — каталожная миграция, не прод
- [ ] `db/init/009-photo-origin.sql` → `tools/scout/007-photo-origin.sql` — каталожная миграция, не прод
- [ ] `.github/workflows/ci.yml` — новая job `db-init`: прогон `db/init/*.sql` дважды на чистой БД
- [ ] `.github/workflows/deploy.yml` — smoke/откат с `if: !cancelled()`, чтобы страховка не отключалась
- [ ] `.memory_bank/decisions.md` — ADR: граница «прод-схема vs каталожная схема» + гарда
- [ ] `.memory_bank/deployment.md`, `.memory_bank/core/*` — актуализация путей и правила
- [ ] `.memory_bank/core/lessons.md`, `.memory_bank/anti-patterns.md` — урок про молча отключённый smoke

## Задачи
- [ ] Консультация Codex по плану (правило `codex-adviser`: миграции + новый план)
- [ ] `git mv` двух файлов, проверка применения `db_migrate.py` на дев-БД (дважды, идемпотентность)
- [ ] CI-job `db-init` + локальная проверка на одноразовом контейнере postgres
- [ ] Правка deploy.yml (smoke не пропускается)
- [ ] `pnpm typecheck && lint && test && build` → коммит → push → дождаться зелёного Deploy prod
- [ ] Память: ADR, сводки, уроки; `/memory-check`

## Критерии приёмки
- [ ] «Deploy prod» завершается **success**, в логе есть «OK: health 200»
- [ ] `db/init/` содержит только прод-схему (001–007), каталожные миграции — в `tools/scout/`
- [ ] CI-job `db-init` красный, если в `db/init/` положить файл с обращением к каталожной таблице
- [ ] `python tools/scout/db_migrate.py` на дев-БД проходит дважды подряд без ошибок
- [ ] Прод отвечает 200, версия = HEAD `main`
- [ ] Lint / build / тесты / типы — зелёные, отладочного вывода нет, файлы вне scope не задеты

## Definition of Done — память (без этого `completed` запрещён)
- [ ] Memory Bank обновлён: `decisions.md` (ADR), `deployment.md`, затронутые `core/*`
- [ ] Новой функциональной области нет → отдельная `core/<домен>.md` не нужна
- [ ] «Уроки» заполнены; перенесены в `core/lessons.md` / `anti-patterns.md`
- [ ] `/memory-check` выполнен, audit «чисто»

## Лог выполнения
- 2026-09-05 — план создан (draft), диагноз доказан логом прогона и `\dt` на проде

## Completion summary
[Заполняется при переводе в completed]

### Уроки (ОБЯЗАТЕЛЬНО)
[Заполняется при завершении]

## Follow-up work
- [ ] `mesh_demand` / `asset_revisions` создаются runtime-строкой в `mesh_queue.py`, а не миграцией →
      на ЧИСТОЙ дев-БД `db_migrate.py` упадёт на `006-mesh-binding.sql`. Долг существовал и до этой
      правки; чинить отдельным планом (перенос control-plane DDL в `NNN-*.sql`).
