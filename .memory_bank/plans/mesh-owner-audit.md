---
workstream: mesh-pipeline
slug: mesh-owner-audit
title: Ручная приёмка мешей владельцем — страница по 20, кнопка «переделать», честная очередь
status: in_progress
created: 2026-09-05
updated: 2026-09-05
completed:
---

## Цель
Ссылка, где владелец глазами проверяет меши: по 20 карточек на страницу (одна карточка на
товар), у каждой вертящаяся 3D-модель и одна кнопка «переделать». Нажатие фиксируется в БД как
ручная проверка, меш выводится из сетов и встаёт в общую очередь на перегенерацию — так, чтобы
решение НЕ ТЕРЯЛОСЬ и не отменялось следующим прогоном конвейера.

## Источник задачи
Промпт владельца 05.09 + уточнения в той же сессии: 3D-модель (не картинки); переделка в общую
очередь (без обгона); максимум 2 ручные переделки на товар с обязательной пометкой; модели возить
партиями с кнопкой «следующая партия»; удалять только КОПИИ на проде, исходники — никогда.
Ответы на вопросы: одна карточка на ТОВАР (1291, 65 страниц); авто-перегон системы в лимит двух
ручных НЕ входит; прогресс «смотрел» считать автоматически. Затем: «сделай аудит плана ещё раз,
поправь» → второй аудит (свой + Codex №2), план одобрен через Plan Mode 05.09.

## Проверенные факты (05.09)
- Диск DEV: **1490** `model.glb` на **1291 товар** (162 товара с 2–3 попытками), 31 ГБ.
- Меш 7,6 МБ; DEV→прод **2,2 МБ/с** → партия 200 товаров ≈ 1,5 ГБ ≈ 12 минут.
- Прод: свободно 17 ГБ из 38; там же приёмник конвейера (порог `sink_health.py:55`: красный ниже
  **7 ГБ**), сайт, БД, VPN-нода. `/test/*` отдаётся с `no-store` → нужен свой маршрут с кэшем.
- Пул Salad с 07:41 UTC снова работает (баланс пополнен) — конвейер `batch_show --batch 200`
  бежит, его разбор по таймеру каждые 15 мин зовёт `ingest_registry.py`/`mesh_bind.py`
  подпроцессами: правки П0 подхватятся сразу, без перезапуска.
- В `mesh-reseed.json` 836 ожидающих перегонов (770 — seed 1, 66 — seed 0).

### Что нашли аудиты (Codex №1 → свой → Codex №2; всё подтверждено по коду)
1. **Ревизии схлопываются**: `ingest_registry.py` пишет все seed одного товара в одну строку
   `sku|sha|v1`, обходит папки по алфавиту (не по времени) и при каждом прогоне возвращает
   статус в `generated`. Колонки `generation_variant`/`rejected_reason` заведены
   (`006-mesh-binding.sql`), но не заполняются.
2. **Привязка** `mesh_bind.py:95` берёт свежайший файл по mtime, отказ владельца не смотрит;
   отвязки нет вовсе.
3. **Готовность** `mesh_ready.py:45` связывает ориентацию по SKU → перегон наследует ориентацию
   старого меша. В `mesh_dims.py:58` уже есть честная сверка по `glb_sha`.
4. **Дозапись в живой `mesh-queue-v1.json`** запрещена регламентом `rules/mesh-priority.json`
   (§identity: позиционный курсор запрещён, завершённость по `job_key` с seed), затирается
   `mesh_priority.py --build-queue`; `batch_show.py:636` считает `total` один раз.
5. **Seed**: `apply_repairs.py` сам заказывает seed+1 для seed 0 → ручной и авто-перегон могут
   заказать один seed и получить cached.
6. **Legacy-читатели** `gallery_build.py:195` и `topview_render.py:255` смотрят sidecar
   `owner_reject.json` в папке меша — отказ из БД они не увидят.
7. **Партия «удалить → лить 12 минут»** оставляет владельца без моделей; порог диска ≥6 ГБ
   ниже порога приёмника (7).

## Решения (после двух аудитов)
1. **Идентичность физического меша — аддитивно.** `revision_key` не меняем. Таблица
   `mesh_generations` (`generation_key = sku|sha16|v1|seed|glb8`, путь, `generated_at`,
   `machine_verdict`, `owner_verdict`, `owner_decision_id`) + указатели
   `asset_revisions.current_generation_key`, `products.mesh_generation_key`. «Текущее» — по
   монотонному `generated_at` с tie-break по ключу; порядок обхода диска на итог не влияет.
2. **Отказ не наследуется перегоном.** `owner_reject` у ревизии держится ТОЛЬКО пока
   `current_generation_key` = отвергнутое поколение; новое поколение → `generated`. Ingest
   никогда не затирает человеческий вердикт поколения.
3. **Отказ = товар без меша до перегона.** Старые попытки НЕ воскрешаются. Одна DEV-транзакция:
   ledger по `prod_decision_id` → `owner_verdict` поколению → ревизия в `owner_reject` при CAS
   → очистка `products.mesh_uri/mesh_at/mesh_generation_key`, `mesh_status='rejected'` → запрос
   переделки; sidecar `owner_reject.json` в папке поколения (для legacy-читателей). Тогда
   `mesh_priority.products()` (`status != 'ready'`) сам вернёт товар в очередь на его ярусе.
4. **Готовность по `glb_sha`** — в `mesh_ready.py` условие из `mesh_dims.py`.
5. **Единый allocator seed**: первый свободный среди `mesh_generations`, pending-переделок,
   `mesh-reseed.json` и активного снимка очереди.
6. **Лимит буквально: 2 ручные переделки на SKU за всё время**, авто-перегоны не считаются;
   обнуление при смене фото — без отдельного решения владельца не вводим. Параллельные клики:
   `for update` + уникальность `(sku, manual_attempt_no)`.
7. **Очередь честно**: инбокс `mesh_rework_requests`; в снимок — при `--build-queue`
   (`seeds:[next_seed]`, `queued` только после атомарной записи снимка). До этого карточка:
   «принято, ждёт сборки очереди». Процедура «старт волны» документируется и скриптуется;
   в `batch_show` курсор выводится из имени `MESH_SAMPLE`.
8. **Прогресс — `seen_at` у карточки**, таблицы просмотров страниц нет.
9. **Активы под `/opt/remlab/test/mesh-audit/`**, `handle_path /test/mesh-audit/*` с кэшем.
10. **Смена партии с grace**: новая активна после проверки манифеста; предыдущая удаляется
    через 10 минут; вкладка опрашивает состояние и просит обновиться.
11. **Два замка**: минутный sync и 12-минутный публикатор — разные процессы и lock'и.
12. **Тесты двух уровней**: `--selftest` без БД (CI), `--dbtest` на одноразовой БД в
    `remlab-devdb` (DEV, перед приземлением), vitest на проде.
Отвергнуто из Codex №2: постеры «только для партии» (делаю для всех, в порядке партий);
рефактор `mesh_jobs.job_key` под seed — follow-up.

## Скоуп — что НЕ входит
Сжатие/децимация мешей; ориентация (`/lab/mesh-review`); приоритетная волна; рефактор
`mesh_jobs`; автозамена товара при «нужна замена» (только список).

## Файлы к изменению
П0 — учёт: `tools/scout/007-mesh-owner-audit.sql`, `tools/scout/salad/ingest_registry.py`,
`tools/scout/mesh_bind.py`, `tools/scout/mesh_ready.py`, `tools/scout/mesh_queue.py`
(имя БД из env для dbtest), `tools/scout/tests/mesh_owner_audit_dbtest.py`.
П1 — переделки: `tools/scout/mesh_priority.py`, `tools/scout/salad/batch_show.py` (курсор),
`tools/scout/mesh_wave_start.sh`.
П2 — прод: `db/schema.ts`, `db/init/010-mesh-audit.sql`, `tools/migrate.mjs`,
`app/api/lab/mesh-audit/{items,decisions,batch}/route.ts`, `app/lab/mesh-audit/page.tsx`,
`components/lab/MeshAuditCard.tsx`, `components/lab/MeshAuditBatchBar.tsx`,
`lib/mesh-audit/client.ts`, `public/vendor/model-viewer.min.js`, `caddy/Caddyfile`,
`tests/unit/mesh-audit.test.ts`.
П3 — `tools/scout/mesh_audit_publish.py`. П4 — `tools/scout/mesh_audit_posters.py`.
П5 — `tools/scout/mesh_audit_sync.py`, крон, шаг в `batch_show.post_steps()`.
П6 — CI: новые модули в цикл `scout-selftest`.

## Задачи
- [ ] П0 миграция + ingest (детерминированный, батчем, без затирания вердикта) + bind (только
      текущее поколение, явная отвязка) + ready по `glb_sha` + `--dbtest`; отчёт по 1490 файлам.
- [ ] П1 allocator seed + `--build-queue` с переделками + атомарный снимок + курсор по имени
      снимка + `mesh_wave_start.sh` + раздел в `core/mesh-pipeline.md`.
- [ ] П2 схема/ручки/страница/карточка/партия-панель/вендор model-viewer/Caddy.
- [ ] П3 партии. П4 постеры. П5 тик и публикатор. П6 тесты и CI.
- [ ] typecheck/lint/test/build → коммит → main → `./deploy.sh` → `pnpm db:migrate` на проде;
      живая проверка на 2–3 мешах.

## Критерии приёмки
- [ ] Страница по закладке владельца: 1291 карточка, 65 страниц; GLB не качается до клика; после
      клика модель крутится; вне партии — постер и «загрузить эту партию» с прогрессом.
- [ ] Клик: строка в `mesh_audit_decisions`, бейдж 1/2; через ≤1 мин на DEV
      `owner_verdict='redo'`, ревизия `owner_reject`, `products.mesh_status='rejected'`,
      `mesh_uri` пуст, `owner_reject.json` в папке, строка в `mesh_rework_requests`.
- [ ] Ручной прогон `ingest_registry.py` → `mesh_bind.py` статусы НЕ откатывает.
- [ ] `--build-queue` в scratch-файл: товар есть, seed свободен, запрос → `queued`.
- [ ] Второй клик → 2/2; третий невозможен; параллельные клики → одно решение; старая вкладка
      после перегона → 409.
- [ ] Смена партии: при обрыве rsync активная прежняя; удаляется только каталог партии после
      grace; исходников на DEV 1490 = 1490; свободно на проде ≥ 7 ГБ.
- [ ] Lint / typecheck / тесты / build зелёные; CI `scout-selftest` и `--dbtest` зелёные.

## Definition of Done — память (без этого `completed` запрещён)
- [ ] `core/mesh-pipeline.md` — ручная приёмка, идентичность поколения, защита решения,
      готовность по `glb_sha`, процедура старта волны
- [ ] ADR в `decisions.md`: страница и лимит 2; `mesh_generations` + указатели текущего и
      неприкосновенность человеческого статуса; переделки через инбокс и allocator seed;
      партийная публикация с атомарным переключением
- [ ] `project-state.md` — снимок; `core/lessons.md` — уроки
- [ ] `/memory-check` выполнен, audit «чисто»

## Лог выполнения
- 2026-09-05 — план создан (draft); Codex №1: 4 блокера, подтверждены, план переработан
- 2026-09-05 — второй аудит (свой + Codex №2, `_intake/codex/q-mesh-owner-audit-2.*`):
  правки внесены; план одобрен владельцем (Plan Mode) → `in_progress`
- 2026-09-05 — П0 (e3ede8c): миграция 008 (007 занят photo-origin), реестр/привязка/готовность;
  dbtest 18/18; на живой базе указатели у 0 товаров изменились, ready 702 против 714 ложных
- 2026-09-05 — П1 (5a1c11e): allocator seed, атомарный снимок, курсор по имени снимка,
  `mesh_wave_start.sh`; сухой `--build-queue` → 10 511 заданий
- 2026-09-05 — П2–П5 (1c47efa): прод-схема/ручки/страница, DEV sync/publish/posters;
  typecheck/lint/test/build зелёные; Caddyfile применён руками с бэкапом; постеры рендерятся;
  CI gate был красным из-за чужого шага (инструмент аудита памяти не закоммичен) → починено
  соседней сессией (afbd43b), деплой пошёл

## Completion summary
[заполняется при завершении]

### Уроки (ОБЯЗАТЕЛЬНО)
[заполняется при завершении]

## Follow-up work
- [ ] `mesh_jobs.job_key` с seed — привести к регламенту `rules/mesh-priority.json`
- [ ] Список «нужна замена товара/фото» — отдельная выгрузка владельцу
- [ ] `/test/mesh-pilot10` — 1,6 ГБ старых GLB на проде (решение владельца: чистить или нет)
