---
workstream: generation-pipeline
slug: interactive-object-selection
status: draft
created: 2026-07-02
updated: 2026-07-02
completed:
---

## Цель
Сделать так, чтобы **выбор пользователя реально управлял генерацией**. Двухэтапный флоу: сначала
разбираем фото на реальные объекты и предлагаем действия, пользователь правит (поменять/оставить/убрать),
и только потом генерируем картинку по ЕГО решениям + стилю.

## Источник задачи
Разбор владельца по генерациям №1/№3: сейчас пользователь жмёт заранее заданные кнопки, но
(а) по объектам решает модель, а не пользователь, (б) промпт перерисовки игнорирует и объекты из analyze,
и кнопки «что оставить», и ограничения — картинка это общий рестайл по стилю. Нужно, чтобы решения
пользователя доходили до генератора.

## Как сейчас (факт, из кода) — что меняем
- `modules/generation-job` (`runPreview`) делает analyze → restyle → ideas за один заход, без остановки.
- `lib/prompts/registry.ts` `restylePrompt.build({style, brief})` — использует ТОЛЬКО стиль + interventionLevel.
  Игнорирует `analysis.objects`, `brief.keep`, `brief.constraints`.
- `analysis.objects` (keep/suggest_change) решает модель; влияет только на текст ideas (и то через summary).
- Экран `app/p/[id]/preview` показывает analysis-чипсы и ideas ПОСТФАКТУМ.

## Целевой флоу (двухэтапный)
**Этап A — «Разбор» (analyze, интерактивный):**
1. Пользователь загрузил фото + базовый бриф (стиль, бюджет, ограничения — как сейчас; **кнопки
   «что оставить» УБИРАЕМ**, их заменяет пообъектный выбор ниже — решение владельца 2026-07-02).
2. Vision-модель по ЭТОМУ фото возвращает список **реальных объектов** + предложенное действие на каждый:
   `change` (поменять) / `keep` (оставить) / `remove` (убрать). (Сейчас keep/suggest_change → стать change/keep/remove.)
3. Показываем экран выбора: объекты с переключателями действий (быстро протыкать), редактируемо
   (можно переопределить предложение модели). **Плюс ОДНО общее поле-пожелание (свободный текст)** — человек
   одним текстом пишет всё, что хочет (решение владельца 2026-07-02: НЕ на каждый объект, а одно на кадр).

**Этап B — «Генерация» (restyle по выбору пользователя):**
4. Пользователь подтвердил. Собираем промпт из: фото (референс) + **явные пообъектные действия**
   (поменять / оставить нетронутым / убрать) + **общее поле-пожелание пользователя** + стиль/палитра/уровень.
   «Поменять» без пожелания = менять в рамках выбранного стиля (дефолт, решение владельца).
5. Генерируем картинку. Показываем результат (+ ideas/каталог/бюджет как сейчас).

## Скоуп — что входит
- Разбить `runPreview` на два шага с точкой ожидания выбора пользователя между analyze и restyle
  (сейчас всё в одном вызове). Вероятно: `runAnalyze(projectId)` и `runGenerate(projectId)`.
- Расширить `analysis.objects.action`: добавить `remove` (contracts/project + room-analysis + промпт + UI).
- Хранить выбор пользователя по объектам в проекте (новое поле, напр. `objectChoices`/расширить analysis).
- Переписать `restylePrompt.build` — принимать пообъектные инструкции пользователя и вшивать их явным
  списком «поменять: …; оставить без изменений: …; убрать: …» + стиль. Бампнуть `version`.
- Новый/расширенный экран выбора объектов (между брифом и превью) + серверный экшен сохранения выбора.
- Обновить `app/p/[id]/preview` и прогресс-степпер под новый шаг.
- Трейсинг: новый состав шагов → бампнуть версию пайплайна (`lib/pipelines/registry.ts`), промпты — version.

## Скоуп — что НЕ входит (пока)
- Реальный подбор товаров из фидов (отдельный воркстрим v0.3, ADR-0014).
- Инпейнтинг точечных объектов (замена конкретного предмета «на месте») — сложнее; пока перерисовка всей
  комнаты, но управляемая пообъектными инструкциями.
- Изменение бизнес-модели/paywall.

## Решения человека (владелец, 2026-07-02)
- [x] Объекты: только быстрые кнопки **поменять / оставить / убрать** (без текста на объект).
- [x] Пожелание: **одно общее поле** (свободный текст на весь кадр), уходит в модель вместе с выбором.
- [x] Кнопки брифа «что оставить» — **убрать**, заменяются пообъектным выбором.
- [x] Пресеты «готовых вариантов» — НЕ делаем.
- [x] «Поменять» без пожелания = менять в рамках выбранного стиля (дефолт).
- [x] Двухэтапность (2 вызова модели с паузой) — принята.
- [→] Порядок и тексты экранов — вынесены в **подплан** `generation-screens-copy.md`.

## Модель данных (ключевое)
- Объект выбора: `{ label: string, action: "change"|"keep"|"remove" }` (без текста на объект). Массив —
  выбор по фото; храним в проекте (напр. `objectChoices` или расширить `analysis.objects`).
- **Одно общее пожелание:** `wish?: string` на проекте — свободный текст пользователя на весь кадр.

## Файлы к изменению (черновик, уточнится в реализации)
- [ ] `contracts/enums.ts` / `contracts/project.ts` — `action` = change/keep/remove, `objectChoices`, `wish?`.
- [ ] `lib/prompts/registry.ts` — `restylePrompt` принимает пообъектные действия + общее пожелание; `room-analysis`
      возвращает change/keep/remove; bump version обоих.
- [ ] `modules/room-analysis/index.ts` — объекты с тремя действиями (предложение модели, дефолт для UI).
- [ ] `modules/generation-job/index.ts` — расщепить на analyze-фазу и generate-фазу (пауза на выбор).
- [ ] `modules/visual-generation/index.ts` — прокинуть выбор + общее пожелание в промпт.
- [ ] `app/p/[id]/*` — убрать кнопки «что оставить» из брифа; новый экран выбора объектов (переключатели +
      одно поле-пожелание) + серверный экшен + степпер + preview (генерация переносится сюда). Копия — подплан.
- [ ] `lib/pipelines/registry.ts` — версия пайплайна (новый состав/интерактив).
- [ ] Тесты (unit: сборка промпта из выбора+комментариев; e2e happy-path нового флоу).
- [ ] `.memory_bank/core/*` (user-flow/observability) — отразить новый флоу.

## Критерии приёмки
- [ ] Пользователь видит объекты СВОЕГО фото и сам выбирает действие по каждому.
- [ ] Промпт генерации содержит явные пообъектные инструкции пользователя (проверяемо в `/trace`).
- [ ] Выбор пользователя реально меняет результат (два разных выбора → разные промпты/картинки).
- [ ] Трейс: версии пайплайна/промптов бампнуты; шаги видны.
- [ ] typecheck / lint / тесты / build зелёные; деплой + smoke; VPN цела.

## Лог выполнения
- 2026-07-02 — план создан (draft). Пайплайн сверен по коду; подтверждён разрыв «выбор пользователя не
  доходит до генератора картинки».
- 2026-07-02 — решения владельца учтены: 3 действия + поле-комментарий на объект; кнопки «что оставить»
  убираются; пресеты не делаем; двухэтапность принята. Реализацию не начинаем (ждём «деплой»).

## Completion summary
[при завершении]

## Follow-up work
- [ ] Инпейнтинг конкретного объекта (замена «на месте») — отдельный план.
- [ ] Связать выбранные-к-замене объекты с подбором реальных товаров из фидов (v0.3).
