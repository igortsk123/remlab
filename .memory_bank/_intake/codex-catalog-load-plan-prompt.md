# Запрос критики плана `catalog-load-hardening` (03.09.2026)

Ты — независимый ревьюер. Репозиторий открыт read-only. Прочитай план `.memory_bank/plans/catalog-load-hardening.md`
целиком и запись аудита `.memory_bank/_intake/dialog-catalog-load-0309.md` (раздел «Вопрос 18» и «Вопросы 19–23»).
Проверяй по коду `tools/scout/{refresh_daily.sh,load3.py,catalog_api_sync.py,feed_guard.py,dim_resolver.py,
category_map.py,mesh_ready.py,mesh_queue.py,cutout_sync.py,render_strategy.py,mesh_bind.py,stock_truth.py}`.

Ответь структурированно, по-русски, без общих слов, каждое замечание с file:line и приоритетом (блокер / важно / мелочь):

1. Порядок и зависимости пакетов П0–П5: что сломается, если делать в предложенном порядке; чего не хватает между шагами.
2. П2.3 — гейт бэкфилла `image_url_hd` из фида: верна ли цепочка «новый адрес → cutout_sync source_sha → mesh_queue →
   mesh_ready не готов → новое задание»? Достаточно ли двух волн (`HD_BACKFILL_SKIP_MESHED`) и приёмки
   `mesh_ready --coverage` до/после? Есть ли более дешёвый и честный гейт?
3. П2.5 — контракт `attrs_hash`: список ключей, baseline без сброса версии, связь с `enrichment_version` и с
   `capabilities.py` при `openai.off`. Что упущено?
4. П2.6 — порог 70 % по магазину: минимальные точки правки в `load3.py`, риск синтаксической ошибки `in ()`, семантика
   `FORCE_SHRINK`. Что не учтено (магазин с двумя mid в одном фиде, магазин, пропущенный по карантину)?
5. П3.4 — глубина из меша: формула `d = extent_d × w / extent_w` в каноническом фрейме из `orientation_state.resolution`;
   калибровка на 61 диване, порог 80 % в допуске ±10 %. Корректна ли постановка, какие ловушки (угловые диваны,
   подушки, масштаб от высоты), как оформить провенанс, чтобы фид не затирал.
6. П1.2/П1.3 — третье состояние `warn` через маркер `WARN:` в логе шага и дайджест; сторож на проде по опубликованному
   `refresh-status.json`. Где это ломается (несколько строк WARN, кодировка, часовые пояса, права на /opt/remlab/test)?
7. Что в плане лишнее для соло-проекта и что критически пропущено. Противоречия с уроками в
   `.memory_bank/core/lessons.md` и `.memory_bank/anti-patterns.md`.

В конце — список «изменить в плане до начала кода» (не больше 10 пунктов) и список «можно начинать как есть».
