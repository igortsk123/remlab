Продолжение аудита Юли (ADR-0115/0116). Владелец подтвердил: каноны должны УЧИТЫВАТЬСЯ при
генерации планов. Смоук боевого solve_zoned показал:

1. `media_between_windows` — РАБОТАЕТ (в зажатой сцене тумба встала в простенок по оси дивана).
2. Каскад `place_media_fireplace` (side_by_side → adjacent → tv_over, '+tvfp' первым в цепочке
   зон) в бою ПОЧТИ НЕ ВЫЗЫВАЕТСЯ: лестница посадки забирает носитель раньше через «медиа-минимум»
   (`zones.py` ~695-705 `_m0 = place_media(...)`, а также ветки _m1/_mw/_m_a) — к '+tvfp' keep
   уже без тумбы. В свободной сцене итог случайно совпадает (камин доклеивается '+fp' на ту же
   стену), но БЕЗ схемы/варианта, и adjacent/tv_over недостижимы.
3. В узкой комнате (360×430) камин гибнет на ВСЕХ кандидатах: `place_fireplace` пускает в
   _view_filter ≤60°, а validate `FIREPLACE_FAR_FROM_SEATING` требует сектор дивана 75°/2=37.5° —
   все in-view кандидаты hard-фейлятся. Несостыковка порогов (pre-existing).

Мой план:
A. Ввести helper `_media_min(room, keep, free, fixed)` в zones.py: если в keep есть и камин, и
   носитель (тв-тумба/стенка) — сначала `place_media_fireplace` (совместные схемы, вариант-теги),
   фолбэк — прежний `place_media`. Заменить 4 вызова place_media в ветках лестницы (минимум,
   лестница диванов, вейвер, альтернативные позиции). '+tvfp' в цепочке оставить (вход, когда
   лестница прошла без медиа).
B. Порог: _view_filter в place_fireplace должен читать ТОТ ЖЕ сектор, что validate (37.5 для
   дивана), а не 60 — иначе кандидаты-зомби. Либо наоборот: сектору в validate дать 60?
   Канонический источник — какой? (fireplace_view_max_deg=60 в правилах vs сектор 75° в
   FIREPLACE_FAR_FROM_SEATING).
Вопросы: согласен ли с A (риски порядка лестницы?); что канон для B; и не надо ли '+tvfp'
двигать/убирать из цепочки после A. Ответ кратко.
