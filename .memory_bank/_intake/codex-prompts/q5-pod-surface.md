Ты — независимый советник по каталогу/сборке сетов remlab (read-only). Проект тот же, сессия постоянная.

КОНТЕКСТ (факты): Q5 свода №13 ввёл контракт второго pod (quiet): `build_quiet(quiet_chat)` = кресло 3 + кресло 4 + ОБЯЗАТЕЛЬНАЯ малая поверхность (`приставной` | `столик 2` | `столик`), кресла под 35° к центру; `fireplace_flank` — пара по сторонам камина (только если камин не в углу и есть место). Замер по `tools/scout/sets3.json` (126 сетов, 72 ≥25 м²): `приставной` — 0 сетов (роли-SKU в каталоге нет; A2 в compose2 добирает его только как узкий столик ≤55 при `_zreq/_zopt`, и pick2('приставной') ничего не находит), `столик 2` — 0 сетов, `столик` — 69, `кресло 3/4` — 36 (атомарная пара, pair_key exact_sku, с 25 м²), `камин` — 62. Главный `столик` уходит в главную группу → quiet_chat НЕДОСТИЖИМ во всех сетах; pod возможен только fireplace_flank (набл.: set92 камин в углу y=65 → flank невозможен, кресло 3 перекрывает ТВ — честный отказ). Итог: пара 3/4 в 36 сетах чаще всего мёртвый груз (`passed_not_placed`).

ВОПРОСЫ (кратко, с якорями на код compose2.py / каталог):
1. Где брать поверхность для pod: добирать в compose2 атомарно вместе с парой 3/4 роль `столик 2` (категория столик/журнальный/приставной, малый: w≤70 или dia≤70, mid≠главного столика, в пределах floor cap; ≥25 м²), как один «pod-комплект» (pair_key на пару + surface_key)? Или иной источник (торшер? нет). Какие фильтры размера/цены разумны, есть ли в каталоге достаточно малых столиков (проверь по данным: catalog/фиды в tools/scout, `pick2`)?
2. Если поверхности нет и камин непригоден — оставлять ли пару 3/4 в сете (мёртвый груз) или НЕ создавать пару без комплекта (сет честнее, fill ниже)? Что масштабируемее для будущей LLM-визуализации (планы → 3D)?
3. Пара 3/4 сейчас берёт SKU без ограничения глубины (пример set92: 99×118 — почти реклайнер). Нужен ли для pod фильтр компактности (d ≤ ~95, w ≤ ~90) — по практике reading/quiet nook?
4. Что ещё в конвейере сетов должно измениться, чтобы `quiet ≥50 (только pod с поверхностью)` было достижимо, не ломая fill/бюджет сета? Риски: коллизия с A2, вытеснение ковра/столика (гейт _CORE_ZONE), P4 alt-политика.
Ответ: выводы + якоря + риски + рекомендация; коротко.
