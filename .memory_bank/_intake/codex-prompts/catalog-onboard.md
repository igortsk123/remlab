You are being onboarded as the PERSISTENT adviser for the CATALOG/PIPELINE domain of project "remlab" (separate from the layout-solver adviser session). Read-only. Answer in Russian in later turns.

Read: CLAUDE.md; .memory_bank/INDEX.md → core/catalog.md → domain/catalog-enrichment.md; core/access-and-integrations.md (Codex section); .memory_bank/decisions.md (skim ADRs about catalog: feeds, load3, enrichment, freshness, heal); tools/scout/refresh_daily.sh (daily pipeline: feeds → feed_guard → load3 → phash → enrich (--pool --vision --batch, enrich_wait) → candidates → sets index/check/health/metrics/heal → escalate → layout page), tools/scout/{feed_guard.py,load3.py,category_map.py,enrich.py,enrich_wait.sh,sets_incremental.py,health.py,compose2.py} (signatures/docstrings), tools/scout/feed-freshness.json, refresh-status.json, tail of refresh.log; DB products table (docker exec -i remlab-devdb psql -U remlab -d remlab: columns, shop_mid, in_stock, last_seen, cat_role, params).

Then answer this first question:
FACTS: (1) Daily run 2026-08-16 09:40 FAILED at load3: feed 777e580d (Гдеслон export) returned 404 HTML, curl saved it as .xml.zip over the previous archive → BadZipFile → the whole chain (enrich/candidates/heal/page) did not run. Fixes already applied in working tree: refresh_daily.sh checks PK magic and never overwrites a good archive with a non-zip; load3.py skips feeds with feed-freshness state=broken / non-zip and logs. (2) Feed 777e580d = shop 116933 nonton.ru: 11 892 products in DB, 11 883 in_stock — the feed has been 404 for some time (unknown since when: check refresh.log/feed-freshness history). (3) Owner sent a "new divan.ru feed" (bceea2bc…) — verified identical (merchant 112923, 9561 articles, prices equal) to feed 1b9f77d2… already in the pipeline; owner agreed no alias needed.
QUESTIONS: (a) What should happen to products of a shop whose feed disappeared (404) — when to mark out_of_stock/hidden (policy by days? via feed_guard state?), how that propagates to sets3 (heal/replace) and to compose2 freshness filter; is there existing logic (feed_guard degraded/stale, sets_heal) that already covers it, and what is missing? Cite code. (b) Is it safe/right to run `bash refresh_daily.sh --force` now (after the layout exam finishes) — what will enrich do (batch to ChatGPT: how many новинок, cost guard?), what could break; and how to verify afterwards that enrichment/index/heal actually ran (which files/status to check). (c) Any risk in my two fixes (PK magic check; load3 skip broken)? Return concise, actionable, with file:line anchors.
