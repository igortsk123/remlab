Вопрос НОРМЫ предметной области (нужны источники, не мнение).

КОНТЕКСТ. У нас есть схема `reading.window_anchor` (`services/planner-solver/planner/template.py:
place_window_reading`, паспорт `rules/templates.json → zones.reading`): кресло для чтения у окна.
Сейчас геометрия такая: кресло стоит спинкой к окну (спинка к простенку/подоконнику, отступ по
правилу зазора радиатора), ЛИЦОМ В КОМНАТУ, ориентация строго осевая (rot 0/180 — параллельно стене
окна). Пример из библиотеки канонов: комната 440×420, окно на север (offset 140, ширина 160,
радиатор под ним), кресло x=220 y=336.5 rot=180, торшер за плечом слева, приставной столик справа.

ВОПРОС ВЛАДЕЛЬЦА (дословно): «в таком случае направление кресла — его реально спиной к окну ставят
как тут, или под каким-то углом?»

ЧТО НУЖНО ОТ ТЕБЯ:
1. Как в практике интерьера ориентируют кресло для ЧТЕНИЯ у окна: спинкой к окну (свет через плечо
   на страницу), лицом к окну (вид), или по диагонали к проёму? От чего зависит выбор — сторона
   света, наличие вида, глубина простенка, батарея под окном, тип занятия (чтение vs «посидеть с
   кофе»)? Дай нормы с ИСТОЧНИКАМИ (дизайн-издания, руководства по эргономике освещения), а не
   общие слова.
2. Есть ли норма по УГЛУ: считается ли диагональная постановка (например 20–45° к плоскости окна)
   каноничной для оконного уголка, и при каких условиях. Если норма отличается для одиночного
   кресла и для пары — скажи.
3. Есть ли норма по расстоянию от кресла до окна/подоконника/радиатора и по стороне, с которой
   ставят торшер (через какое плечо), — сверь с тем, что у нас уже записано в
   `rules/occupancy.json` и `rules/templates.json`.
4. Посмотри наш код (`place_window_reading`, `build_reading`) и скажи, какая МИНИМАЛЬНАЯ правка
   приведёт геометрию к норме, если она расходится: менять ориентацию, добавить вариант с углом,
   или оставить как есть.
5. Отдельно: что из этого — жёсткое правило, а что вкусовое (нам важно не выдавать гипотезу за норму;
   всё недоказанное помечаем status: hypothesis с провенансом).

Файлы не изменяй. Верни: вывод; доказательства с ссылками на источники и на файлы репозитория;
риски; альтернативы; рекомендацию; что осталось неопределённым.
