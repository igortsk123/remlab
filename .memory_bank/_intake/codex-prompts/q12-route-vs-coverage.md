Вопрос ПОРЯДКА ЯРУСОВ ключа выбора плана (владелец: «ошибка в планах — исключение, поправь»).

ФАКТЫ.
- Ночной экзамен 20.08: 272/272 ok, но сторож `tests/test_template_integrity.py::test_route_is_walkable`
  упал: сцена `set5-trapezoid` — главный маршрут 60 см при пороге теста 70 и полу
  `zones.json → quality_gate.route_min_cm = 75`.
- Замер по всем 272 планам: маршрут <75 см ровно в ОДНОЙ сцене (set5-trapezoid, 60 см).
- Разбор сцены: победила гипотеза `sofa_lamp+tpl+tv+st+dc`, ключ
  [0, 0, 0, -3, 0, 1, [0,0], 0, 20.258, 0, 4, 0]; конкурент `compact_sectional#1:pouf_table`
  имел ключ [0, 0, 0, -2, 0, 0, ...]. То есть победитель покрыл на ОДНУ preferred-зону больше
  (-3 против -2), но имеет `MAIN_PATH_TIGHT` = 1, а конкурент — 0.
- В `planner/zones.py: plan_key` ярус `-covered_preferred` идёт РАНЬШЕ яруса
  `_main_path_violations`. При этом наш же `quality_gate` в zones.json декларирует правило
  «предмет не ставится вообще, если ухудшает уже достигнутое (маршрут, фокус, щели)», а
  комментарий сторожа гласит «циркуляция важнее лишнего предмета».
- `MAIN_PATH_TIGHT` сейчас SOFT при любом сужении (`validate.py` ~строка 283), порог берётся из
  `passage_min_cm('main')`.

ВОПРОС.
A. Что правильнее: (1) поднять ярус main-path ВЫШЕ `-covered_preferred` в plan_key;
   (2) сделать `MAIN_PATH_TIGHT` HARD ниже пешеходного минимума (например <70 см), оставив soft
   в вилке 70–90; (3) оставить как есть и признать 60 см допустимым в трапеции;
   (4) что-то иное. Оцени риск каждого варианта на 272 сценах (у нас нет права ломать floors:
   столовая 238 сцен, медиа 269, TIMEOUT 0).
B. Если (2) — что делать, когда НИ ОДНА гипотеза не держит 70 см (узкие/контурные комнаты):
   отказывать сцене (как MEDIA_MISSING с сертификатом) или релаксировать с записью в трейс?
C. Есть ли норма практики на минимальную ширину главного маршрута в жилой комнате (источники)?
   У нас 75 см — «цель главного маршрута», 91 см — книжный проход, 46 см — вторичный.
D. Не сломает ли поднятие яруса детерминизм/производительность (ярус считается по готовому плану,
   MAIN_PATH_TIGHT уже есть в validate).

Посмотри код сам: `planner/zones.py` (plan_key, _main_path_violations), `planner/validate.py`
(MAIN_PATH_TIGHT), `rules/zones.json` (quality_gate, routes), `rules/occupancy.json` (route_min_cm).
Файлы не изменяй. Верни: вывод; доказательства; риски; рекомендацию с конкретной правкой; что
проверить после неё.
