# Критика плана `stock-and-dims-honesty` (03.09.2026)

Ты — независимый ревьюер, репозиторий read-only. Прочитай `.memory_bank/plans/stock-and-dims-honesty.md`
целиком. Проверяй по коду `tools/scout/{page_alive.py,stock_check.py,stock_truth.py,003-stock-truth.sql,
stock_canary.py,catalog_media.py,flat215_demo.py,compose2.py,solver_run.py,scene_build.py,export_plans_ai.py,
mesh_dims.py}` и по `.memory_bank/decisions.md` (ADR-0141/0147/0148/0171), `core/lessons.md`, `anti-patterns.md`
(уроки 320/326 про текстовые маркеры).

Правила владельца, которым план обязан соответствовать: (1) размер никогда не придумывается — только
достраивается по мешу с честной пометкой; (2) флаг наличия API Гдеслона не собираем; (3) наличие проверяем
там, где возможно, антибот-магазины не дёргаем и честно пишем «неизвестно»; 404/сбои сайта/блокировки не
равны «нет в наличии»; (4) со страницы брать дополнительное, если дёшево.

Ответь по-русски, каждое замечание с file:line и приоритетом (блокер/важно/мелочь):
1. Н0: правка регулярки `availability` (snake_case, href=) и второй голос `overallStock` — риски массового
   ложного снятия при включении; какой замер обязателен ДО; как встроить в `fold()`/`gate()`.
2. Н1: `failure_kind`, якорь домена, `blocked_until`, канарейка в цикле, вычистка `suspect`, gzip/ранний
   обрыв — что ломается, чего не хватает, что лишнее для соло-проекта.
3. Н2: `stock_confidence` — где считать (в `reconcile()`), как не сломать `catalog_media.state` и
   `drop_unavailable`; достаточно ли трёх значений.
4. Н3: цена/имя со страницы — стоит ли писать `page_price` в `products` или держать в наблюдениях; риск
   двух писателей цены (load3 vs stock_check).
5. Р1: удаление всех дефолтов размеров — точки в коде, последствия для сетов (77/116 диванов с предположенной
   глубиной), порядок: пересборка сетов, экзамен солвера, демо; как показывать «размеры не указаны».
6. Р2: что проверить в худших случаях калибровки (Риббл 186 vs 108, Ольен 72 vs 120) прежде чем строить v2.
7. Порядок пакетов и оценка; чего критически не хватает в Verification.
В конце: «изменить до начала кода» (≤10 пунктов) и «можно начинать как есть».
