Ты — независимый senior-ревьюер. Разбери задачу сам, по коду репозитория. Не считай ничьи
гипотезы верными, своё мнение строй на файлах и строках.

## ЦЕЛЬ ВЛАДЕЛЬЦА
Прогон 3D-мешей на нодах Salad (`tools/scout/salad/ssh_run.py`, супервизор
`tools/scout/salad/batch_show.py`). Владелец: нода, которая систематически валит задания,
должна автоматически переаллоцироваться после 3 сбоев подряд, «делай системно»,
«повторяться не должно».

## НАБЛЮДЁННЫЕ ФАКТЫ (из журнала `tools/scout/mesh-run-progress.jsonl` и логов, 01.09.2026)
- Нода `35b10e39` за прогон вернула 30 результатов со статусом `input_failed`, ошибка
  `<urlopen error [Errno 101] Network is unreachable>` и `<urlopen error timed out>`.
  Она оставалась в пуле до конца прогона и продолжала брать задания.
- За последний час: 38 `ok`, 9 `input_failed` (8 из них — эта нода), 1 `flat_shape`,
  1 `transport_failed`, 2 `cached`.
- Распределение `flat_shape` по нодам за весь прогон: 7, 6, 4, 4, 4, 4, 3, 3, … (много нод).
  Распределение `input_failed`: 30 на `35b10e39`, 7 на `270e036a`, остальные — единицы.
- Есть ещё статус `failed` (3 раза на `270e036a`).
- Существующий механизм переаллокации: `batch_show.cull_slow_pulls()` — POST на
  `containers/{grp}/instances/{iid}/reallocate`, с бюджетами `MAX_CULL_PER_TICK`,
  `MAX_CULL_PER_HOUR`, `CULL_FREEZE_S`, историей `_CULL_LOG`. Судит только инстансы в
  состоянии `downloading` (медленная тяга образа).
- Локальная постобработка между пачками (`batch_show.py` ~строки 289-300) шла 38 минут,
  три шага убиты earlyoom на машине с ~11 ГБ ОЗУ: `apply_repairs.py` RSS 6.95 ГБ,
  `orient_worker.py --run --limit 200 --vlm` 8.6 ГБ, `topview_render.py` 8.86 ГБ.
  Всё это время ноды тёплые (`--keep-alive`), оплачиваемые и без заданий.

## ВОПРОСЫ, НА КОТОРЫЕ НУЖЕН ТВОЙ САМОСТОЯТЕЛЬНЫЙ ОТВЕТ
1. Прочитай `ssh_run.py` (жизненный цикл задания: класс `Jobs`, `worker()`, `summary()`,
   `run()`) и `batch_show.py` (курсор `done`, шаги постобработки, `cull_slow_pulls`).
   Какие статусы результата сейчас считаются виной ноды, какие — свойством задания, и что
   происходит с заданием и с курсором `--skip` в каждом случае? Приведи строки.
2. Теряются ли задания безвозвратно? Если да — по какому пути и сколько именно за этот прогон.
   Проверь, попадают ли такие SKU в очередь перегона `tools/scout/mesh-reseed.json`
   (её собирает `apply_repairs.py`).
3. Как правильно устроить автоматическое снятие/переаллокацию ноды: по какому сигналу, с
   каким счётчиком и порогом, где хранить состояние при том, что ноды обслуживаются
   потоками (`nodes`, `nodes_lock`), и какие предохранители нужны, чтобы при общей деградации
   сети не выкосить весь пул. Где именно в коде это место.
4. Какие статусы можно безопасно считать «виной ноды», а какие нельзя, и почему.
   Разбери `worker.py` (`tools/scout/salad/worker.py`) — откуда берутся `input_failed`,
   `bad_cutout`, `flat_shape`, `failed`.
5. Есть ли гонки/дедлоки в предлагаемом тобой решении с учётом `Jobs.cv`, `nodes_lock`,
   супервизора и `js.wait_all()` / `STALL_S`.
6. Простой нод во время локальной постобработки: как это чинить системно и в каком порядке,
   учитывая, что шаги умирают по OOM (то есть работу не доводят), а машина одна и слабая.

Инспектируй репозиторий сам. Ничего не меняй, файлы не создавай и не удаляй.

Верни: 1) выводы; 2) доказательства с путями и строками; 3) риски и краевые случаи;
4) альтернативы; 5) что рекомендуешь и почему; 6) допущения и неопределённости;
7) какое свидетельство изменило бы твой вывод.
Будь критичен: ищи причины, по которым очевидное решение окажется неверным.
