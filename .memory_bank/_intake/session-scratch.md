# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->

## 2026-08-06 — полный аудит пайплайна (5 субагентов: каталог, обогащение, виз, планировщик, веб-сверка)
- CRITICAL: vision-батч part 4 (2000 товаров, весь свет: люстры 1071, бра 240, вазы 115) ОПЛАЧЕН и completed у OpenAI (batch_6a74919bb34c8190bc9c8d75540ce3c2), но в БД не записан; покрытие обогащения реально 23 028/25 028 (92%), не «весь пул». Завтра 09:40 крон отправит эти 2000 повторно платно БЕЗ --vision и затрёт enrich-batch-id.txt.
- CRITICAL: refresh_daily.sh:73 отправляет батчи обогащения, но шага забора (--fetch/enrich_wait) в кроне НЕТ — ежедневная оплата в никуда; утренний текстовый батч всего пула (~$3) выброшен. enrich_wait.sh:11 считает failed/expired «готово»; fetch() молча return при не-completed.
- Слияние текст+vision НЕ состоялось: model_text==model_vision у всех 23 028 строк — запись в catalog-enrichment.md про «роль/функция от текстового прогона» и «13 пакетов без ошибок» неверна.
- Дельта-хеши (text/geometry/image) НИЧЕГО не триггерят: todo() фильтрует только по версиям; товар со сменившимся описанием/фото не переобогащается никогда. phash покрывает 17% пула (сканирует thumbs/, не imgcache/).
- «Качество 0.86» — системная эвристика quality() (enrich.py:67), не голден-метрика; +0.30 за эхо категории из промпта. Голден = согласие с terra, не с истиной.
- Каталог: роль «кашпо» = 0 товаров (DENY «товары для сада» съедает ветку раньше ALLOW), ковёр 26 шт — системные дыры всех стилей. Молчаливый fallback load3.py:52 «грузим всё» при нечитаемом category-roles.json; нет предохранителя от похудевшего фида; KIDS-фильтр не в daily-пути (зануленные детские вернутся). Алертинга крона нет (сбой 05.08 заметил человек).
- Фото: 24 920/24 997 в imgcache (99.7%), 77 без картинки (4 битых /None/ URL из фида), 0 пустых файлов.
- Планировщик: дефолт движка 'dfs' (solver_run.py:537), beam только в set_report.py; batch_collage/render6/7 рендерят DFS — разнобой движков = часть «нелогичности». Метрика solver_check «ok» слепа к soft-нарушениям («числа соблюдены, расстановка глупая»). Ковёр в движок не передаётся (кладётся пост-хок по столику), rug-правила из occupancy.json кодом не читаются; щель 20–70 реализована мягко (5–60) вместо hard. llm_planner.py (Э6) написан, план draft. Дверь/окно захардкожены одни на все сеты. Тесты 60 зелёные.
- Виз (ветка B ADR-0060..0063): пост-проверки финала НЕТ (ворота collage_audit до оплаты, после gpt-image-2 — только глаза; в ветке A QA был — потерялся при переезде); fidelity финала держится только промптом (перегенерация без маски); edit_gpt_raw и fal_run БЕЗ ретраев (роняло 2/7 прогонов); 13% позиций брак приёмки после 3D (столик×7, кашпо, стеллаж); 39% мешей бракует самопроверка; usage OpenAI не считает входные image-токены — экономика кадра занижена; промпт 12.7КБ против урока «сжать»; ключи OpenAI из чужих .env (v0-health-card, mltest); viz-fidelity-playbook.md описывает ветку A — отстал на поколение. shell-база (ADR-0063) не дефолт, batch_collage глотает exit-код приёмки.
- Веб-сверка 2025-26: по всем 4 направлениям паритет с индустрией. Layout: rule-based+beam надёжнее SOTA-LLM (OptiScene: 50% usable гостиных); гибрид LLM→solver = наш Э6. SKU-fidelity: коллаж+edit — мейнстрим; A/B-кандидат Nano Banana Pro / Gemini 3 Pro Image ($0.134/2K, батч −50%). Image-to-3D: Hunyuan3D 2.1 на fal $0.05 дешевле Trellis-пути. Обогащение: каскад — стандарт (Shopify ICLR 2025); не хватает golden из прод-ошибок, LLM-judge-сэмплинга трафика, дрифт-бейзлайна, uncertainty-эскалации.
