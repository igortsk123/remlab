# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 31.08 (продолжение): топ-вью переведён на попиксельный mesh_render (кляксы центроидного сэмплинга — замечание владельца); ваза 99272_180… подтверждена перевёрнутой (низ шире верха ×1.7) — upright-ярус в плане orient-v2 (regress-тест). Пилот: волна тестового сета ждёт машин mesh-run10 (batch, baked, 10 реплик). Незакрытое к утру: пересборка топ-вью (фон b3pexw78c), волна+остаток очереди, финальный отчёт пилота.
- 31.08 вечер, фронт корпусных (владелец: «проблема с тв-тумбами/комодами»): рабочий метод — front_by_depth в `tools/scout/salad/cabinet_front.py`: ступени глубины 2–15% толщины корпуса (фасеточные волны Hunyuan умирают <2% — калибровано на комоде 5969), полость тоже фасад (ниша ТВ-тумбы); кандидаты только 2 широкие стороны. RGB/edge/шейдинг НЕ работают (петли боков, фасетки, скользящий свет). Верх: «ножки+крышка» (громкий margin ≥0.3) и паспортные классы осей (Y/Z/X, 3 класса — 6 гипотез дают margin 0 из-за X180-двойников). Правило владельца: «у комодов/тумб видны ножки, когда лицом». Codex-вердикт (cabinet): согласие 2 из 3 сигналов, VLM только pairwise A/B (промпт готов в answer.md), 5582 = открытая консоль → front_equivalence [0,180] (front_access_kind='open' в capabilities.py); бенч 60 (20 calibration + 40 blind). Внедрение в orient_worker — шаг плана orient-v2. 8333 — чёрный (цвет-брак), ждёт HD-перегена, не мучить.
- 31.08 (демо/сцена, финальный контур владельца): по кнопке демо — СЕРВЕРНЫЙ кадр 3D-сцены из мешей БЕЗ ИИ/GPT (`draft_render.scene3d_frame`, режим SCENE3D=1 в контейнере remlab-draft): clay-комната + `scene_mesh.raster_mesh` в общий z-буфер; предмет ЛИБО мешом, ЛИБО clay (двойников нет); GLB качаются с галереи по sid в кэш контейнера; ориентация из orient.json. Грабли, починенные по одной: (х,y) плана — УГОЛ, world_vertices ждёт ЦЕНТР (+w/2,+d/2); bind сервиса 127.0.0.1 → DRAFT_HOST=0.0.0.0 (Caddy получал refused, docker cp затирает контейнерные правки!); memory limit draft 640MB → 1.5GB (OOM рвал ответ «Load failed»); trimesh доставлен pip'ом В контейнер (при пересоздании образа draft — переустановить/вшить!); ковёр в сцену мешом нельзя (канон, «сабля в воздухе»); ТВ: низкая тумба ≤55см → НА ней, выше → над ней +12см (правило владельца). План top-вью НА плане демо: topsprites/<sid>.png поверх форм (фронт вниз). Кнопкам вернули прямой рендер; интерактивная 3D-комната — служебный ?scene3d=1.
- 31.08: в цикл конвейера пачек И волны добавлены шаги «ориентация» (orient_worker --vlm по pending) + «топ-вью» (кэш) + публикация orient.json/topsprites — каждый новый меш размечается сам. Фронт кресла 6583 подтверждён (сиденье к камере) — «спинкой в кадре» = правильный rot плана.
- scene3d ковёр: вклейка ГОМОГРАФИЕЙ (2 аффинных треугольника = излом-«бугор» по диагонали) только в ПОЛОСУ пола (sub > ztc−8, сдвиг растеризаторов калибруется медианой zt−sub, не фикс +25см — тот закрашивал низ кресла); «второй коврик»/пятна = дыры пороговой маски 238 на светлых местах ковра → адаптивная маска по углам фото (фон карточки 255, тело ковра ~220) + fill_holes — `draft_render.py:_paste_rug`
- Правило владельца 31.08 (ВСЕ сцены): камера ВСЕГДА внутри периметра комнаты, сквозь стену не смотреть — клэмп глаза pad 25см в render() scene3d-ветки `draft_render.py`
