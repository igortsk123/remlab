# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-09-05 07:30 — снято: `hub_page.py` закоммичен (`d421d38`) после подтверждения владельца,
  что параллельная сессия закончила. `scene_mesh.py` в дереве уже чист.
- 2026-09-04 — ГАРД от урока 399: PreToolUse-хук на Bash `tools/hooks/guard-bash.mjs` блокирует
  `pkill -f` / `pgrep -f` (exit 2, fail-open при своей ошибке); включён в
  `.claude/settings.local.json`. Дисциплина 4 раза не удержала — теперь держит машина.
  → ADR + `core/regression-net.md`; проверить работу хука, когда починится Bash.
- 2026-09-04 — Мёртвый Bash-инструмент ≠ мёртвая машина: хуки (`node`, `git`) в тот же момент
  работали, аудит памяти прогонялся. Признак — `EDQUOT` на запись в scratchpad при том, что
  `/tmp` занят на 31 %. Значит квота (блоки/иноды), а не «кончилось место». `/tmp` смонтирован
  с `usrquota` (`findmnt`), утилита `quota` не ставится на tmpfs — лимит меряется опытом:
  626 МБ отказ, 243 МБ работает.
- 2026-09-04 — ТУПИК ПРИЁМНИКА (корень ручной чистки, пункт 4 «Остатка»): вся цепочка очистки
  живёт ТОЛЬКО внутри `batch_show.post_steps()`, в кроне её нет (`crontab -l` — только
  `idle_guard.sh` и `batch_window.sh`). Приёмник переполнился → сторож денег погасил группы
  (`mesh-group-halt.json`, kind=shared_infra) → конвейер встал → чистка не запускается →
  приёмник остаётся полным. Разрывает круг только человек. → нужен сторож вне конвейера.
- 2026-09-04 19:42 UTC — ЗАМЕР СКОРОСТИ после починки учёта (`tier_compare --hours 1`, пул
  работает час): low-4 39 мешей / 4.1 опл. нодо-часа = **9.5 меша на оплаченный нодо-час**
  (1.32 ₽/меш), low-5 31 / 4.4 = 7.0 (1.75 ₽). Вместе ≈ **8.2 меша/нодо-час, ~1.5 ₽/меш**.
  Потолок при медиане генерации 208 с — 17.3 меша/нодо-час, то есть занятость ≈47 %; потери —
  прогрев и простой, а не скорость генерации. За 24 ч (грязное окно с остановкой): 360 мешей /
  103.5 опл. часов = 3.5/час. `mesh-batch-3` за сутки 6 мешей на 8.8 опл. часов = 11.46 ₽/меш,
  сейчас корректно `stopped` (19:42 вне окна 09–15). → в план п.7, сверить с ADR-0177.
- 2026-09-04 20:31 UTC — ВТОРАЯ остановка, причина ДРУГАЯ: `kind: silence` (715 нодо-минут,
  40 мин без меша), приёмник при этом ЗЕЛЁНЫЙ (4.05 ГБ). Два узких места: (1) образ
  `cu124-localpaint2` весит **62.6 ГБ**, после смены образа кэш Salad холодный — ноды висят в
  `downloading`. НО закачка БЕСПЛАТНА и в оплаченные часы не входит: `tier_compare.paid_hours`
  считает только состояние `running` (владелец поправил меня — я сперва свалил цену на закачку,
  это неверно). (2) НАСТОЯЩАЯ причина: перепись 19:40–20:31 показывает стабильно ~16 нод в
  `running` (6 в low-4, 9–11 в low-5) — их и оплачивали, — а супервизор видел «кандидатов 1,
  тёплых 0, подключено 0» и «порт NNNNN: воркер не прогрет или не отвечает», затем «нет живых нод
  30 мин». То есть контейнер у Salad поднят и тарифицируется, а воркер внутри не отвечает.
  Классика урока 388 («Running» ≠ работает), но теперь в масштабе: 16 оплаченных нод → 6 мешей за
  50 мин, 23–27 ₽/меш. Отказы транспорта за час: `ssh/mid_generation` 8, `ssh/empty` 7.
  → в план: почему воркер не встаёт на `localpaint2`; зомби-детектор (`phase=='failed'`, P1-7)
  либо не отработал, либо ноды честно грелись все 50 минут.
- 2026-09-04 — ПОДТВЕРЖДЕНО В ПРОДЕ, что фикс учёта работает: в логе конвейера приёмник дорос до
  7.4/7.3 ГБ → «чистка приёмника: ok» → сейчас 4.05 ГБ. До фикса счётчик после уборки не падал
  (7.37 при реальных 5.13) и лечился только перезапуском контейнера.
- 2026-09-04 — Цепочка очистки — ПЯТЬ шагов, а не три: `drain.sh --keep` → `ingest_registry`
  → **`apply_repairs` (приёмка)** → `mesh_bind` → `receiver_purge --apply`. Пропуск приёмки
  даёт «к удалению: 0, нет в базе 161»: без вердикта приёмки база не помечает `mesh_status`,
  а чистилка удаляет только помеченное. Порядок владельца (01.09): копирование → база → сверка
  → удаление. `batch_show.py:503-520`.
- 2026-09-04 21:00 UTC — ОТВЕТ НА ГЛАВНЫЙ ВОПРОС (замер `~/scout-scenes/.warmup-probe/warmup.jsonl`):
  прогрев ИСПРАВЕН — нода `aa0d35ca` прошла `phase=warming` → `warm=true, phase=ready` за **147 с**.
  Деньги жрут МОЛЧАЩИЕ ноды: `c9cedfa9` не отвечала на `/health` 305 с и дальше, вися в
  оплачиваемом `running`. → урок: «running + не отвечает» и «running + греется» — разные болезни
  с разным лечением, и в логе их надо различать (правка `probe_warm`, коммит eb62cac).
- 2026-09-04 — ДЕФЕКТ НАЙДЕН И ЗАКРЫТ (коммит 6d12057): два детектора зомби расходились.
  `batch_show.cull_dead_warmups` считает пустой `/health` смертью, а супервизор `ssh_run` — нет
  (`warmup_fault({})` == '', `phase` отсутствует). Но `batch_show` работает только МЕЖДУ пачками,
  а `ssh_run --keep-alive` держит процесс часами → молчащие ноды тарифицируются и не снимаются
  никем. За 5 часов лога: 1 пересадка на 16 оплаченных нод. Фикс — `ssh_run.silent_fault()`,
  порог `MESH_SILENT_ZOMBIE_S`=600 с, причина без секунд (иначе ломается группировка
  «общая беда»), тест `case_silent_node_is_zombie`.
- 2026-09-04 — Решения владельца перед сном: цикл проверок ТОЛЬКО в этой сессии (облачный агент
  не видит дев-машину); деньги — пробовать всю ночь даже при простое; разрешена пересборка образа
  (`localpaint3` новой группой по ADR-0154), если причина в образе.
- 2026-09-04 22:37 UTC — ПУЛ ОСТАНОВЛЕН ПЛАТФОРМОЙ: `no_credits_available` — на Salad кончились
  кредиты. Группы погасил не наш сторож (в `money-guard.log` после 20:31 остановок нет, стоп-файла
  нет, `idle_guard` молчит с 15:20) — это сделал Salad. `ensure_group_started` теперь говорит
  прямым текстом «НЕТ КРЕДИТОВ … нужно пополнение» (урок 404 внедрён, коммит 48bc1eb).
  ВАЖНО для диагностики впредь: «группы stopped + стоп-файла нет + сторож жив» = сперва проверять
  баланс, а не искать виноватого в своём коде.
- 2026-09-04 21:11–22:37 — ЧИСТОЕ ОКНО ПОСЛЕ ВСЕХ ПОЧИНОК: 104 меша за 2 ч на 12.3 оплаченных
  нодо-часа = **8.5 меша/нодо-час, 1.46–1.48 ₽/меш** — в норме ADR-0177 (low 1.61–1.62).
  Замер прогрева за те же 2 ч (318 наблюдений, 78 уникальных нод): прогрелась 31 нода, медиана
  **162 с** оплаченного `running` до `warm=true`; фазы `warming` 101, `ready` 31, `failed` 1.
  Молчащих нод всего 5, и по-настоящему долго молчала одна (`c9cedfa9`, 1176 с) — её новый
  `silent_fault` снял бы на 600 с. Вывод: прогрев и приёмник здоровы, узкое место было в учёте
  приёмника и в неснимаемых молчащих нодах, оба закрыты (ADR-0178, коммит 6d12057).
- 2026-09-05 00:40 UTC — Стенд вмешивался в ПРОД: `case_halt_blocks_start` писал фальшивый
  стоп-файл в боевой `~/scout-scenes/mesh-group-halt.json`, и пока он там лежал, живой конвейер
  отказывался поднимать группы. Плюс краснел при настоящей остановке. Третий случай того же
  класса за сутки (`case_window_gate` читал живой JSON правил, `case_group_status_mixed` требовал
  боевой ключ). → урок: тест, который читает или пишет боевой путь, — не тест, а участник прода.
- 2026-09-05 05:32 UTC — Ночной цикл (8 обходов 21:00–05:32) закончен, задание снято. Все восемь
  итераций: `no_credits_available`, нод ноль, деньги не тратятся, приёмник зелёный (4.05–4.36 ГБ),
  стоп-файл не появлялся, конвейер и сторож денег живы (13+ ч аптайма). Чинить было нечего —
  блокер внешний, пополнение баланса делает только владелец. За ночь закрыт пункт 10 плана
  (стенд писал фальшивый стоп-файл в БОЕВОЙ путь) и снят PLAN-STUCK у `q12-situational-canon`
  (переведён в `partial`: заморожен владельцем 19.08, а не забыт).

- 2026-09-05 07:41 UTC — Баланс Salad пополнен, пул поднялся: `mesh-low-4` running (10 реплик),
  `mesh-low-5` и `mesh-batch-3` deploying (20+20), ~30 машин качают образ. Ночной простой
  закончился — блокер был чисто внешний.
- 2026-09-05 — ЗАМЕР ВЫХОДНОГО ДНЯ (инициатива владельца): суббота, `mesh-batch-3` поднята ВРУЧНУЮ
  до окна, чтобы собрать статистику дешёвого тарифа в выходные — таких данных нет. Ручной запуск
  владельца выше правила «вне окна не поднимать» (оно про НАШ подъём). В 15:00 UTC группу штатно
  погасит `batch_window.sh down`. Считать батч ОТДЕЛЬНО «до окна» и «в окне»: смешивать нельзя,
  на этом уже обожглись (урок 401 — вышло «в 6 раз дороже» вместо 15 %).
- 2026-09-05 — ПОЛНЫЙ АУДИТ ПАМЯТИ: пишем регулярно (721/1226 коммитов, блокнот 231), кит v1.6.0
  без дрейфа; дефекты — Tier 0 устарел (М1–М3 в проде, фокус на мебельном треке без ADR),
  decisions.md 474 КБ + 7 дублей номеров, уроки 217 КБ без тем, 61 открытый план, `_intake/` 22 МБ
  вне аудита. План → `plans/memory-bank-audit-2026-09.md` (draft, одобрен владельцем; ждёт Фазу 0
  и «деплой»). Кит-фидбек (6 пунктов) — в Части 3 плана. Codex: дубли ADR → новые номера, не суффикс.
- 2026-09-05 — РЕШЕНИЯ ВЛАДЕЛЬЦА (Фаза 0 аудита памяти): фокус = мебельный трек первым до точки
  «партнёр принял демо», потом смета (цель v0.4 не меняется) → ADR при деплое; перенос уроков по темам
  разрешён; архив планов по манифесту — да; CI-гейт → block сразу после финального `/memory-check`
  уборки. Правило общения (просто, без терминов, с «зачем») → строка в CLAUDE.md.
