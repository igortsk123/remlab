# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 26.08 ОТКРЫТО (к следующему заходу): (1) плумбинг диагностики оси — `_axis_contract.media` пишет глобал, перетираемый последним вызовом place_media, из-за чего сцены с носителем от схемы «ТВ+камин» отчитываются `centered` с чужим числом (реально ~47 см у Г-дивана, 23 сцены); (2) расхождение артефакт↔экспорт 4 см по ковру (`test_pose_hash_round_trip`, set10) — экспорт пересчитывает геометрию ковра; (3) вердикт владельца по плану №210: set105-pylons теперь собирает `two_sofas_2armchairs` вместо `sofa_2armchairs` — нужен пересмотр сторожа; (4) лесенка состава + сертификат низкого заполнения (Codex q13) — заполняемость нельзя делать целью ключа.
- 28.08 ПРОТОТИП ДВУХПРОХОДНОЙ СХЕМЫ (/test/share/twopass/, идея владельца): проход А —
  paste=True коллаж (вклейки по маскам сцены + следы на полу, ~3 с, без ИИ); проход Б —
  gpt-image-2 medium «только улучшить, ничего не двигать» + лист эталонов. Места ДЕРЖАТСЯ на
  обоих видах (торшер/стол/стеллаж/ТВ впервые все на местах без ремонта). Klein на роль
  улучшателя не годится (теряет текстуры, оставляет служебные контуры). Грабля: в promptе
  прохода Б обязательно правило «синий прямоугольник = ОКНО» — иначе рисует синюю рулонную
  штору. Хвост: ТВ-панель в боковом ракурсе видна с ребра → на кадре наклонена/сползла.
- 28.08 ДВУХПРОХОДНАЯ v2 — «повёрнутая аппликация» (инсайт владельца: GPT легко правит ВИД, тяжело
  расставлять/поворачивать): фото товара натягивается перспективным преобразованием на плоскость
  предмета в его повороте (ковёр — на плоскость пола; белый фон → альфа; дальние первыми).
  Проход Б после этого держит и места, И развороты, и правильные SKU; дефект «ТВ сползло с тумбы»
  ушёл. ГРАБЛЯ: PIL transform(PERSPECTIVE) ждёт ОБРАТНОЕ преобразование (выход→фото).
  Скрипты orient_paste.py/twopass2b.py в scratchpad, страница /test/share/twopass/ (v2 и v1).
- 28.08 ЧЕРНОВИК v3 (проход А, /test/share/rough/): аппликация кладётся на ВИДИМУЮ грань коробки
  (выбор по нормали к камере × вес длины грани) — стулья перестали быть «лезвиями»; слои
  ковёр→тени→мебель; окно рисуется стеклом с рамой ПО ПРОЕКЦИИ проёма (маска синего врёт из-за
  затенения clay; раму не рисовать на стене, за которой камера, и клипить по кадру); кадр
  обрезается по комнате (semantic>0) — зона «за стеной» убрана, GPT не выдумывает лишний угол;
  предмет вплотную к камере (>32% кадра) фото не получает — остаётся объёмом. Скрипт
  orient_paste2.py в scratchpad; при переносе в draft_render — это новый collage_oriented().
- 28.08 ЧЕРНОВИК: три подхода на одной расстановке (/test/share/rough/): v3 повёрнутые
  фото-аппликации (узнаваемость макс, развороты хромают), v4 цветные прокси (места идеальны, вид
  мультяшный), v5 SDXL-ControlNet по НАШЕЙ depth (~10 с, фотореализм и объёмные формы; цвета
  приблизительные, окно/дверь теряются — лечится пост-наложением по проёму и промптом). Владелец
  просил «не обязательно фото — реалистичные формы с похожим цветом»: v5 ближе всего.
- 28.08 РАЗБОР СОВЕТА GPT (q21, Codex + свой прототип): принят ПРИНЦИП «первый проход
  детерминированный», но GLB-путь — гибридный кандидат, не готовая схема. Факты: Trellis-брак по
  нашему кэшу 38% (столики 2/6, торшеры 0/2); СЦЕНОВОГО z-buffer-рендера мешей у нас НЕТ
  (mesh_render — один товар орто; вставка была экранная) — спайк 3–5 дней; уникальных SKU
  напольных ролей в банке 378 (≈$7.6/6ч на Trellis), считать по SKU, не по позициям. Баг старого
  кэша: Hunyuan-фолбэк копируется поверх, а отрицательный *-check.json остаётся — mesh_trusted()
  вечно false. EasyControl на fal ЕСТЬ (spatial/subject), но регион-масок нет; masked IP — только
  через причинностный тест. Normals — вторично. Прототип v8 (/test/share/rough/): одиночный стул
  из фото пары (crop→Trellis), развороты стульев к столу ПРАВИЛЬНЫЕ; вписка орто-рендера в маску
  инстанса даёт «врезки» на тёмном. План: 2 эксперимента Codex — (1) гибридный Pass A (аппликации
  vs GLB-z-buffer, один GPT-проход, гейт scene_exact_rate), (2) причинностный fal-тест масок.
- 28.08 СЦЕНОВЫЙ РЕНДЕР МЕШЕЙ (v9, scene_mesh.py в scratchpad; только 9 товаров примера, по
  требованию владельца без массовой генерации): один z-buffer на комнату (пустой compile_scene
  как фон и глубина стен) + перспективная растеризация всех GLB с текстурами (низ меша = пол,
  масштаб по-осям в W/D/H, YAW_FIX=180 — фронт Trellis смотрит на +Z); ковёр — фото-варп на пол
  до мешей; окно стеклом. 3–4 с/вид на CPU. Проход Б поверх: фотореализм, места и развороты
  держатся; единственный сдвиг — столик в C1 перепрыгнул за диван. Это лучший конвейер из всех
  опробованных: черновик v9 (4 с) → gpt-image-2 (35 с).
- 28.08 КАЛИБРОВКА ФРОНТА МЕШЕЙ — автоматическая, масштабируется без агента (вопрос владельца):
  фронт GLB определяется сверкой силуэта с ФОТО товара (рендер yaw 0/90/180/270, IoU; yaw пишется
  рядом с GLB в кэш, один раз на SKU). Симметричным предметам разворот не важен (IoU ровный);
  слабый сигнал (торшер 0.21) = флаг «ориентация ненадёжна» → fail-closed в прокси/аппликацию.
  Мировой разворот = p.rot − yaw_калибровки. После калибровки диван встал лицом к комнате.
  Страница /test/share/rough/ переведена в формат «только меши»: 1 вид-меши, 2 вид-GPT, 3/4 то же
  для второго вида, 5 — лист товаров для сравнения (порядок показа — требование владельца).
- 28.08 МЕШИ, ФИКСЫ ПО НАХОДКАМ ВЛАДЕЛЬЦА: (1) масштабирование в Ш×Г×В обязано идти ПОСЛЕ
  выравнивания фронта калибровкой — иначе при калибровке 90/270 ширина растягивает «глубинную»
  ось меша (столик 120×50 вставал поперёк); нормали граней — через ту же матрицу выравнивания.
  (2) Высоты: h в payload страницы null → рендер брал типовые по роли (стеллаж Кейн 75 рисовался
  120); добавлен fallback «высота из каталога по имени товара» (name→h из demo-data roles).
  Отвечено владельцу: расстановка ПОЛНОСТЬЮ наша (солвер+рендер), GPT только «фотографирует»;
  калибровка фронта и вся цепочка автоматические, на SKU один раз, слабый сигнал → fail-closed.
- 28.08 СТУЛЬЯ: конвенция rot у стульев спорит с фронтом — снято СЕМАНТИКОЙ, а не знаком:
  стул в меш-рендере всегда разворачивается лицом на центр ближайшего стола (atan2; face=(−sin r,
  cos r) → r=atan2(−dx,dy)). (2) В промпт прохода Б добавлен пофреймовый паспорт проёмов из
  _cam_geom («в этом кадре одно окно…/двери нет; углы на N% ширины») — GPT перестал выдумывать
  окно и терять дверь. Оба вида B теперь совпадают с мешами по составу, местам и разворотам.
- 28.08 ГРАБЛЯ КОНВЕНЦИЙ ПОВОРОТА (два неверных «бумажных» вывода подряд): направление фронта в
  меш-рендере определять ТОЛЬКО эмпирическим тест-листом (один предмет, rot 0/90/180/270,
  фикс-камера) — rot-test.jpg. Измерено: фронт стула в scene_mesh = (−sin r, −cos r); формула
  «стул лицом к столу»: rot = atan2(−dx, −dy). КОРЕНЬ ХАОСА: силуэтная калибровка фронта не
  отличает перед/зад стула (силуэты совпадают, IoU 0.39) — в калибровку нужно добавить сравнение
  ЦВЕТА (сиденье видно спереди), иначе пер-меш конвенции гуляют на 180°.
- 28.08 ПРИЁМКА МЕША — КАК ЛОВИТСЯ БРАК (проверено на живом стеллаже): фронтальный силуэт+цвет
  брак НЕ ловят (плита прячется ЗА корпусом: Trellis-брак 72% IoU = «годен» — вот почему старая
  самопроверка пропускала). Работает ПРОФИЛЬНЫЙ замер: аспект бокового ракурса против реальной
  глубины товара (Г/В). Кейн 75×35×75: Trellis-профиль 1.02 при ожидаемых 0.47 → БРАК; Hunyuan
  0.49 → годен. Порог: профиль/фронт ≤ 1.8× ожидаемого. Вошло в план viz-mesh-orientation
  (блок «приёмка»), Hunyuan3D-фолбэк $0.10 против $0.02 Trellis (только для брака, ~$21-22 на банк).
- 28.08 РЕШЕНИЕ ВЛАДЕЛЬЦА: двойной брак меша (Trellis и Hunyuan) → ЗАМЕНА ТОВАРА в сете штатным
  лечением слота, а не вечная аппликация. «Годный меш» = 4-е условие контракта слота
  визуализируемых ролей (к фото/наличию/габариту, расширение ADR-0121/0124). Аппликация — только
  временная деградация до пересчёта замены. Внесено в план viz-mesh-orientation.
- 28.08 (вечер) ДЕПЛОЙ viz-mesh-orientation, состояние: модули в репо (scene_mesh/mesh_orient/
  mesh_gate/orient_selftest, фикс mesh_make, хук слота, очередь refresh_daily за флагом), селфтест
  зелёный, коммит cddcbdc. ОТКРЫТАЯ ГРАБЛЯ: VLM-судья стабильно путает перед/зад стула с высокой
  спинкой (и по орто-, и по сцен-кадрам, со свап-проверкой) — для «сидячих» ролей ИИ только
  кросс-чек, первично детерминированная геометрия сиденья (совпало со схемой владельца).
  Владелец предложил гибрид (фронт-метадата один раз + мат-проверка facing ±15° + сцен-QA
  vision-моделью с JSON-вердиктами + коррекция кодом) — ПРИНЯТ с поправкой (сидячие — геометрия
  первична). Финальный комплект: ручная карта фронтов + правило «стул к столу» в рендере
  (временно до facing_target в экспорте) — /test/share/rough/ (1-меши/2-GPT/3-меши/4-GPT/
  5-товары). Codex q23 (алгоритм фронта сидячих, seat anchors, порядок работ) — в фоне.
- 28.08 (ночь) ГИБРИД ЗАДЕПЛОЕН: (1) mesh_front.py — детерминированный фронт «сидячих» по
  геометрии (посадочная плоскость 0.25–0.65H нормалью вверх → спинка = масса выше сиденья у
  кромки; abstain при отрыве <0.25) — на золотом мини-сете попал в истину с запасом (стул margin
  1.0, диван 0.99), там где VLM стабильно ошибался; (2) в mesh_orient сиденье-геометрия —
  АВТОРИТЕТ для seat-ролей, VLM понижен до тени (не даёт confident, q23); (3) СПРАЙТЫ ВИДА
  СВЕРХУ из GLB на канвасе демо (идея владельца): рендер pitch 88° с фронтом вниз, файл
  sprites/<imghash>.png, страница кладёт поверх стилизованной формы (404 → форма остаётся) —
  опубликовано. (4) Требование владельца: СОЛВЕР тоже должен знать перед/зад — семантически он
  знает (build_dining ставит развороты), не хватает экспорта seat_anchor/facing_target с
  валидатором (q23 день-2, контракт в плане). q23: canonical_transform хранить ОБРАТИМО рядом с
  raw GLB (не перезаписывать), манифест инвалидировать и по версии алгоритма+hash фото.
- 28.08 РАЗВЯЗКА СТУЛЬЕВ (вердикт владельца по A/B-паре): истинный фронт стул-меша = 0 —
  детерминированный классификатор mesh_front был ПРАВ с первого раза; ошибались (а) VLM-судья
  (стабильно 180) и (б) мои собственные чтения перспективных рендеров (три противоречивых
  прочтения подряд). УРОК-ПРАВИЛО: пер-модельная ориентация утверждается ТОЛЬКО (1) детермин.
  классификатором и (2) человеческим кликом по A/B-паре сцен-кадров для спорных SKU (секунды);
  ни агентское чтение рендеров, ни VLM решающими не являются. Финальный конвейер примера:
  rot солвера без доворотов + фронт 0 → кадры одобрены. В план: A/B-подтверждение — штатный
  шаг голден-сета.
- 28.08 БЕНЧ VLM «перед/зад» (протокол владельца: один рендер на вопрос, истина известна, 5×2,
  модель отдаёт confidence; счёт = ±confidence): НА СТУЛЕ с кратким промптом «спереди/сзади» все
  путают зад; С ПРИЗНАКАМИ СТОРОН В ПРОМПТЕ («сзади видна только спинка, сиденье скрыто») —
  qwen3-vl-instruct 10/10 (9.50), gpt-5.6-terra 9/9 (8.74). Диван/тв-тумба: обе 10/10 (~9.8).
  gpt-4.1-mini 7/10 — УБРАН ПОЛНОСТЬЮ по решению владельца (даже не тень). claude-haiku-4.5
  уверенно врёт наоборот (2/10, −5.5), gemini-3.5-flash не отвечает на этом эндпоинте.
  ФОЛЛБЭК вместо человека (человек может 200–300 максимум): qwen3-vl основной + terra кросс,
  промпт с признаками по роли, голосование 5×2; расхождение/низкий счёт → человеческий A/B-клик.
- 28.08 В ОЧЕРЕДЬ (владелец, исследовать): ориентация мешей на 30k — (1) Trellis-OA
  (orientation-aligned генерация, фронт стандартизирован сразу; проект «Orientation Matters»,
  xdimlab.github.io/Orientation_Matters — авторы сами показали систематические ошибки VLM на
  неочевидных фронтах); (2) 3d-orienter (ICML 2025): оценка up/side/front осей меша, pretrained
  чекпойнты, batch inference; (3) One-shot 3D Object Canonicalization (CVPR 2025): ОДИН эталон на
  категорию → все стулья приводятся к его ориентации (33k Objaverse, проверка людьми ~8 часов).
  Прод-пайплайн владельца: Trellis-OA → confidence → согласованность двух методов + стабильность
  к пересэмплингу (скачет на 180° = спорный) → VLM (наш qwen-протокол) → человек ТОЛЬКО как
  последний fallback (десятки-сотни, не 1500). Пороги: >0.9+согласие=авто; 0.6–0.9=второй метод;
  <0.6 или расход >45–90° = exception queue. Спорные отбираются АВТОМАТИЧЕСКИ по confidence,
  не глазами.
