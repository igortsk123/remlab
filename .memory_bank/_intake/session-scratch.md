# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 16.08 (каталог, запрос владельца): глубокий разбор фидов под nook/консоль/складной — В ФИДАХ ЕСТЬ, но спрятано ролями/фильтром импорта: «Кушетки» 152 (роль диван; 42 шт 120–130×55–58 — nook-скамья), «Пуфы и банкетки» 218 (17 банкеток 100–144×37–40 под ролью пуф), «Тумба консольная Норд» 150×35×22 (роль тв-тумба — это низкая консоль за диван), 572 узких комода 90–140×33–45 (консоль/пристенный ряд), 124 раскладных обеденных (8 шт ≤90 в сложенном), 7 круглых ≤110; отфильтровано при импорте category_map.py «не относится к гостиной»: «Кухонные уголки» 117 (магазин 116933) и «Столы-книжки» 17. Консолей как категории/роли нет. Решение владельца: разметка ролей (банкетка/кушетка/консоль/раскладной), возврат категорий.
- 16.08 слепая оценка раунд 1 (10/20): beam 2, greedy 5, оба плохи 3 — plan_key НЕ совпадает с глазом владельца. Кодекс замерил по артефактам 7 повторяющихся сигналов: маршрут от двери через коридор диван→ТВ (01: 49 vs 166 см), кресло медиапригодно ≤45° (02/05/09), дальность кресла до ТВ (03), столовая во фронтальном конусе 45° (05/06/10: 100%/31%/94%), фронтальная композиция ТВ-стены (компаньоны 0/1/≥2), Г-диван hugged vs floating в <30 м² (07), фактическая посадка/пуф vs номинальный seat_rank (04). НАХОДКА (проверена по коду): solver_run._extra строит экземпляры только из qty базовой роли + «диван 2»; отдельные роли «кресло 3/4» и alt-«кресло 2» (P4) в FLOOR НЕ попадают → солвер не видит богатый банк → «57 м² и одно кресло». Также plan_key.have не учитывает storage как preferred. Порядок ключа предложен: hard → missing_required → entry_sightline → media_seat_contract → dining_view_cone → corner_contract(<30) → missing_valid_preferred → frontal_composition → seating_deficit → -realized_seats/-armchairs/-footrest → axis → штрафы; seat_rank номинальный убрать. Кресла: схемы default/facing/bridge/tandem ориентируют кресла поперёк/к дивану/назад — нужны атомарные media_parallel (0°/45°/315°) и media_bridge (45°/315°) с intent-метками.
- 16.08 Q1 находка: старая ветка «диван 2» в solver_run читала items['диван 2'] ПОСЛЕ сворачивания «X 2»→«X» — ключа никогда не было, второй диван до солвера не доезжал (как и кресло 3/4). Через RAW_BANK set126 (57 м²) стал sofa_facing_sofa+qz+fp+din. Урок: адаптеры-нормализаторы ролей проверять тестом «каждый SKU банка — ровно одна терминальная причина» (_bank_unused).
- 16.08 Q1: pkill -f 'acceptance_run.py zoned' убил и мои фоновые waiter'ы (в их cmd та же строка) — killить только по точному префиксу процесса; реплеи сцен не запускать параллельно экзамену (10 воркеров + реплей → таймаут 10 мин). Codex (постоянная сессия): причина dining 220→218 — не «диван 2», а бюджет beam (первые K инвентарных строк, недостижимые ступени съедают бюджет) и dining_sacrifice.max_steps_down по строкам, а не по достижимым деградациям; оба фикса внедрены, симптоматический (диван 2 secondary в small) откачен.
- 16.08 правило владельца: исследовательские задачи (root cause/дизайн/каталог/порог) — сначала Codex-советник (постоянная сессия), потом делать (mirrored в авто-память feedback-codex-adviser-first).
- 16.08 задача владельца: НОВЫЙ ФИД divan.ru https://export.gdeslon.ru/uploads/exports/bceea2bcdc0946b0e09e4d0a2a1e7708382bdc0a.xml.zip — скачать, автообновление по аналогии с другими фидами (cron/pipeline), нужные категории → роли, обогащение (ChatGPT) — проверить, что проходит автоматически; загрузить товары; пересчитать статы (матрица цен/классов изменится: дорогие диваны). Отдельный пакет после текущего шага Q3; Codex — ОТДЕЛЬНАЯ сессия (каталог/пайплайн — другой домен).
- 16.08 фид divan.ru (bceea2bc…) = ТОТ ЖЕ магазин 112923, 1:1 с 1b9f77d2… (9561 артикулов, цены равны) — второй раз не грузим (задвоение), владелец: алиас не нужен — просто забыл, что фид уже есть; ничего не подключаем. НАХОДКА: конвейер 16.08 09:40 упал — load3 FAIL: фид 777e580d = 404-HTML сохранён поверх архива → BadZipFile → обогащение/индексы/heal/страница НЕ прошли. Фиксы: curl проверяет PK-magic и не затирает прежний файл; load3 пропускает broken/не-zip по feed-freshness. Перезапуск --force после экзамена (sets_heal пишет sets3).
- 16.08 Q3 бюджет (Codex): семейный+общий циклы складывались (до 4+12 полных прогонов; set18-long 6 мин/11 гипотез, 6 TIMEOUT) → ЕДИНЫЙ full_chain_cap по режимам (small 3/transitional 4/large 4/xl 3), семейства только large/xl, общий цикл — если <2 валидных семейств; баги: ключ бюджета 'medium' при room_mode 'transitional' (бюджет не читался); ранний return в enumerate_k делал media-квоту недостижимой (media-forms in chosen = 0) — починен. u_cluster (пара по бокам столика) выиграл в 13 сценах при старом ключе.
- 16.08 16:30 (после перезагрузки): Q1–Q3 в рабочем дереве, НЕ закоммичены (ждут гейта экзамена: 272 без TIMEOUT, dining ≥220, медиа 252, attempted ≤ cap). Q4 патч (plan_key_v2 shadow) готов в scratchpad q4_plan_key.py, применять после коммита Q3. Далее Q5 (посадка по площади/второй pod: кресло 3/4 с 25 м², пара одной модели), Q6a–e каталог, Q7 слепой раунд 2. Также: перезапуск refresh_daily.sh --force (после экзамена; sets_heal пишет sets3), Codex каталожная сессия — про фид 777e580d (404) и его товары.
- 16.08 16:45: две перезагрузки VM подряд во время экзамена @10 воркеров — RAM на VM сейчас 4 ГБ (было 6–10): 10 воркеров × ~0.4 ГБ = перебор → ACC_WORKERS 10→6. Владелец: 12 ядер дал, но памяти мало — при следующем апгрейде просить RAM ≥8 ГБ для 10 воркеров.
- 16.08 гейт Q3: dining база 227 (было 220), TIMEOUT 0 @6 воркеров, u_cluster выиграл 15. Регресс LEVEL A: sacrifice по реальным деградациям дошёл до бездиванной armchair_pair (+sacr6, set47/39) — guard «диван в банке ⇒ бездиванные ступени не жертва» (LEVEL A старше столовой). Тесты «ковёр под посадкой»/«группа растянута» судили кресла 3/4 и второй диван из ДРУГИХ зон — скоуп на _zones.seating.members и secondary_scope_roles (Кодекс: _zones агрегирует по tpl_id — членство зоны нужно из артефакта).
- 16.08: /tmp (scratchpad) — tmpfs, ПЕРЕЖИВАЕТ НЕ ВСЕ перезагрузки: патч Q4 и промпты Codex потерялись после reboot → переписал; промпты/ответы Codex теперь в .memory_bank/_intake/codex-prompts/ (в git). Правило уже в авто-памяти (dev-env: /tmp=tmpfs — большое на диск), распространить на «всё, что жалко».
- 16.08 каталог (Codex-каталожная сессия): конвейер НЕ работал с 11.08 (nonton 404 → BadZipFile валил load3 ежедневно; enrich/heal/индексы стояли 5 дней). Мои фиксы дополнены: _h→_feed_hash (затирал функцию — Codex поймал регресс), load3 пропускает stale/empty (не только broken — иначе старый архив оживляет in_stock), curl --fail + полная zip-проверка, feed_guard сохраняет mids при broken (иначе compose2 не знает чей источник) + broken_since; реестр: 777e580d=116933 nonton, broken с 11.08. Nonton — в 1076 позициях всех 126 сетов → карантин через compose2 freshness-фильтр (новые сеты) + dry-run heal, НЕ массовый in_stock=false. Перед платным enrich — enrich.py --sample 0 (размер дельты, 2×N запросов, лимита стоимости в cron нет) → показать владельцу.
- 16.08 19:45: пересборка sets3 для Q5 дала ГРЯЗНЫЙ дифф (856 расхождений во всех 126 сетах): восстановленные mids для broken nonton (116933) включили freshness-карантин в compose2 → магазин выпал из ВСЕХ банков (1076 позиций) → банки пересобрались иначе. sets3 ОТКАЧЕН к бэкапу. Решение: карантин nonton — отдельный шаг с владельцем (меняет все планки/слепые пары), не смешивать с Q5; для Q5 временно оставить nonton в банках (mids у broken пусты до решения).
- 16.08 Q5 (продолжение): применён pod-патч (quiet_chat = пара 3/4 + ОБЯЗАТЕЛЬНАЯ поверхность, fireplace_flank; skip_when primary_rich/existing_pod), `check_quiet_contract` в validate (QUIET_POD_* H0, severity.json), `QUIET_DIAG` (template.place_quiet) → cert.second_pod.quiet_diag; v2 `large_enrichment_deficit` категориальный (пара | 2 дивана | valid pod), `enrichment_reachable`.
- 16.08 Q5 root cause «pod недостижим»: в 126 сетах 0 `приставной`/`столик 2` (роли-SKU приставной в каталоге нет; pick2 главного столика режет малые по конверту 120–135 и prop_check к дивану). Codex (каталожная сессия, `_intake/codex-prompts/q5-pod-surface.answer.md`): атомарный pod-комплект кресло 3+4 (один SKU, qty=2, компактные ≤90×95 pref / ≤100×105 hard, без реклайнеров) + столик 2 (dia 35–70 | стороны ≤70, h 40–65, вне конверта/пропорции); без поверхности пару НЕ создавать; лечить pod целиком; pod_delta_price/pod_floor_fp отдельно. Внедрено: compose2 (`pick2(pre_filter,no_envelope,no_prop)`, pod_key), zones.json `seating_pods.pod_kit`, requires_roles + столик 2, secondary_scope_roles + столик 2, templates.json claimable quiet + столик 2, sets_incremental heal pod целиком, solver_run dia→w/d.
- 16.08 Q5 геометрия quiet_chat: якорь-кресло под 35° у стены торчало углом в стену → 0 позиций; переделано: якорь = поверхность (спиной к стене), кресла вдоль стены ±35° к центру, сдвиг на вылет поворота. Контракт: reach ≤120 см, сходимость фасадов 40–140°, оба к центру (не «интервью» 90/270).
- 16.08 утечка Q1/Q3: форма `u` главной sofa_2armchairs тянула кресла 3/4 в главную группу (→ sofa_4armchairs greedy, pod обезглавлен) — теперь a3/a4 в `u` только для group_id sofa_4armchairs.
- 16.08 набл.: v1 plan_key предпочитает богатую primary (sofa_2armchairs/two sofas) плану «тонкая primary + pod» (seat_rank) — pod ставится, когда primary не богата; в set92 камин в углу (y=65) → fireplace_flank невозможен (честный отказ).
- 16.08 sets3.json пересобран полностью с pod-комплектами (бэкап `sets3.json.q5pod.bak` = до pod-комплектов); экзамен полный после сборки.
- 16.08 22:07 sets3 пересобран: 126 сетов, pod-комплект в 72/72 сетах ≥25 м² (0 gap); РИСК: все 72 пары кресел 3/4 — nonton.ru (фид сломан с 11.08, карантин ждёт владельца) — при карантине pod исчезнут; нужен фолбэк на другие магазины (Codex: компактные 90×95 в основном divan.ru). Экзамен полный запущен 22:07 (logs/exam-q5pod.log).
- 16.08 экзамен Q5 (после pod-комплектов): 269+3, TIMEOUT 0, dining 241, media 269; two-sofa 30 (гейт 37 — на других сетах), quiet выбран 4 / cert full_valid 64/74. Codex (`codex-prompts/q5-exam-gate.answer.md`): гейт Q5 по СЕРТИФИКАТАМ (second_pod full_valid|точная причина, two_sofa full_valid, 0 голых pod, планки, TIMEOUT 0); v1 выбор не менять до Q7; primary_rich не привязывать к площади (несколько зон — по функции, не по метражу: H&G/House Beautiful); v2: убрать ярус -valid_arm (сделано); pod из broken/stale-фида — fail-closed (mids+mids_quarantine_pending; сделано только для pod_kit; общий карантин — владелец); two_sofa block_infeasible: 22/32 угловой главный диван (guard) → код unsupported_subtype:corner_main; дыра поиска _add_L только слева → форма L_right (сделано); планку 37 не переносить на новые сеты (доля full_valid/eligible без corner). Мой корень доп.: ARMCHAIR_TOO_DEEP убивал П/facing-композиции (кресло лицом к дивану за столиком ≈120 см) — исключение facing ≤30° (occupancy.json armchair_too_deep_exempt_facing_sofa_deg), путь к экрану судит check_zone.
- 16.08 время: премиум-сцены large_xl (set102/105) 7.5–9 мин на сцену после правок (лимит 900 с) — следить за TIMEOUT.
