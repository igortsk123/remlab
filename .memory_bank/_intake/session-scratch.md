# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 01.09 АДРЕС ДЕМО: постоянный `https://remont-lab.online/test/buildup/` (решение владельца).
  Старый `/test/flat215-demo/` оставлен переадресацией. Пути правились в 5 местах:
  `publish_demo.sh`, `flat215_demo.py`, `improve_mode.py` (DEMO_URL/IMG_BASE),
  `salad/batch_show.py` (топ-спрайты!), `mesh_priority.py`. Спрайты (289) переехали.
- 01.09 БАГ РОЛЕЙ: ключ ленты замены считался как `role.split(' ')[0]`, поэтому «стол обеденный»
  превращался в «стол» — такого ключа в ENV нет, и обеденные столы НЕ попадали в ленту вовсе
  (лист подбора пустой, заменить нечем). Правильно — убрать только хвостовой номер
  (`re.sub(r'\s+\d+$','',role)`), чтобы «стул 2» → «стул». Починено в сборке и на странице.
- 01.09 БАГ ПРИВЯЗКИ КАДРОВ: `loadShots()` при неизвестном `vid` сваливал кадр в ПЕРВЫЙ вариант.
  После удаления «Моих вариантов» все их снимки разом уехали в лофт — карточка показывала чужой
  стиль. Теперь привязка по стилю → по названию, не опознан — `orphan`, не показываем.
- 01.09 Светильник ARTE LAMP A7051PN-1BK: единственная ревизия меша `flat_shape`, основание меша
  0.26×0.15 от высоты вместо квадратного 38×38 → при масштабировании по осям растягивается в 1.7
  раза. ГПТ ни при чём: черновик — наш рендер. `_scene3d_glb()` берёт GLB по sid из галереи и
  качество НЕ проверяет (`mesh_ready` в этом пути не участвует).
