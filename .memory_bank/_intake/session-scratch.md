# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 26.08 ОТКРЫТО (к следующему заходу): (1) плумбинг диагностики оси — `_axis_contract.media` пишет глобал, перетираемый последним вызовом place_media, из-за чего сцены с носителем от схемы «ТВ+камин» отчитываются `centered` с чужим числом (реально ~47 см у Г-дивана, 23 сцены); (2) расхождение артефакт↔экспорт 4 см по ковру (`test_pose_hash_round_trip`, set10) — экспорт пересчитывает геометрию ковра; (3) вердикт владельца по плану №210: set105-pylons теперь собирает `two_sofas_2armchairs` вместо `sofa_2armchairs` — нужен пересмотр сторожа; (4) лесенка состава + сертификат низкого заполнения (Codex q13) — заполняемость нельзя делать целью ключа.

## 2026-08-28 — 3D-меши: уход с fal на свой образ Hunyuan3D 2.1 (Salad)
- Решения владельца: fal НЕ используем; Trellis НЕ используем (только Hunyuan3D 2.1 ради PBR);
  площадка SaladCloud, свой образ; пилот 500; люстры включить (прежнее «подвесное не моделим» —
  ограничение Trellis, а не задачи). Лицензия Hunyuan (Territory без UK/EU/KR) — владелец 28.08:
  разрешение есть, работать спокойно, результаты качать к нам. Гейт снят решением человека.
- Экономика: fal Hunyuan 3.1 rapid+PBR $0.375/шт → пул 11 631 = $4 361; свой образ на Salad
  ($0.16/ч 4090) ≈ $31–105. Пилот 500 ≈ $2–4.
- Воронка каталога (замер 28.08 по remlab-devdb): products 32 347 → in_stock 19 529 →
  +обогащение active/quality≥0.65/фото/цена 16 245 → −мёртвое фото = 11 631 в
  `candidates-index.json`. Напольных ролей 4 182. Люстра — крупнейшая роль пула (3 732).
  126 сетов используют 529 уникальных SKU, 1 205 с альтернативами.
- ТЕХ-НАХОДКА: 29 ГБ VRAM у Hunyuan 2.1 — это СУММА стадий (форма 10, покраска 21), а не пик.
  При выгрузке формы перед покраской пик 21 ГБ → 4090 (24 ГБ) должна тянуть без offload.
  Публичные «139 с/модель» — вероятно, замер с обеими моделями в памяти. Проверяется пилотом
  (`STAGED=0/1` в `tools/scout/salad/pipeline.py`).
- Лимит Salad: образ ≤35 ГБ в СЖАТОМ виде; гейт 28 ГБ в `salad/build.sh` по
  `docker manifest inspect`. Blackwell (5090) требует CUDA ≥12.8 → ДВА образа, один не годится.
- Опровергнуто моё же утверждение: у Salad ЕСТЬ терминал/SSH-ключи в портале.
- `cutout` в `viz_paste.py` ходит в fal (birefnet) → в образе локальный BiRefNet
  (`salad/preprocess.py`), fal из конвейера уходит совсем.
- Дефекты приёмки, найденные и ИСПРАВЛЕНЫ (GATE_VERSION 2→3): `geometry_layer` не применял
  трансформы узлов (а `mesh_render.load_parts` применял — два слоя судили о разной геометрии);
  манифест инвалидировался только по хешу GLB и версии гейта, не по фото/габаритам/генератору
  (отрицательный вердикт переживал уточнение габаритов); у профиля не было нижней границы
  (сплющенный меш проходил) → PROFILE_MIN 0.45; вырожденные треугольники были заявлены в
  docstring, но не проверялись → DEGENERATE_MAX 0.02.
- Найдено попутно: роль `шкаф` (165 товаров) обогащается и индексируется, но отсутствует в
  `compose2.ROLES` (compose2.py:230) → никогда не подбирается. В follow-up плана.
- Расхождение памяти: `core/furniture.md` пишет 25 034 товара, в БД 32 347 (28.08).
- 28.08 ПРОГОН 41 SKU ВСЕХ СТИЛЕЙ (лист вердиктов владельцу): цепочка Trellis→приёмка→Hunyuan→
  замена дала ready 11 / suspect 22 / hunyuan_fixed 2 / replace 6 (двойной брак: 3 синих дивана
  Босс, торшеры). Классификатор: confident 13, ambiguous 5, no_seat 17 (несидячие роли — фронт
  через qwen-фоллбэк). Тайминги на товар: Trellis 30с/$0.02, приёмка 8с/$0, классификатор <0.1с,
  Hunyuan 88с/$0.10 (только браки). ГРАБЛИ ПРОГОНА (починены): trimesh.split висит часами на
  «губчатых» мешах → метки связности + numpy-агрегаты (0.3с); прореживание _thin до 50k граней
  для контрольных рендеров; nohup без -u = пустой лог; pkill -f ловит обёртку харнесса (селф-килл
  дважды). Параллельная сессия дорабатывает mesh_gate (GATE_VERSION 3: context_sha, PROFILE_MIN,
  деген-грани, свой Hunyuan на Salad, пилот 500) — правки совместимы.
