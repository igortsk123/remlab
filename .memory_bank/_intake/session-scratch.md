# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 31.08 18:10 — меши/Salad, открытые хвосты: (1) владелец просил прогнать ВСЕ товары демо flat215 —
  в волну поставлены 34 (все мешевые из 6 вариантов), но ленты замен в `demo-data.json` идут БЕЗ
  `sid` (~180 карточек) → сопоставить с каталогом нельзя, нужна доработка `flat215_demo.py`;
  (2) предложено расширить GPU-классы группы (3090/3090 Ti/A5000 — 24 ГБ, $0.09–0.10/ч против
  $0.16 у 4090; замеренный пик воркера 13.9 ГБ покраска / 8 ГБ форма) — ЖДЁТ решения владельца,
  правка группы пересоздаст инстансы; (3) квота организации 10 реплик, все заняты.
- 31.08 17:30 — состояние на паузу (моя часть durable разнесена: ADR-0139/0140, core/demo-planner,
  deployment, project-state, lessons — закоммичено). НЕЗАКРЫТОЕ: волна лечения 111 перегонов идёт
  на 1–2 нодах (вытеснение batch), конвейер `batch_show --batch 5` ведёт СОСЕДНЯЯ сессия
  (перезапуск 17:09 с динамическим пулом, её план `plans/mesh-dynamic-node-pool.md`) — не трогать
  из этой сессии, чтобы не создать гонку; лог `_intake/batch-run5.log`. Ждёт владельца: апгрейд
  VDS (4–8 ядер/8GB) вместо DEV-костыля рендера (ADR-0139). Не сделано: параллельная векторизация
  растеризации (кадр 11с, цель ~5с), страница разметки спорных ориентаций, gold-set 60 тумб.
- 31.08 17:40 НАБЛЮДЕНИЕ для владельца конвейера (соседняя сессия, план mesh-dynamic-node-pool):
  Salad показывает 2 running-ноды, но `warm_ports()` отбраковывает ОБЕ («порт 24031/21051: воркер
  не прогрет или не отвечает») — при том что 24031 РАНЬШЕ работала (на ней шла волна 21/91).
  Гипотеза: `/health` воркера не отвечает, пока идёт генерация (синхронный обработчик) либо
  ssh-проба таймаутится под нагрузкой → прогон считает ноду мёртвой и уходит в «нет тёплых».
  Проверить: `/health` во время генерации + таймаут ssh_text (50с). Если так — пробу делать
  неблокирующей (отдельный поток/эндпоинт) или считать ноду тёплой при активном задании.
- 31.08 УРОК (моя ошибка, владелец поймал): я несколько раз доложил «новых мешей нет», проверяя
  ТОЛЬКО конечную папку DEV (`~/scout-scenes/.../hunyuan21/v2`) — а туда меши попадают лишь после
  шага «стаскиваю» (drain.sh) в конце волны. Реально они всё это время копились на ПРИЁМНИКЕ прода
  (контейнер `mesh-receiver`, `/data`, MESH_SINK_URL из спеки группы): 164 комплекта, 3.1 ГБ, 54 за
  последние 90 мин. Правило: прогресс генерации мерить на приёмнике (`docker exec mesh-receiver
  find /data -name complete.json -newermt ...`), а конечную папку — только для готовности к показу.
  Второе: тишина в логе прогона ≠ простой (ssh_run печатает результат ЗАДАНИЯ по завершении, а не
  прогресс) — проверять активные ssh-сессии/инстансы, а не отсутствие строк.
- 31.08 ЗАМЕР ЭКОНОМИКИ (для плана mesh-dynamic-node-pool, соседняя сессия): фактический темп
  31 меш/час на 6 нодах (94 меша за 3 ч по приёмнику) → при $0.66/ч (смесь классов) себестоимость
  $0.021/меш — В ПЯТЬ РАЗ дороже идеальной ($0.0043 = 3090 171с при 100% загрузке). Ноды считают
  <1/3 времени: задания подаются по одному через ssh с паузами на раздачу и прогрев. Главный
  резерв удешевления — очередь заданий на ноду вместо одного за раз (не число нод: больше нод =
  быстрее, но мешей на те же деньги столько же). Баланс на 31.08 19:30 — $11.99 (владелец) ≈
  570 мешей / 18 ч в нынешнем режиме; остаток пилота 445 + волна ≈ $9 — хватает впритык.
  Замеры карт (по манифестам приёмника): 4090 медиана 110с $0.0049/меш · 3090 171с $0.0043 ·
  3090 Ti 215с ~$0.0060 (4 замера). 3090 медленнее 4090 в 1.55×, но дешевле на меш на 13%.
- 31.08 ТОЧНОСТЬ ГЕНЕРАЦИИ — разбор с Codex (полный ответ: _intake/codex-quality-answer.md),
  РЕШЕНИЕ ВЛАДЕЛЬЦА: ничего не менять сегодня, вернуться завтра. Суть вердикта (правит мою
  гипотезу): octree 380→512 и paint 512→1024 НЕ лечат «расплывшийся силуэт» — детализация лишь
  режет уже предсказанную форму, а выше 768 покраска вне документированного режима. Лучший
  выигрыш на рубль — ДВА SHAPE-SEED + выбор лучшего по silhouette IoU/паспортным пропорциям и
  ОДНА покраска победителя (≈1.5× цены, не 2×). Multi-view (сильнейший рычаг) требует другой
  чекпойнт Hunyuan3D-2mv, а в фидах Гдеслона у оффера РОВНО ОДНО фото (проверено 5000 офферов,
  max 1 <picture>) → нужен скрейп галерей + спайк на 15-20 товарах. Параметры сейчас:
  `pipeline.py:455` octree 380/steps 50/guidance 5.0, `pipeline.py:76` MAX_NUM_VIEW 6/PAINT 512.
  План на завтра: замер на 12-20 сложных SKU (2 seed vs 1; paint 6/512 vs 6/768), затем ярус
  качества для ~25% (диван/кресло/стеллаж). Бюджет: baseline на остаток пилота ≈$9.35 при
  балансе $11.99 — глобальное удвоение не влезает.
- 31.08 УРОК (моя грабля, дорогая): `timeout N ~/venvs/.../python script.py` убивает ОБОЛОЧКУ,
  а питон с ProcessPoolExecutor остаётся жить сиротой (ppid=1) вместе с 10 воркерами — за вечер
  я так наплодил два зомби-пула, которые ели ядра и мешали конвейеру, а их вывод уходил в
  убитый пайп («прогон молчит»). Правило: тяжёлые прогоны запускать в фоне с логом в файл и
  убивать ПО PID группы (`kill -9` по ppid=1 + multiprocessing), а не полагаться на timeout;
  после проверки — `ps -eo pid,ppid | awk '$2==1'` на сирот.
- 31.08 диагноз (на завтра): шаг «ремонт» (apply_repairs) тоже стал падать по таймауту шага
  2700с — та же болезнь, что была у топ-вью: объём вырос (~190 мешей), кэш приёмки есть, но
  полный проход дорожает. Лечится так же — бюджет времени + пропуск уже обработанного.
- 01.09 ЗАМЕР ЦВЕТА (191 модель, `scratchpad/color_diag.py`; + рендер-сверка 40 шт):
  врёт СВЕТЛОТА, не оттенок — медиана отклонения хромы 3/255, >20 лишь у 7%; |dL|>20 у 49%,
  |dL|>40 у 26%. Направление в среднем — СЖАТИЕ к середине (альбедо ≈ 0.69·фото+48, равновесие
  L≈154; по рендерам 0.77·фото+33, L≈145), т.е. тёмное чуть светлеет, светлое темнеет — ОБРАТНО
  гипотезе владельца, но у него верно про хвосты: тумба (79,78,76)→(0,0,0) чёрная,
  комод (111,110,108)→(210,210,209) почти белый. Обе прикидки с изъяном (атлас = невидимые
  грани; рендер = свой ламберт), точную кривую мерить на калиброванном наборе.
  Гипотеза владельца «на пёстром разницы не видно» — подтверждается: ошибка в светлоте,
  фактура/рисунок её маскируют, однотонный серый показывает в упор.
  Потеря именно в покраске, не в показе: в альбедо-карте витрины 112923:4922194797449750070
  фасады уже чисто чёрные при сером фото. Ещё: металличность карт >0.3 у 15% моделей
  (стеллаж 0.85) — отдельная проблема, мебель блестит как металл.
- 01.09 АУДИТ ПОКРЫТИЯ МЕШАМИ демо flat215 (сверка живого прода: `demo-data.json` ×
  `/test/mesh-pilot10/<sid>/model.glb` × `topsprites/<sid>.png`). 46 позиций в 6 вариантах,
  37 уникальных sid. GLB есть у 26/37; топ-спрайт — у 11/37 (план показывает схему у 26).
  Дыры по причинам: (а) гейт `flat_shape` — 5 торшеров + диван Сэндж-3 остановлены на форме
  («тонкая/длинная 0.15–0.36 при паспортных ~1.00»), в хранилище только `shape.glb`, покраски
  не было → в галерею (нужен `model.glb`) не попадают; (б) НИКОГДА не запускались: диван Бостон
  Мини `114667_13126173492540816003`, стол обеденный TC Vox `99272_12780789729488032325`,
  ковёр Ска `112923_1378688880859112776`; (в) 4 ковра — меш не нужен по канону
  (`asset-strategies.json`: ковёр = `procedural_plane`; сервер-рендер кладёт фото на пол,
  `draft_render.py:2051 _paste_rug`), но топ-спрайт для плана нужен; (г) `mesh-index.json`
  отстал — ковёр Льерс лежит на проде как GLB, но не в индексе (браузерная 3D-сцена гейтится
  индексом, `index.html:1582`; сервер-рендер — фактом файла, `draft_render.py:1791`).
- 01.09 РАЗБОР «мёртвый товар в демо» (диван Бостон Мини `114667:13126173492540816003`, жалоба
  владельца). Карточка реально 404. В БД: `status=active`, `in_stock=true`, строки в
  `product_page_status` НЕТ — не проверялся ни разу. Причина не в логике, а в возрасте механизма:
  шаг `stock_check` вошёл в `refresh_daily.sh` коммитом d01a8d6 (ADR-0141) 31.08 в 18:10 — ПОСЛЕ
  вчерашнего ночного прогона (тот финишировал 12:01), т.е. ночной конвейер с проверкой карточек
  не отработал ещё НИ РАЗУ (первый раз — 01.09 в 09:40). Для mnogomebeli он до 01.09 06:21
  (ADR-0144) и не мог сработать: обрезанная ссылка вела в раздел, раздел всегда 200 → `unknown`
  → снятие невозможно. Покрытие на 01.09 07:00: карточка проверена у 1011 из 20 553 товаров (5%),
  из 488 товаров банка — у 24; снято карточкой 384. `direct_url` у всех 1092 товаров mnogomebeli
  уже починен (полный путь `/!вариант/`) — детект сработает. Проверил заодно два других товара
  демо без меша: стол TC Vox (`backorder` = продаётся) и ковёр Ска (`InStock`) — живы, ложной
  тревоги нет.
- 01.09 ДЫРА ДИЗАЙНА (демо): `flat215_demo.drop_unavailable` мёртвую позицию СНИМАЕТ, но не
  ЗАМЕНЯЕТ — вариант остаётся без дивана до ночного `sets_incremental --heal`. Владелец 01.09
  требует именно замену («автоматом на то что в наличии и подходит по стилю»). Подборщик уже
  есть — `sets_incremental._live_candidates` (та же роль, в наличии, ±30% цены, ступень стиля);
  снимать — только если замены нет.
- 01.09 ГРАБЛЯ (повтор урока 264, попался снова): `nohup ... &` ВНУТРИ `run_in_background`
  → харнесс отрапортовал «completed, exit 0», хотя питон продолжал работать. И вторая:
  запустил дубль `stock_check --shop mnogomebeli.com`, не проверив `ps` — полный обход того же
  домена уже шёл с 05:58 из соседней сессии (два бота на один магазин = риск антибота). Правило:
  перед любым сетевым обходом магазина сначала `ps -eo pid,etime,cmd | grep stock_check`.
- 01.09 ДВЕ ДЫРЫ КОДА демо (из того же аудита, чинятся дёшево): (1) «стул 2» — второй экземпляр
  комплекта идёт с `sku=null`, а топ-спрайт (`flat215-demo/index.html:728`) и 3D-сцена (`:1580`)
  читают `it.sku.sid` НАПРЯМУЮ, минуя готовый `skuOf()` (`:802`, который берёт товар базовой
  роли) → вторая штука всегда заглушка, хотя меш первой есть; (2) 178 товаров лент замен
  (`feeds`+`sofa_feed`) идут без `sid`, а `payload()` (`:861`) шлёт `sid: it.sku.sid||null` →
  ЛЮБАЯ замена товара партнёром деградирует в кадре до серой clay-заглушки.
- 01.09 НАХОДКА (Codex + сверено мной по апстриму): в покраске Hunyuan3D-Paint 2.1 seed и CFG
  ЗАХАРДКОЖЕНЫ — `hy3dpaint/utils/multiview_utils.py`: `seed_everything(0)`,
  `generator=...manual_seed(0)`, `guidance_scale=3.0`. Наш job seed до стадии покраски НЕ
  ДОХОДИТ (передаётся только в форму — `pipeline.py:459`). Следствия: (1) перегон с другим
  seed в принципе не мог починить цвет — он меняет только форму, значит утренняя отмена
  «цвет_mismatch → на перегон» (ADR-0143) была верной и по этой причине тоже; (2) цвет —
  детерминированная функция входа; разнообразие покраски достижимо только патчем обёртки.
  Codex зарубил идею «нормализовать экспозицию входа и поделить альбедо обратно»:
  F(k·x)/k ≠ F(x) (VAE+диффузия+CFG+RealESRGAN+запекание), плюс это запрещённая владельцем
  постобработка. Замена: exposure bracketing на входе ±0.5–0.7 EV в ЛИНЕЙНОМ RGB, результат
  принимается как новая ревизия. Метрика: unlit-рендер baseColor с ракурса каталога, ошибка
  яркости в СТОПАХ (log2), ΔE00, гейт «нет коллапса в чёрное/белое»; авто-выбор обязательно
  сверить с 10–20 слепыми парами владельца. Эксперимент: сперва 4 repaint-задания на проверку
  детерминизма и локализацию сдвига (сохранять multiview до/после RealESRGAN, атлас, GLB),
  потом 12 SKU с экспозицией. Образ собирать на ЗАФИКСИРОВАННОМ commit (`HY_COMMIT=main` —
  риск нечистого сравнения). Ответ целиком — `_intake/codex-color-answer.md`.
