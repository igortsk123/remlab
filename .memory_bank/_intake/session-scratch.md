# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-07-28 — Владелец наговорил пул задач «подготовка к запуску» (главная: заглушки всего кроме калькуляторов; my.lab вместо раздела смет, сессия 1 мес; UX калькуляторов; LLM-извлечение параметров из ссылок). Пока только структурирование списка в чате, план будет отдельной командой «сделай план». Не план, не код.
- 2026-07-28 — Решения владельца к запуску: тире чистим по ВСЕМУ сайту; /calc/remont (вход Б) прячем заглушкой — не доделан; раздел /estimates прячем совсем, история расчётов в /lab; сессия 30 дн; зонт-план `plans/launch-prep.md` (П1–П6), подпланы после «деплой» зонта выполняются сразу без отдельного согласования. → ADR при завершении.
- 2026-07-28 — Риск запуска: Директ «Этап 2 ремонт» ведёт на /calc/remont (закрываем заглушкой) — в П1 дать владельцу список «кампания→куда ведёт», паузы за ним. Иконка обоев 🧻 (app/calc/page.tsx:10, sovety) → заменить.
- 2026-07-28 — Чтение ссылок: цепочка добычи HTML = прямой fetch → fallback РФ резидентский/мобильный прокси из VPN-проекта (медленный, только на неудачу; секреты в .env, в банк — указатель) → ручной ввод. Клиентское скачивание браузером отклонено (CORS). → П5 launch-prep, при реализации ADR.
- 2026-07-28 — П7 (план `launch-p7-cheaper-leads.md`): «найдём дешевле» → все 3 чипа = одна модалка с городом (справочник городов РФ, ОДИН нас. пункт — решение владельца), + IP-регион; заявки в СЛУЖЕБНЫЙ TG-бот, ответ владельца реплаем маршрутизируется в канал клиента (письмо/TG/MAX). Токены 3 ботов и почта — позже; почта palmarius.ru@yandex.ru (доступ: sup2 `_secrets/ACCESS.md`, вне git). Модуль leads в remlab уже есть — расширяем, не переписываем.
