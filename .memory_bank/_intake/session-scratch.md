# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-08-04 ДОЛГ: 9 доков с находкой CODE-DRIFT — утверждения про уехавший код не
  пересверены; нужен прогон субагента `verify`. Апгрейд кита до v1.5.0 не прогонялся.
- 2026-08-05 В ОЧЕРЕДИ по визуализации: чтение заводского чертежа из фида («Изображение с
  размерами») для точной глубины угловой секции; перевод солвера и плана на полигоны
  (сейчас Г-след включён только в геометрии сцены); зеркалирование стороны угла по комнате;
  валидатор данных перед отправкой (размер в названии против size_cm).
- Дефект правила разнообразия (`compose2.py:35 overlap_ok`): пересечение считается по ВСЕМУ
  текущему сету, поэтому как только `len(cur & keys) > lim` (4 при лимите 3), отсекается ЛЮБОЙ
  следующий кандидат — даже отсутствующий в том сете. Роли добираются по очереди, декор последний →
  сеты 17/20/29 разом потеряли торшер/люстру/подушки/плед/вазу (6-7 позиций вместо 11-13).
  Каталог ни при чём: люстр 5592, ваз 705, подушек 398, а ковров реально 17 (ADR-0066).
  Лечение: проверять только кандидатов, входящих в keys (`if cand_key not in keys: continue`).
- `identity_sheet` брал позиции по `numbering()` (только попавшие в кадр) → ваза/подушка выпадали
  из листа эталонов. Теперь лист строится от состава комплекта; внекадровые — с пометкой
  «вне кадров» и запретом рисовать в промпте (`viz_final.py`).
- Страница владельцу: `tools/scout/page_sets10.py` → /test/c10/sets10.html (аппликация + эталоны +
  карточки товаров + причины пустых ролей).
