# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-08-10 — решение владельца: судью-LLM НЕ запускать автоматически (ни крон, ни пост-приёмка) — только по явной команде владельца → core/layout+ADR-0085 дополнить
- 2026-08-10 — заявка владельца: в галерее приёмки у каждого плана площадь+Ш×Г комнаты, и в координатных JSON тоже (_room.m2/size_note) — судья-LLM должен видеть размеры (сделано в acceptance_gallery.py)
- 2026-08-10 — решение владельца (архитектура шаблонов): ЗОНЫ РАЗДЕЛЬНЫ — шаблон описывает
  только СВОЮ зону (общение отдельно, ТВ/медиа отдельно, столовая отдельно...); связи
  посадка↔экран (диагональ-дистанция, прицел ≤30°) и проходы между зонами ≥91 — отдельный
  МЕЖЗОННЫЙ слой правил (уже в движке: sofa_tv_*, passage). Не смешивать ТВ в шаблон
  общения → в solver-speed план + core/layout при финализации.
- 2026-08-10 — витрина шаблонов согласуется владельцем итеративно: чертёж = блок крупно
  (не комната), габарит блока + целевая площадь в заголовке; замечания: угловой диван
  ВСЕГДА со столиком в карточке (геометрия позволяет, гэп 38); Г-стык — ковёр не под
  торцом второго дивана (визуальное «перекрытие»). Все правки — в КОД шаблонов.
- 2026-08-10 — уроки доводки библиотеки на реальных сетах (в lessons при финализации):
  (1) снап «hard не больше числом» позволял ОБМЕН нарушений — правило «никаких новых
  видов hard»; (2) дроппер не разбирает hard-чистые блоки (protected=fixed);
  (3) порядок цепочки зон: группа → МЕДИА → столовая → хранение → чтение (обеденная
  раньше медиа занимала ТВ-стену); (4) фланг кресла у широких (285) диванов якорится
  к столику; (5) пара стульев только если w_стола ≥ 2×стул+24; (6) узкие комнаты:
  кресла столбиком; (7) честное визави несовместимо с прицелом ТВ — блок только без
  носителя; (8) cwd-грабля: solver_run гонять ИЗ tools/scout (три пустых прогона).
- 2026-08-11 — датасеты для проектирования шаблонов (решение владельца; 3D-FRONT ЗАПРЕЩЁН —
  лицензия NC, «удалить исходники» не легализует, проверено текстом соглашения Alibaba):
  скачаны ProcTHOR-10k (Apache 2.0, ~/datasets/procthor-10k) и ReplicaCAD (CC BY 4.0).
  Майнер tools/scout/mine_compositions.py: 9013 гостиных → находка: кресло ВИЗАВИ дивана
  так же часто, как фланг (612/626) → shape 'facing' добавлен. Соло-диван 52%. ReplicaCAD
  для сверки зазоров непригоден (одна квартира, нет журнального столика у дивана). Отчёт:
  /test/composition-mining/. Правило владельца: его слова = гипотезы, проверять в
  инете/книге/KB (записано в авто-память feedback-verify-owner-hypotheses).
- 2026-08-11 — ПРАВИЛО ВЛАДЕЛЬЦА для сборки сетов: «украшают ПОВЕРХНОСТИ, а не бока».
  Пруф: майнинг 9013 гостиных ProcTHOR (mine_decor.py, /test/decor-mining/) —
  поверхности несут 1.0–2.8 предмета, напольного декора 0.7–1.0 на комнату и он редко
  вплотную к мебели. Внедрено: compose2 (вторая ваза при ≥2 носителях), zones.json
  decor_policy, core/lr-checklist. Медиа-блок: один напольный акцент вместо пары.
- 2026-08-11 — каминная зона стала БЛОКОМ (build_fireplace: камин + симметричные
  стеллаж×2 → стеллаж+комод → зелень; в цепочке зон тег +fp). Источник — только
  веб-свод: в ProcTHOR каминов 0 из 9013 (данными не проверить).
- 2026-08-11 — ТВ-зона показана двумя вариантами (стенка+акценты / тумба+акцент).
