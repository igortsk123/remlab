# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-08-11 — АРХИТЕКТУРНОЕ ПРАВИЛО ВЛАДЕЛЬЦА (напомнил повторно): «если это не шаблон —
  ставить НЕЛЬЗЯ; расставляем только шаблоны; не хватает шаблонов — создаём новые».
  Внедрено: solve_zoned собирает раскладку ТОЛЬКО из зонных блоков, остаток честно
  уходит в skipped (флаг LAYOUT_ONLY_TEMPLATES=0 отключает для A/B). Поштучный beam
  больше не добирает остаток — он и портил геометрию зон.
- 2026-08-11 — коридор заполнения 30–45% (zones.json fill_policy) с пруфами веб-свода
  (правило 30%; ≤40% в малых; 40–45% «тяжело»; пристенное = половина футпринта) +
  приоритет зон посадка→медиа→камин→столовая→хранение→тихая→декор. Метрика FILL в
  выводе солвера и отчёте приёмки.
- 2026-08-11 — обогащение сетов до нижней границы (compose2): медиана заполнения
  19.8→28.6%, предметов 17→23, выше 45% — ноль. ВАЖНО: обогащение БЕЗ защиты ядра
  вытесняло ковёр (26 сцен) и столик (17) — добавлена защита: обогащение не работает,
  пока в сете нет ядра зоны (столик+ковёр); ROLE_ORDER: ядро зоны перед корпусной мебелью.
- 2026-08-11 — уроки экзаменов: (1) сравнивать раскладки по soft НЕЛЬЗЯ без учёта
  наполнения — старый путь «выигрывал», выбрасывая вдвое больше мебели (150 против 74);
  (2) фолбэк «берём лучший из двух» отвергнут владельцем — прячет баги и обедняет
  комнаты; (3) правки движка во время экзамена запрещены (урок 225).
- 2026-08-11 — ЖЁСТКОЕ ПРАВИЛО ВЛАДЕЛЬЦА (повторено дважды, фиксировать везде):
  ШАБЛОН АТОМАРЕН. «Стул не ставится» — недопустимая формулировка; не ставится ШАБЛОН,
  и тогда берётся ДРУГОЙ шаблон меньшего состава. Записано: zones.json
  template_atomicity, core/layout.md. Проверять при любой правке движка.
- 2026-08-12 — МОДЕЛЬ ДАННЫХ (решение владельца): СЕТ = банк кандидатов (мебель, которая
  МОЖЕТ подойти), РАССТАНОВКА = один вариант планировки под конкретную геометрию; из одного
  сета в будущем собираются разные расстановки разных квартир. Неиспользованные предметы
  банка — НОРМА, не «избыток/ошибка». Смета — по факту расстановки. Внедрено: zones.json
  set_vs_layout, вывод солвера UNUSED + USED n/m, приёмка пишет used_of_bank; core/lr-checklist.
