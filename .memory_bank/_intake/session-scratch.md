# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-08-13 — аудит эвристик (план rules-consistency-audit, деплой): реестр 90 правил
  собирается из кода `tools/scout/rules_registry.py` → `rules/registry.json`; найдено
  противоречие №8 (проба места проверяла голый носитель, зона ставилась с акцентом) и
  №9-регресс (условие _FOCUS_LEVEL>=1 ОТКЛЮЧАЛО пробу на последнем круге — медиа 223→118;
  починено: проба на всех кругах, медиа 190, пустых фокус-стен 62).
- 2026-08-13 — замечания владельца по планам №1-10, внедрено СИСТЕМНО: (1) схема «посадка
  без столика» УДАЛЕНА из каскада — столик+ковёр неприкосновенны; (2) пуф либо перед
  креслом, либо на ПРОТИВОПОЛОЖНОМ фланге, иначе выбывает; (3) тумбе (не стенке!) можно
  ПЕРЕД ОКНОМ с отступом от радиатора (relaxed-круг, `_window_candidates`); (4) плавающий
  диван без столовой за спинкой запрещён — пересборка из пристенных позиций (+tpl-wall).
- 2026-08-13 — правило владельца: медиа-зона должна быть В КАЖДОЙ гостиной (реально);
  сторож test_focus_wall_not_empty оставлен КРАСНЫМ (порог 29, факт 62) — не подкручивать,
  это открытый долг. Конфликт №9 для разбора: слот ковра меньше → диван не отжат → тумбе
  нет места (два канона тянут в разные стороны).
- 2026-08-13 — как работаем с планированием комнат (свод владельцу, зафиксирован в чате):
  библиотека неделимых шаблонов → слоты размеров → товар под слот (−20/+10) → заполнение
  по приоритету стадий → «лучше не поставить, чем испортить» → реестр правил с пруфами →
  экзамен 252 + сторожа.
- 2026-08-13 — ЛЕСТНИЦА ШАБЛОНОВ ПОСАДКИ внедрена (план seating-template-ladder): pick_ladder
  (порядок — zones.json seating_ladder), спуск = смена шаблона; каскад дропов УДАЛЁН;
  младшие ступени sofa_pouf/sofa_lamp/sofa_solo (+footprint_m2 обязателен — композитор падал
  без него); сторож test_seating_matches_ladder_step. Столик И ковёр — required во ВСЕХ
  13 ступенях (владелец: «все шаблоны коммуникативной зоны вкл столик и ковёр»).
- 2026-08-13 — ЛЕСТНИЦА НОСИТЕЛЕЙ (владелец: «стенка не лезет — автоматом тумба»): сет-банк
  несёт ОБОИХ (compose2 мьютекс снят, 68/126 сетов с обоими), place_media пробует стенку →
  тумбу; solver_run MUTEX удалён. Глубокие комнаты (>430 см): диван на ТВ-вилке
  (_tv_range_candidates), за спинкой столовая (владелец: «двигать коммуникативную зону ближе
  к медиа, вторая часть — столовая»).
- 2026-08-13 — УРОК: «снятая» эвристика ARMCHAIR_ON_FOCUS_WALL осталась в коде (вырезан не
  тот кусок) — кресло исчезло из ВСЕХ 252 сцен; поймано замером «сцен с креслом: 0». После
  любого снятия правила проверять grep-ом, что код удалён, и замером, что поведение вернулось.
- 2026-08-13 — порог сторожа test_focus_wall_not_empty = лучший замер (29), двигается только
  вниз; красный тест = открытый долг, подкручивать запрещено.
