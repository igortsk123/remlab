# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-08-08 — Аудит рефери v2 (полный конвейер, 49 разделов) критически оценён с нашими пруфами; принятое внесено в MASTER-zones-first.md (секция «Аудит рефери v2»); ответ рефери — /home/pakar/referee-reply-audit-v2.md. Ключевой наш пруф: в 10 фидах Гдеслона атрибута unit= НЕТ (0 вхождений), но эвристика load3 >400→/10 калечит ~46 реальных см-товаров (П-образные диваны 440–442 см mnogomebeli/divanboss → «44 см»), при этом tvoydom/gipfel шлют мм (высота 780/диаметр 490) — нужен dimension-evidence resolver per shop×role, не порог. → при консолидации: факт в core/catalog.md (дефект загрузчика) + ADR на resolver, когда пойдёт в работу.
- 2026-08-08 — «18 804 реальных сцен 3D-FRONT» — неверная формулировка (датасет синтетический, professionally designed): исправлено в сводном документе конвейера (artifact + md); проверить остальные доки банка на «реальных сцен».
- 2026-08-08 — Рефери раунд-2 (разбор нашего ответа v2): оценки подняты (solver evidence 6.8→8.0), уточнения внесены в MASTER-zones-first «Раунд 2» (manual-override не проигрывает свежему фиду; приор единиц shop×parameter; 3-й голос на 25–30% для субъективных style-полей, Krippendorff α; SKU-бенч на своих same-series hard-negatives, split по family; повороты развести: кресла — замер binding, wall-tangent — geometry correctness отдельно; constraint-contract CI; версионирование бенчей по commit; evidence ledger приоров). Side-table ловушка (его §7) проверена кодом — НЕ реализовалась (приставной = отдельная роль в солвере), нужен smoke-инвариант. Диалог с рефери сошёлся: следующий этап — 4 закрытия (resolver, human gold, real-room bench, SKU bench).
- 08.08 финал волны: приёмка с полным пакетом (рефери + обеденные фиксы + стул-у-кромки ≤45) = 238/252 (94.4%); дроп обеденной 107→71, группа реально размещается; сравнение: рефери-пакет без dining 243/252 — 5 сцен трейд за возможность обеденной. Остаток 14 провалов: SOFA_SLIVER×5, UNREACHABLE×3, DEAD_ZONE×3 (кресло/столик за диваном), DINING_IN_LIVING×1, AIM×1, TIMEOUT×1 — солверная очередь. Страница layout10 пересобрана/опубликована; xlsx rules-for-referee-2026-08-08.xlsx (13 листов, финальные цифры).
- 2026-08-08 — «Деплой подряд» MASTER-truth-first: T0–T6 выполнены, 9 коммитов до b0483c1, push+CI; ADR-0079 (резолвер размеров) и ADR-0080 (детерминизм+ТВ-канон) записаны; core/catalog, core/layout, core/regression-net, project-state, уроки 204–207 обновлены; verify-агент прогнан, его 7 находок закрыты (CI-джоб планнера, ТВ-канон в candidates/score, гейт-SLA exit, докстринги, формулировки плана). План остаётся in_progress: открыты пункты данных/владельца (разметка gold, 100–300 планировок, SKU-эмбеддер, биллинг, суммы корзины). Консолидация этой строки — при следующем memory-check (сейчас всё уже разнесено по канону, строка = след для аудита).
- 08.08: экспорт раскладок для внешнего ИИ — tools/scout/layout_export.py → https://remont-lab.online/test/layout10/layouts.json (самодостаточная _schema: см, оси, повороты, проёмы; предметы роль+товар+ШГВ+центр+rot; skipped честно). Решение: JSON первичен (числа без потерь), план-PNG — вторичная проверка для мультимодальных.
- 2026-08-08 — Решения владельца: (1) биллинг OpenAI пополнен — но проба 07:49 всё ещё 429 credit_balance_exhausted (пропагация/другая организация?); автосторож enrich_resume.sh (nohup, PID-долгожитель) сам отправит пробу 40 → полный хвост; (2) gold-разметка = предзаполнение автоматом + правки человека (реализовано: --prefill, 362/400; C-25% слепое для anchor-bias и α; расхождения human↔model = карта ошибок автомата через --calibrate). A/B оптимизатора: /test/referee/sets-ab.html (15 замен). Таблица бюджетов корзины из фактических перцентилей показана владельцу на утверждение (п.4).
- 2026-08-08 — Решения владельца (раунд): (3) реальные планировки — ПОЗЖЕ, его 3 фотки уже референс-контуры Э8 приёмки, сигнал даст сам; (4) бюджет корзины НЕ нужен — per-role тиров достаточно; (5) sets3-optimized НЕ включаем («и было неплохо») — остаётся как замер гипотезы рефери (greedy почти оптимален на top-3 альтернативах). ADR-0081: стенка = носитель ТВ (внедрено: compose2+validate+score+candidates+tv.py ниша+scene_build+тест; сет 55 пересобран). Пересборка остальных 71 сета со стенкой — следующим полным compose2-прогоном.
- 08.08 ревью раскладок рефери (партия 4): P0.2 ПОДТВЕРЖДЁН И ЧИНЕН — экспорт Г-дивана в solver_run мутировал раскладку (d→max(150) «с плечом», corner_left=True хардкод): SKU без глубины солвер решал с d=95 валидно, реконструкция scene_build с d=150 уезжала за стену (set66, −22.5 см). Фикс: экспорт solve-time параметров без мутаций + corner_left по IoU с полигоном солвера + WARN при IoU<0.98. P0.1/0.7: layout_export теперь отдаёт requested_group/actual_group/group_slots с terminal state (PLACED/DROPPED/NOT_IN_CATALOG) + recheck_hard (QA-перепроверка реконструкции канон-валидатором, только напольные роли — производные сцены типа подушек НА диване законно пересекаются в проекции). Урок: любая мутация данных ПОСЛЕ валидации = вторая правда; canonical footprint один — солверный. Десятка пересобрана/переопубликована, layouts.json с полной отчётностью. Остаток P0 рефери: UNREACHABLE-диагностика (bottleneck+blockers), dining pullout envelope; P1 очередь в плане.
