# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-08-13 — аудит эвристик (план rules-consistency-audit, деплой): реестр 90 правил
  собирается из кода `tools/scout/rules_registry.py` → `rules/registry.json`; найдено
  противоречие №8 (проба места проверяла голый носитель, зона ставилась с акцентом) и
  №9-регресс (условие _FOCUS_LEVEL>=1 ОТКЛЮЧАЛО пробу на последнем круге — медиа 223→118;
  починено: проба на всех кругах, медиа 190, пустых фокус-стен 62).
- 2026-08-13 — замечания владельца по планам №1-10, внедрено СИСТЕМНО: (1) схема «посадка
  без столика» УДАЛЕНА из каскада — столик+ковёр неприкосновенны; (2) пуф либо перед
  креслом, либо на ПРОТИВОПОЛОЖНОМ фланге, иначе выбывает; (3) тумбе (не стенке!) можно
  ПЕРЕД ОКНОМ с отступом от радиатора (relaxed-круг, `_window_candidates`); (4) плавающий
  диван без столовой за спинкой запрещён — пересборка из пристенных позиций (+tpl-wall).
- 2026-08-13 — правило владельца: медиа-зона должна быть В КАЖДОЙ гостиной (реально);
  сторож test_focus_wall_not_empty оставлен КРАСНЫМ (порог 29, факт 62) — не подкручивать,
  это открытый долг. Конфликт №9 для разбора: слот ковра меньше → диван не отжат → тумбе
  нет места (два канона тянут в разные стороны).
- 2026-08-13 — как работаем с планированием комнат (свод владельцу, зафиксирован в чате):
  библиотека неделимых шаблонов → слоты размеров → товар под слот (−20/+10) → заполнение
  по приоритету стадий → «лучше не поставить, чем испортить» → реестр правил с пруфами →
  экзамен 252 + сторожа.
- 2026-08-13 — ЛЕСТНИЦА ШАБЛОНОВ ПОСАДКИ внедрена (план seating-template-ladder): pick_ladder
  (порядок — zones.json seating_ladder), спуск = смена шаблона; каскад дропов УДАЛЁН;
  младшие ступени sofa_pouf/sofa_lamp/sofa_solo (+footprint_m2 обязателен — композитор падал
  без него); сторож test_seating_matches_ladder_step. Столик И ковёр — required во ВСЕХ
  13 ступенях (владелец: «все шаблоны коммуникативной зоны вкл столик и ковёр»).
- 2026-08-13 — ЛЕСТНИЦА НОСИТЕЛЕЙ (владелец: «стенка не лезет — автоматом тумба»): сет-банк
  несёт ОБОИХ (compose2 мьютекс снят, 68/126 сетов с обоими), place_media пробует стенку →
  тумбу; solver_run MUTEX удалён. Глубокие комнаты (>430 см): диван на ТВ-вилке
  (_tv_range_candidates), за спинкой столовая (владелец: «двигать коммуникативную зону ближе
  к медиа, вторая часть — столовая»).
- 2026-08-13 — УРОК: «снятая» эвристика ARMCHAIR_ON_FOCUS_WALL осталась в коде (вырезан не
  тот кусок) — кресло исчезло из ВСЕХ 252 сцен; поймано замером «сцен с креслом: 0». После
  любого снятия правила проверять grep-ом, что код удалён, и замером, что поведение вернулось.
- 2026-08-13 — порог сторожа test_focus_wall_not_empty = лучший замер (29), двигается только
  вниз; красный тест = открытый долг, подкручивать запрещено.
- 2026-08-13 — вердикты владельца по 4 сценам без медиа (большие/изрезанные комнаты):
  угловой диван не влезает по-человечески → ПРЯМОЙ шаблон, диван в верхнюю половину,
  медиа напротив, внизу столовая. Внедрено: лестница диванов (угловой не дал минимума →
  прямой из банка, поиск в ИСХОДНОМ составе items), composer кладёт запасной прямой при
  угловом главном, фильтр «топпер/чехол — не диван» (sane_dims).
- 2026-08-13 — корень set123-pylons: TOP_FULL_VALIDATE=24 — топ-24 позиций по эвристике
  сплошь пристенные, вилочные (tv_range, деление глубокой комнаты) до полного разбора не
  доходили. Фикс: КВОТА — вилочные разбираются все сверх топ-N; ступени дистанции вилки
  (середина и 0.92*hi — блок с ковром глубже одного дивана). set123: собрался полностью
  (+tv+fp+din+st+st2+dc).
- 2026-08-13 — SOFA_TV_DIST у СТЕНКИ шире тумбы (диагональ-метод: стенка 275 → 138-394);
  смягчение верха у validate уже было. NB: пары правил вилок сверять по обоим носителям.
- 2026-08-13 — КОСЯК «не из той папки» (4 раза за день, включая тихий показ СТАРОГО отчёта
  как нового): план cwd-free-tooling (draft) — все скрипты через HERE, run.sh, печать
  абсолютного пути отчёта+возраста, CI-сторож запуска из корня. Урок: rm -f молчит, харнесс
  сбрасывает cwd между командами — «я уже перешёл» ничего не гарантирует.
- 2026-08-13 — МИНИМУМ ВЛАДЕЛЬЦА ДОСТИГНУТ: медиа-зона во ВСЕХ 252 сценах (утром было 42 без
  медиа). Последний замок set71: SOFA_TABLE_DIST в validate требовал 32 см у Г-дивана, а
  канон «столик внутри буквы Г, шезлонг вплотную» был внедрён только в инварианте схемы —
  ДВОЙНОЕ правило разошлось (то самое, что ловит rules-consistency-audit). Фикс: у corner-
  дивана нижней границы нет. Порог сторожа MAX_EMPTY_FOCUS_SCENES=0 — держим ноль.
- 2026-08-13 — MASTER-tv-sofa-pair П0+П1 задеплоены: planner/room_map.py (карта стен/участков/
  маршрутов, двери режут участок, окна помечают) + planner/tv_sofa.py (генератор ПАР
  медиа-блок×блок посадки, WallScore из паспорта, цель 1.6×D, углы 15/30, маршрут не режет
  пару). Новые схемы паспортами: floating_pair, axis_shifted, window_back (§17),
  media_between_windows. Пары — приоритетные якоря дивана в глубоких комнатах.
  Замер: 252/252, медиа 252/252 ДЕРЖИТСЯ, смещение медиана 24→22, сторожа 9/9.
- 2026-08-13 — large-room-mode L1-L6 + small-room-mode S1-S6 задеплоены подряд (владелец:
  «делай всё пока не завершишь»): L1 room_mode триада (двойник _deep удалён); L2 пороги
  1.2/1.5×V только в large на opposite_wall; L3 инвариант group_compactness (верх=VIS_FACE,
  без второго числа); L4 машина остатка R из паспорта residual_behind_sofa (заменила бинарный
  запрет floating-без-столовой); L5 схемы хранения (периметр/shallow 30-40/zone_divider);
  L6+S6 сторожа (дистанция ≤1.5×V в large, группа ≤VIS_FACE, ядро small связное);
  S2 small-WallScore (циркуляция+25, floating штраф, margin стены +20); S3 tv_advice в
  артефакт; S4 схемы столовой (остров→у стены→круглый→складной, каскад 4→2);
  S5 вертикальный бонус хранения в подборе + media_storage_combo; уровни A/B/C/D в паспортах.
  Гейт после каждого этапа: 252/252, медиа 252/252. Конфликт-сверка — таблицей в плане.
- 2026-08-13 — slots-everywhere в работе: слоты ВСЕХ 15 напольных ролей (диван по площади
  180/210/230/260 — макс в ≤16 м² стал 198 вместо 230); ОБОБЩЁН парсер by_area_m2 в
  slot_ideal (фиксированный список ключей молча ронял слот дивана в старшую ветку — диван
  230 в 15 м²); отчёт «СЛОТ НЕ ЗАКРЫТ» в композиторе (комод 462 вне конверта — идеал 100
  узок против p25-p75 80-140? проверить). cwd-free-tooling completed (run.sh, печать пути
  отчёта, test_cwd_free 3/3).
- 2026-08-13 — УРОК: import compose2 ИСПОЛНЯЕТ весь скрипт (нет __main__-guard) — тестовый
  импорт запустил пересборку sets2. Не импортировать; тестировать парсеры копией логики или
  вынести в модуль.
