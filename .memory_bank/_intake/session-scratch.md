# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 31.08 — ДЫРА НАЛИЧИЯ (разбор по жалобе владельца на диван divanboss, 4 факта): (1) НИ ОДИН из 9 фидов Гдеслона не отдаёт атрибут `available` (87k офферов, все `None`) — сигнала «нет в наличии» из фида не существует в принципе; (2) `load3.py` его и не читает: `in_stock` захардкожен `'t'` в строке copy + `in_stock=true` в upsert (`tools/scout/load3.py:124,173,243`) — наличие = «есть в фиде и есть цена»; (3) API Гдеслона отдаёт `available="true"` даже для карточек, которые на сайте 404 (проверено вживую для `divan-atlanta-2-rogozhka-malmo-siniy`, id 7430502010629678213); у divanboss ВСЕ 1132 позиции api_offers = available true, false бывает только у divan.ru (112923); (4) единственные, кто смотрит на карточку (`health.py` — только товары из sets*, 721 из 20k; `linkcheck.py --probe` — 400 ссылок/день по кругу), перебиваются load3 наутро: 14 помеченных health мёртвыми имеют status=active и есть в свежем фиде → воскреснут в 09:40. Итог: 353 ссылки с HTTP 404 в кэше `link-alive.json` (divanboss 256 + tvoydom 97) — ВСЕ 353 в каталоге числятся in_stock=true; ни одна не дошла до порога `fails>=2` (у всех fails=1 — круг 400/день не возвращается). У divanboss 404 надёжно различает живое/мёртвое (6 ok → 200, 6 dead → 404), своего JSON-API с остатками нет (Bitrix, варианты рендерит JS). → в `core/catalog.md` + ADR (контракт наличия), фикс требует плана.
- 31.08 17:00 — состояние на момент паузы (durable уже разнесено: урок 332/333, project-state, план `mesh-dynamic-node-pool.md` в статусе draft, ждёт «деплой»): волна лечения 21/91 идёт на ОДНОЙ ноде (порт 24031), вторая (21051) прогрета и простаивает — прогон её не видит; группа mesh-run10, 8 реплик ещё в allocating. `batch_show --batch 5` (pid 819571) и `ssh_run --jobs-file mesh-reseed.json` (pid 824570) живые, лог — `_intake/batch-run5.log`.
