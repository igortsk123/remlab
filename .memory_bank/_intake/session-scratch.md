# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 26.08 ОТКРЫТО (к следующему заходу): (1) плумбинг диагностики оси — `_axis_contract.media` пишет глобал, перетираемый последним вызовом place_media, из-за чего сцены с носителем от схемы «ТВ+камин» отчитываются `centered` с чужим числом (реально ~47 см у Г-дивана, 23 сцены); (2) расхождение артефакт↔экспорт 4 см по ковру (`test_pose_hash_round_trip`, set10) — экспорт пересчитывает геометрию ковра; (3) вердикт владельца по плану №210: set105-pylons теперь собирает `two_sofas_2armchairs` вместо `sofa_2armchairs` — нужен пересмотр сторожа; (4) лесенка состава + сертификат низкого заполнения (Codex q13) — заполняемость нельзя делать целью ключа.
- 26.08 КОРЕНЬ «миниатюра не от того товара»: банк `sets3.json` хранил ЗАМОРОЖЕННЫЕ img/url. Сверка с каталогом по (mid,eid): 1490 из 3086 позиций несли фото ДРУГОГО товара той же роли (кресло Дольче с фото кресла Ретро), 202 картинки делились между товарами — при том что в БД image_url уникален у всех 32343. Вывод: медиа — производные данные, резолвим из products, а не храним.
- 26.08 Новый модуль `tools/scout/catalog_media.py` — `media(mid,eid)` + `sync_bank()` (`--apply`); банк приведён к каталогу (фото 1490, ссылок 756 исправлено, backup `sets3.json.bak-media`).
- 26.08 `tools/scout/reflink.py` — `direct()`/`SPA_CUT` вынесены из `load3.py`: у загрузчика нет `__main__`-гейта, и `from load3 import direct` запускал ВЕСЬ конвейер фидов как побочный эффект (проверено — запустился).
- 26.08 `candidates.py` теперь кладёт в индекс `img`/`url`; `sets_incremental._live_candidates` переносит их в кандидата — без этого фото-контракт отвергал любую замену и лечение только СНИМАЛО роли (сухой прогон: было «заменено 0, снято 557», стало «заменено 685, снято 28»).
- 26.08 Фото в банке после синхронизации: 891 ссылка, живых 498 (56%).
- 26.08 Демо (`/test/flat215-demo/`): нижняя панель «Проверка по стандартам» убрана; подсказка — одно короткое предложение ПОД предметом, только пока его тянут (`issues()` + `showTip()`); кашпо — крона в два яруса листьев; типографика переведена на clamp-шкалу сайта (h1 clamp(28,5vw,40) → в демо clamp(24,4.4vw,32), база 16–18px).
- 26.08 ГРАБЛЯ: `solver_run.py N --v3` БЕЗ размеров считает раскладку для условного прямоугольника из метража сета (360×415), а не для комнаты демо (439×325) → вся мебель за стеной. Проёмы передаются через env `SCENE_OPENINGS`/`SCENE_RADIATORS`. Теперь `flat215_demo.solve_layouts()` вызывает солвер сам с габаритами и проёмами из `flat215.json` и пересчитывает, если артефакт старше банка.
- 26.08 ГРАБЛЯ: `python ... | head -1` убивает процесс по SIGPIPE ДО записи артефакта — цикл «пересчитал 6 раскладок» на деле записал одну. Не пайпить долгие генераторы в head.
- 26.08 Гдеслон: API выгрузок под наш токен (`_gs_at`, XML-поиск) НЕТ — /api/exports* 404; поиск игнорирует фильтр магазина и отдаёт только sima-land (mid 105470). Ссылка выгрузки nonton (777e580d…, mid 116933, 11 595 товаров) 404 с 11.08 — нужна новая из кабинета. В `refresh_daily.sh` добавлен журнал `feed-links.json` + тревога при 2 неудачах подряд.
- 26.08 ГДЕСЛОН API (владелец дал доки): хост `www.gdeslon.ru` редиректит на api и ТЕРЯЕТ параметры — ходить сразу на `api.gdeslon.ru`; поиск `GET /api/search.xml?q=&m=<mid>&l=<=100&p=&_gs_at=<токен>` работает с фильтром магазина; список рекламодателей `GET https://www.gdeslon.ru/api/users/shops.xml?api_token=<токен>` (147 магазинов). Справочники: `api.gdeslon.ru/merchants`, `api.gdeslon.ru/gdeslon-categories.json`.
- 26.08 ФАКТ: nonton.ru (mid 116933, 11 595 товаров) ОТСУТСТВУЕТ в списке рекламодателей — программа закрыта, поэтому выгрузка 404 с 11.08. Товары сняты с продажи (status=archived), 248 позиций банка сняты (coverage_gap), 166 заменены. Новый `tools/scout/gdeslon_api.py --check/--retire/--search`; шаг `shops_check` в ежедневном цикле.
- 26.08 Контракт слота теперь требует: товар доступен в каталоге (state=available) И каталог знает хотя бы один габарит. Причина: коврик 90 см стоял в банке как 230×160 (габариты от прежнего товара слота).
- 26.08 Банк после уборки: 2675 позиций, недоступных/без размера/без фото — 0; демо-варианты выбираются по составу банка (`pick_banks`), а не по жёстким номерам сетов.
- 26.08 ЧЕРНОВОЙ РЕНДЕР. Замеры fal (прогретые): flux/schnell 5.2 с (без привязки), fast-lightning-sdxl i2i 5.6 с, sdxl-controlnet-union по глубине 5.3–8.6 с, nano-banana/edit 12.5 с. ХОЛОДНЫЙ старт controlnet/lcm — 76–174 с, поэтому нужен прогрев. Выбрана nano-banana/edit: единственная превращает наш коллаж в мебель, сохраняя места.
- 26.08 Рецепт чернового кадра (`tools/scout/draft_render.py`): clay-рендер комнаты как фон + фото каждого товара, вклеенное в МАСКУ ЭКЗЕМПЛЯРА из `compile_scene` (не проекция граней — `viz_paste` при косой камере отказывался вклеивать 11 из 12 предметов). Мягкое (плед/подушки/шторы) не вклеиваем — плоская ткань в маске читается как флаг. Итого ~15 с.
- 26.08 Сервис `tools/scout/draft_service.py` → контейнер `remlab-draft` в `remlab-net` (`/opt/remlab/draft`, Dockerfile: pillow+numpy+pydantic+shapely), Caddy-маршруты `/api/draft*` и `/api/warm*`. Лимит 60 рендеров/час, 2 одновременно, ключ FAL_KEY добавлен в `/opt/remlab/.env`. Бэкапы compose и Caddyfile сделаны.
- 26.08 `tools/scout/falmini.py` — лёгкий клиент fal (fal_key/fal_run/uri_from_image), вынесен из viz_base: тот тянет `steps` и половину витрины, контейнер не собирался.
- 26.08 Грабля: `scene_build.load_scene` падал на служебных ключах артефакта (`_templates` и т.п.) — чинено фильтром.
- 26.08 Демо перестроено по UX-референсу владельца: две вкладки («План квартиры» — схема комнат из flat215.json по площадям, «Расстановка комнаты»), состояние во `#hash`; блок над планом «Расстановка изменилась» с двумя кнопками (черновое ~15 с / реалистичное ~40 с); лента кадров с прокруткой вправо, кадры НЕ удаляются (localStorage `flat215-shots`); просмотр кадра со «Скачать» и «Отправить себе».
- 26.08 Ревизия расстановки = хеш `role|x|y|rot|mid:eid` по всем предметам — по ней страница решает, сняты ли ТЕКУЩИЕ места или нужно новое фото.
- 26.08 Реалистичный режим: два ракурса (C1 от окна, C2 от входа) параллельно, коллаж 1536 px, модель `REALISTIC_MODEL` (по умолчанию `fal-ai/nano-banana-pro/edit`, ~42 с на два кадра). Трек А плейбука (`gpt-image-2 medium`) недоступен: на ключе OpenAI insufficient_quota.
- 26.08 `/api/render` (quality=draft|realistic) и `/api/share` (страница подборки в `/opt/remlab/test/share/<id>/`, отдаётся как `/test/share/<id>/`). Deep-link ботов появляется при `SHARE_TG_BOT`/`SHARE_MAX_BOT`; токенов на проде нет — кнопки честно отключены.
- 26.08 Грабля: `pkill -f draft_service.py` убивает обёртку харнесса (exit 144) — искать PID через `ss -lptn 'sport = :PORT'` и killить по PID.
- 26.08 Реалистичный режим: ДВА ВИДА ОДНИМ ЛИСТОМ (напоминание владельца) — коллажи C1/C2 склеиваются через маджента-полосу 94 px, один вызов модели, ответ режется `_split_pair` и обрезается от остатков полосы. 30–44 с на два кадра, свет и материалы совпадают по построению. Куски публикуются в `/opt/remlab/test/share/frames/` → `/test/share/frames/<id>-C1.jpg`.
- 26.08 ЯКОРЯ на фотографиях: координаты НЕ спрашиваем у модели — берём центр маски предмета из `compile_scene` (доли кадра). В модалке — точки со ссылкой в магазин и список товаров с ценами и итогом.
- 26.08 ТВ на clay-рендере синий, и модель возвращала синюю панель — маску ТВ красим в тёмный до отправки; в промпте «dark rectangle is a switched-off flat TV».
- 26.08 Ключи OpenAI на машине: живой только `tools/scout/.env` (у него нет кредитов, insufficient_quota); ключи в mltest/sib/v0-health-card дают 401. Доступные image-модели на живом ключе: gpt-image-1, gpt-image-1-mini, gpt-image-1.5, chatgpt-image-latest (gpt-image-2 в списке НЕТ).
- 26.08 VERCEL AI GATEWAY (ключ владельца, значение — только в `_secrets/ACCESS.md` и `.env`): `https://ai-gateway.vercel.sh/v1` даёт 352 модели, среди них `openai/gpt-image-2` — ТОТ самый трек А плейбука, недоступный на нашем прямом ключе OpenAI (там нет кредитов). Проверено: `/v1/images/edits`, multipart, поле `image[]` для нескольких картинок, quality=medium, 36–50 с на лист из двух видов.
- 26.08 ВОССТАНОВЛЕН РЕЦЕПТ ТРЕКА А (ADR-0060..0063): реалистичный режим шлёт (1) наш ОБЪЁМНЫЙ clay-рендер двух видов через маджента-полосу, БЕЗ аппликаций; (2) тот же лист с номерами и подписями «N. роль W×D×H см»; (3) лист эталонов — миниатюра каждого предмета с «#N роль · название · размеры», покрывает ВСЕ позиции кадра (ADR-0063: непокрытое модель выдумывает); (4) JSON-список предметов. Промпт: «замени объём №N на товар №N, положение/размер/разворот не меняй, ракурс карточки не копируй».
- 26.08 ПОЧЕМУ БЕЗ АППЛИКАЦИЙ (напоминание владельца + ADR-0062): плоское фото врёт про РАЗВОРОТ — стул, снятый в фас, при виде сбоку читается как повёрнутый не туда, и модель считает это намеренным. Черновой режим аппликации сохраняет (там важен состав, а не разворот).
- 26.08 Недоработано (план `plans/viz-track-a-restore.md`): приёмка кадра числами перед оплатой (ADR-0061) и 3D-подстановка для предметов ≥30° (ADR-0060/0062) в новом пути не подключены.
- 26.08 Ссылки divanboss: обрезка SPA-адреса вела на КАТЕГОРИЮ («Стул НОРД» вместо «Стул АСТИ»). Проверено: у divanboss полный адрес карточки `/!вариант/` отдаёт 200 и нужный товар, у mnogomebeli и карточка, и её родитель — 404, живёт только страница серии. Правило в `reflink.direct()` стало пошаговым: режем ТОЛЬКО mnogomebeli.
- 26.08 План квартиры на вкладке снят с чертежа №180 (76,5 м²), присланного владельцем: Г-образный контур, кухня 14,0 и гостиная 13,9 в левой колонке с окнами, холл 9,5 колонкой, снизу полоса — спальня 13,2, санузлы 3,9/3,9, спальня 18,1. Сумма сходится в 76,5. Геометрия — в `flat215.json` → `plan_example` (контур, комнаты, окна, двери), рисует страница.
- 26.08 Кнопка «Отправить на рендер» вернулась в панель действий и запускает реалистичный режим; рядом «Черновое фото».
- 26.08 РАЗБОР «мебель хреново расставляется»: виновата была КАМЕРА, а не расстановка. Штатные камеры конвейера (`cameras_for`) стоят вплотную к дивану/ТВ — для витрины одного предмета; в планировщике диван вылезал за край, полкадра занимала стена. Свои `demo_cams()`: два противоположных угла, высота глаз 165 см, цель — центр комнаты на 105 см, fov 80°. Видно 7 и 5 предметов из 9 против 1.
- 26.08 ГРАБЛЯ ОСЕЙ СЦЕНЫ: точка камеры — это (x, ВЫСОТА, глубина), вверх смотрит Y. Перепутал порядок — камера уехала в стену, было видно 1 предмет из 9.
- 26.08 Черновик переведён на `openai/gpt-image-2` c quality=low: 20–27 с против 34–50 у medium, тот же трек А (не другой рецепт). Реалистичный — medium.
- 26.08 Страница «исходники» (`/test/share/src/<id>/`): ровно то, что ушло в модель — лист двух видов, лист с номерами и подписями, лист эталонов, промпт и JSON-список. Ссылка мелкая, рядом с подписью кадра.
- 26.08 UI по требованиям владельца: варианты — подразделы вверху с ценой («Вариант 1 · 103 т.р.»), максимум 4 + сохранение своего с именем (localStorage); строка комплектов и кнопки «сбросить/повернуть/подобрать/черновое/рендер» убраны; в ленте карточка «＋ посмотреть изменения на фото», у черновика — «Улучшить фото»; отправка подборки только по контакту (Telegram/MAX/СМС), ссылка наружу не показывается.
- 26.08 ПОЧЕМУ ДЕМО «НЕ ПАШЕТ»: после удаления кнопок в разметке в `init()` остались обработчики на несуществующие узлы (`rotate`, `tryon`) → `Cannot set properties of null` рвал инициализацию, страница отдавалась 200 и выглядела мёртвой. Лечение: хелпер `on(id,ev,fn)` вешает обработчик только на существующий узел + баннер ошибки в подзаголовке.
- 26.08 ИНСТРУМЕНТ ПРОВЕРКИ: `jsdom` (ставится в scratchpad) + скрипт `realtest.mjs` прогоняет ОПУБЛИКОВАННУЮ страницу как браузер — ловит ровно то, что видит владелец (клики по вкладкам/вариантам, число узлов на холсте, товаров в списке, карточек в ленте). Мой самодельный DOM-заглушечный тест этого не ловил.
- 26.08 ЯКОРЯ НА КАДРЕ: наши координаты точны для НАШЕЙ сцены, но gpt-image перерисовывает комнату и мелкие предметы (торшер, тумба, стеллаж) сдвигает — значок повисал не на том предмете. Добавлен `refine_anchors()`: зрячая `openai/gpt-4.1-mini` через тот же шлюз (4 с) возвращает координаты предметов на ИТОГОВОМ кадре, берём её точку только если она ближе 0.28 кадра к нашей. Проверено: 6 из 7 якорей уточнены, на глаз верно 5–6 из 7.
- 26.08 ГРАБЛЯ ПРАВКИ ФАЙЛОВ: `str.replace()` без проверки якоря молча ничего не делает — так функция `pinCard` «добавилась», но в файле её не было, и наведение на цифру падало с ReferenceError. Теперь в патчах обязателен `assert anchor in s`.
- 26.08 Кадры привязаны к варианту (`vid`): лента, «расстановка изменилась», выбор и отправка работают в пределах текущего варианта и не пересекаются между вариантами.
- 26.08 Черновик ускорен: один ракурс вместо двух, лист 1024, quality=low → 18–25 с (было 40+). Реалистичный остался двумя видами одним листом, medium.
- 26.08 Якоря уточняются ВСЕГДА (и в черновике): `refine_anchors` спрашивает координаты у `openai/gpt-4.1-mini` по ИТОГОВОМУ кадру и берёт их, если точка ближе 0.28 кадра к нашей; предметы, которых наша сцена в кадре не видела (частый случай — ковёр), добираются по ответу модели. Проверено: 7 из 7 уточнены, глазами верно 6 из 7 (торшер — тонкий предмет, промахивается).
- 26.08 Подборка = КОЛЛЕКЦИЯ: страница `/test/share/<id>/` показывает под каждым кадром товары с фото, ценой, магазином, ссылкой и суммой; страница демо шлёт товары вместе с кадром.
- 26.08 КОРЕНЬ «синей панели на стене»: в clay-палитре окно = (108,166,208), и я в промпте назвал этот синий прямоугольник «выключенным телевизором» — модель честно рисовала синюю панель. Теперь в промпте: синий прямоугольник = ОКНО (стекло, рама, дневной свет за ним), коричневый = дверь. Проверено: в кадре появилось настоящее окно с видом.
- 26.08 ЯКОРЯ v3: зрячая модель просит РАМКИ предметов, затем вырезки этих рамок проверяются вторым запросом («что на вырезке?»); неподтверждённые значки не показываем вовсе. На проверке C1 4 значка, все верные; скрыто 3.
- 26.08 Черновик снова с ДВУМЯ видами одним листом (владелец: «где два вида?») — 27–33 с; сам вызов gpt-image на минимальном качестве занимает ~20 с, остальное наше.
- 26.08 Ссылка «Исходники запроса» теперь и внутри просмотра кадра, отдельной кнопкой с пояснением.
- 26.08 КОРЕНЬ «ковёр на стене, а на полу чужой серый»: лист эталонов даёт модели фото ковра — плоский прямоугольник с рисунком, и она вешает его на стену как картину, а на пол выдумывает свой. Лечение: в подписи эталона у ковра «— НА ПОЛ (вид сверху)», в промпте — «ковёр снят сверху, класть плашмя на пол с этим рисунком; никогда не вешать ковёр/текстиль/эталон на стену; не выдумывать второй ковёр; не добавлять картины и декор, которых нет в списке». Проверено на минимализме: ковёр Руффи Вью лёг на пол с верным рисунком, картина со стены исчезла.
- 26.08 РАСКЛАДКА ВРЕМЕНИ ЧЕРНОВИКА (два вида одним листом, прод): сцена и коллажи 2.7 с, вызов gpt-image 21.8 с, зрячая проверка значков 6.1 с, публикация 0.1 → 31 с. То есть 70% времени — сам вызов модели; наша часть 3 с. Раскладка возвращается в ответе (`timing`) и показывается в просмотре кадра.
- 27.08 ЗАМЕР КАРТИНОЧНЫХ МОДЕЛЕЙ на одном листе (два вида, size 1024x1536, quality=low, через шлюз Vercel): gpt-image-1-mini 13.7 с, gpt-image-1 15.8 с, gpt-image-2 19.2 с. Черновик переведён на `gpt-image-1-mini` (владелец: «самую простую модель, может не полностью все детали»): прод-замер 22.3 с всего — сцена 2.9, модель 12.5, проверка значков 6.1, публикация 0.2. Реалистичный режим остаётся на gpt-image-2 medium.
- 27.08 РАЗБОР CODEX по якорям (`_intake/codex-prompts/q15-anchors.answer.md`): нашёл fail-open ветки (при сбое проверки показывались сырые координаты), сравнение подстрокой («стол»≈«столик»), проверку класса вместо SKU, неиспользуемый параметр `marks`, центр масс вместо вписанной окружности. Всё, кроме `marks`, исправлено → ADR-0123. Рекомендации на будущее: детерминированная вставка по маске (ADR-0060..0063), локальный детектор+SAM, сравнение эмбеддингом, метрика precision показанных якорей (нужно ~300 без ошибок для оценки 1%).
- 27.08 Codex: `gpt-image-1-mini` помечен OpenAI как deprecated и по нашему же плейбуку сохраняет 6/10 предметов — как постоянный дефолт черновика не годится; кандидат на A/B — `google/gemini-3.1-flash-lite-image` (sub-2s, ~$0.034/1K, есть в шлюзе, но вызывается не через /images/edits, а мультимодальным API).
- 27.08 БАГ «фото не видно в ленте после генерации»: правка, добавлявшая кадру привязку к варианту (`vid`), ссылку на исходники и тайминг, не применилась — якорь текста разошёлся, а `str.replace` промолчал. Кадр сохранялся без `vid` и не проходил строгий фильтр ленты; после перезагрузки миграция проставляла `vid`, и кадры «появлялись». Урок тот же: в патчах обязателен `assert anchor in s`, а проверять — прогоном страницы в jsdom (`dbg.mjs` печатает shots/vid/curVid/карточки).
- 27.08 Быстрая mini теряет маджента-полосу, разрез уходил наугад и сверху второго кадра оставалась полоска первого. Для моделей с `mini` в имени два ракурса считаются ДВУМЯ параллельными запросами (`SPLIT_VIEWS=1`): шва нет, время то же (26 с), промпт строится под один вид.
- 27.08 Значки с фото УБРАНЫ (решение владельца): товары показываются лентой карточек под кадром. Вместе со значками ушла и зрячая проверка координат — самый дорогой шаг после генерации (6–17 с). Вернуть: `ANCHORS=1`. ADR-0123 остаётся в силе как правило «показывать только подтверждённое», если значки вернутся.
- 27.08 Черновик снова на `openai/gpt-image-2`, quality=low (владелец: «gpt-image-2 ставь, но на минималках»): прод-замер 31 с, из них генерация 27 с, наша часть 3 с. mini давала 20 с, но теряла товары и маджента-полосу.
- 27.08 ПОДПИСИ КВАДРАТИКАМИ: в контейнере сервиса не было НИ ОДНОГО ttf, PIL брал встроенный битмап без кириллицы, и в модель уходили подписи из «□□□» — модель не понимала, что за предмет. Лечение: `fonts-dejavu-core` в образе + `_font()` ищет шрифт с проверкой глифа «А» + `_label()` транслитерирует, если кириллицы нет вовсе.
- 27.08 На служебном листе подписаны ОКНО и ДВЕРЬ (по цвету clay-палитры), в модель уходит ТОЛЬКО размеченный лист (чистый убран — владелец: «без номеров не вижу смысла»), в промпт добавлен реальный размер окна (150 см, подоконник 85, высота 150), чтобы модель не рисовала остекление во всю стену. Окно в `flat215.json` приведено к типовому 150×150, подоконник 85 (было 180 см).
- 27.08 Телевизор рисует модель по тумбе: в промпт уходит «над предметом #N (тв-тумба) повесь плоский ТВ шириной ~0.85 ширины тумбы, центр экрана 110–120 см от пола, экран выключен». Отдельного объекта «тв» в расстановке нет.
- 27.08 Модель роняла предметы на одном из видов (диван пропал в C2) — добавлено требование «в кадре должны быть ВСЕ пронумерованные предметы: #1…#N».
- 27.08 Камеры демо стояли в двух углах ОДНОЙ стены — «второй вид» получался соседним ракурсом. Теперь C1 и C2 в противоположных углах смотрят друг на друга через центр: на одном виде диван с ковром и окном, на другом ТВ-зона с дверью и обеденной группой.
- 27.08 «фото нет» на листе эталонов: у экземпляра пары («стул 2») своего изображения в запросе не было — теперь наследует фото базовой роли.
- 27.08 Подписи на листе эталонов налезали друг на друга — обрезаются по ширине ячейки.
