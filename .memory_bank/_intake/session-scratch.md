# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 01.09 план `photo-improve-from-mesh` В РАБОТЕ (гейт памяти застал на середине). Сделано:
  `scene_mesh.raster_mesh` получил `inst_buf`/`inst_id` — карту предметов кадра (пишется в те же
  пиксели, что цвет; по умолчанию None, поведение не меняется). Дальше по плану: маска из этой
  карты, режим `improve`, промпт без «replace every grey volume», композит мебели поверх ответа,
  гейт полосы, кнопка.
- 01.09 ВЛАДЕЛЕЦ ОТКЛОНИЛ рекомендацию Codex «два отдельных вызова вместо склеенного листа», и
  довод сильнее: два вызова = две НЕЗАВИСИМЫЕ генерации ремонта, у них разойдутся цвет стен, пол
  и свет — два вида одной комнаты станут разными квартирами. Один лист даёт согласованность по
  построению, «одинаковый текстовый блок стиля» — нет. Плюс требование: просить МАКСИМАЛЬНОЕ
  разрешение (сейчас вертикальный лист 1024×1536 = ~720 px на вид против исходных 1344×896).
  Из критики принято одно: `_split_pair` при пропавшей полосе МОЛЧА делит пополам — порча ответа
  обязана быть ошибкой.
- 01.09 ПОДТВЕРЖДЕНО по коду (важно для любых будущих правок кадра): масок мебели, нарисованной
  мешем, НЕ СУЩЕСТВОВАЛО — `compile_scene` строит маски только для clay-заглушек
  (`draft_render.py:2146`), а `raster_mesh` вёл лишь цвет и z-буфер. Любое «сравним маски до и
  после» до этой правки было пустым обещанием.

