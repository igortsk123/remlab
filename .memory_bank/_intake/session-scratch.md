# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 19.08 НОРМА (Codex + источники, `codex-prompts/q11-window-chair-orientation.answer.md`): кресло для ЧТЕНИЯ у окна ставят спиной или боком к окну (эксперимент NTNU, 29 участников), лицом в окно — сценарий «вид/кофе» (яркий проём в поле зрения = дискомфортный контраст, IES). Нормативного угла диагонали НЕТ: ±30° — только hypothesis. Наша осевая постановка верна, менять базу нельзя.
- 19.08 ДЕФЕКТЫ у оконного уголка (найдены Codex, НЕ исправлены): setback считается ДО поворота блока; порог радиатора задублирован (`sofa_to_radiator_wall` vs `radiator.hard_min_clearance_cm`); зазор торшера в коде 12 см против `from_armrest_cm` 15–30; кресло центрируется по радиатору и перекрывает половину фронта (лучше позиции сбоку).
- 19.08 витрина: `storage_shallow` было 80 см до спинки (наша же «сирота» 31–90) → 102 см; в `square` вернул ковёр 350×250 (прятал предмет ради обхода мягкого замечания — так нельзя); подпись ориентации теперь всегда есть у дивана/кресла («→ в сторону ТВ (43°)» для осевой позы).
- 19.08 РЕШЕНИЕ ВЛАДЕЛЬЦА (стратегия): каноны — «ситуационные» (функция × якорь × ситуация: у окна, в углу, в эркере…), плюс статистические частоты паттернов как приоры. Прописать по всей памяти и учесть архитектурно. Запрос в Codex: `codex-prompts/q12-situational-canon.md` (ответ ждём) → далее ADR + сводки.
- 19.08 ОТКРЫТО: 7 схем sleeping — владелец просит реализовать (corner_tower, window_seat×2 — код; media_builtin, dining_foldable, storage_zone_divider — данные каталога; tv_over_fireplace — нужна вертикальная модель и нормы тепловых зазоров).
- 19.08 Q12 «деплой» (в работе): Q12-1 (типизированная возможность: пороги в rules, стабильные ID, исход по якорю), Q12-2 (приор — локальное сравнение вместо суммы рангов; ADR-0112), Q12-4 (разбужены corner_tower + window_seat bench_under_window/bay_bench, fail-closed по радиатору и подоконнику, тег цепочки `+ws`). Идёт экзамен `tools/scout/logs/exam-q12.log`. Осталось: Q12-3 (гейт семантики якоря), Q12-5..7 — данные каталога и вертикальная инженерия.
- 19.08 РЕШЕНИЕ ВЛАДЕЛЬЦА: пока каноны не отработаны — планы НЕ пересобирать (экзамен/галерея планов/экспорт). Прерванный экзамен остановлен, частичный отчёт убран в logs/; тесты уровня планов (pose-контракт, focus_wall, dining watchdog) теперь СКИПАЮТСЯ без отчёта, а не падают.
- 19.08 паспорта приведены к ADR-0112: у всех 50 схем есть anchor+form (реестр типов `templates.anchor_registry`: window|bay|corner|wall_segment|opening|free_region|zone_boundary|object|runtime — расширен по разбору Codex), у всех есть when/why/status. Заведены недостающие исполняемые формы: media_parallel, media_half, media_bridge, L_right, L_left. Гейт — `tests/test_passport_situational.py` (в т.ч. «формы из zones.json обязаны быть в паспорте»).
- 19.08 НОРМА (Codex, источники): `tandem_r/l` — НЕ канон узкой комнаты, а ситуационный fallback «пара кресел плечом к плечу на одном фланге»; законен только после сертификата недостижимости pair_sides/u/facing и при парных креслах, свободный фланг обязан быть маршрутом. Перекос 23 vs 5 может быть right-first bias каскада. `square` — каноническая трёхсторонняя группа (H&G), но реализация односторонняя (нет зеркала) и центр кресел считается по глубине первого.
- 19.08 дубли чисел устранены (одно число — один источник): заход ковра 25 (occupancy.dynamic.rug_rules) вместо 15 в templates; зазор торшера 15 из правил вместо «+12» в коде; порог радиатора помечен зеркалом. Сторож — `tests/test_rules_no_dup.py`.
- 19.08 правило RUG_ORIENTATION уточнено: у ТРЁХСТОРОННЕЙ группы (посадочные смотрят в ≥3 направления) ковёр кладётся по внутреннему контуру зоны — правило оси не применяется.
- 19.08 АУДИТ КАНОНОВ с Codex (`codex-prompts/q12-canon-audit.answer.md`). Найдено и исправлено: галерея писала ДВЕ карточки под одним id `seating.default` (разные группы затирали друг друга) → id карточки = (группа, форма); три схемы исполнялись БЕЗ паспорта — `sofa_facing_sofa` (два дивана визави), `console_behind_sofa`, `media_wall` (стенка) → заведены; `dining_edge_nook` статус «unwired» устарел; `fireplace_solo.solo` помечена алиасом (канон один). Частоты окна обновлены под уточнённые числа владельца (кресло 28 / скамья 19 / растения 17 / пусто 17 / стол 10 / диван 9), исход «скамья» отвязан от edge_nook.
- 19.08 ПРЕДЛОЖЕНИЯ Codex (владельцу на решение, НЕ сделано): объединить зеркала под один канон (tandem_r/l → side_pair, L_left/right → two_sofa_l_joint, square/square_r → three_sided_conversation, media_storage_combo+installation, media_mirror внутрь media_centered); переименовать floating_pair → floating_sofa_opposite_media, facing → armchair_pair_opposite_sofa, bulky → deep_armchairs_opposite, quiet_chat → paired_conversation; перевести в fallback-с-сертификатом tandem/bulky/bridge/media_corner/media_at_jamb. Новые каноны по приоритету: reading.window_pair и bay_pair (пара кресел), decor.window_plant, reading.wall_vignette (скрытый fallback без паспорта), dining.window_table (shadow), seating.open_center; НЕ заводить: одиночный торшер как зону, entry-схемы ради матрицы, встроенную скамью из прямой банкетки.
- 19.08 (владелец «согласен делай») — ВЫПОЛНЕНО по аудиту канонов: зеркала слиты в один канон (side_pair ← tandem_r/l, two_sofa_l_joint ← default/L_right, three_sided_conversation ← square/square_r), медиа-слияния (freestanding_media_storage_run ← media_installation+media_storage_combo; media_mirror внутрь media_centered); переименования (floating_pair → floating_sofa_opposite_media, facing → armchair_pair_opposite_sofa, bulky → deep_armchairs_opposite, quiet_chat → paired_conversation) — в паспорте, коду оставлены `runtime_variants`; общий `seating.default` РАСЩЕПЛЁН по группам на 7 канонов (armchair_pair_vis_a_vis, sofa_single_flank, sofa_pair_sides, sectional_solo, sofa_pouf_group, sofa_lamp_group, sofa_solo_group); статус `situational_fallback` + `requires_certificate` у tandem/bulky/bridge/media_corner/media_at_jamb.
- 19.08 НОВЫЕ КАНОНЫ (код+паспорт+карточка): reading.window_pair и bay_pair (`build_reading_pair`, пара кресел с общей поверхностью; у окна пробуется ПЕРЕД одиночным, при неудаче честный откат), decor.window_plant (кандидаты сбоку от проёма, мимо радиатора), reading.wall_vignette (легализован скрытый fallback «кресло у стены полным комплектом»).
- 19.08 гейты канонов: единица учёта — ПАРА (группа, форма) `test_group_shape_pairs_are_covered`; формы из zones.json покрываются id схемы ИЛИ её runtime_variants; галерея больше не пишет две карточки в один файл (id = группа.форма). Библиотека — 64 карточки, покрытие паспорта полное.
- 20.08 НОРМЫ из интернета (проверка по замечаниям владельца): приставной столик у кресла — зазор 5–10 см (2–4″), поверхность на уровне подлокотника ±5 см, у ПЕРЕДНЕЙ половины подлокотника (Kouboo/Wayfair/Eureka); торшер для чтения — за плечом, 46–90 см от центра посадки (BenQ 18–24″, другие 2–3 фута), низ абажура 107–120 см (уровень глаз сидящего). Записано в occupancy.dynamic.extras.{side_table,floor_lamp} с провенансом; геометрия build_reading приведена к нормам.
- 20.08 ТВ+камин (замечание «огонь отвлекает»): источники подтверждают — опасна композиция КАМИН НАПРОТИВ ТВ (отражение пламени в экране), а side-by-side на одной стене как раз рекомендуемая. Заведено мягкое правило `FIRE_REFLECTION_ON_TV` (S2, validate.check_fire_reflection_on_tv) + провенанс в паспорте media.fireplace_side_by_side.
- 20.08 у окна: отступ спинки поднят с 8 см до нижней границы «воздуха» 15 см (наша же back_gap_policy) — 8 см читались как «вплотную», штора не помещалась; блок ЦЕНТРИРУЕТСЯ по проёму, а не якорь (пара кресел уезжала вбок); то же для эркера (block_ref в _bay_candidates).
- 20.08 подписи: у дивана/кресла/банкетки появилось отношение к ЯКОРЮ — «лицом в комнату (спинкой к окну)» / «лицом к окну» / «боком к окну», считается по нормали оконной стены (по вектору на центр проёма смещённая вдоль стены пара подписывалась «боком»).
- 20.08 НОЧНОЙ CRON сам прогнал экзамен 03:28 (272/272 ok, TIMEOUT 0) — но тест `test_route_is_walkable` упал: set5-trapezoid маршрут 60 см при полу 70/75. Разбор требует одиночного прогона сцены — ОТЛОЖЕНО до снятия заморозки планов владельцем.
