# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->

## 2026-08-06 (вечер) — деплой мастер-плана MASTER-pipeline-hardening, волны А0–А7 подряд (решение владельца: «все планы до конца без спроса»)
- А1 выполнена+испытана: refresh_daily теперь vision+автозабор (enrich_wait v2: успех=строки в БД, id-файл архивируется; терминальные статусы=алярм); счётчики потерь в fetch; дельта text/geometry сбрасывает enrichment_version→todo() подхватывает; phash сканирует imgcache (17%→99.7%); alert.sh (TG из tools/scout/.env.alert ИЛИ маркер refresh-alert.log — TG-токена у remlab нет, решение владельца); предохранитель похудевшего фида (<70% от вчера, обход FORCE_SHRINK=1); hard-fail при нечитаемой карте категорий; KIDS в daily; style_score.py убран из крона. Алерт доказан живым сбоем (мой баг парсинга psql — исправлен).
- А2: кашпо ALLOW_FIRST (636 товаров, было 0), обогащены 636/636 кач.0.81 ЧЕРЕЗ НОВЫЙ АВТОКОНТУР (submit→wait→fetch→индексы→судья — весь цикл сам, с деньгами ~$1); ковров в фидах нет (26) — источник за владельцем; /None/ URL отсечены; in_stock в pool/candidates; category_id при upsert.
- А3: дефолт движка beam (solver_run.py); SOFT-метрика: солвер печатает score-термы+soft-нарушения, solver_check собирает + ранжирует «валидные но глупые» (DUMB_T=12, калибровать вердиктами владельца).
- ВАЖНО (урок): «beam 122/126» было цифрой от 02.08 на СТАРЫХ данных (каталог 87k); после очистки базы и замены пуфов honest baseline оказался ~79/126 — массовый провал «столик↔пуф ≤60». Корень: пороги пуфа (столик↔пуф 60, диван↔пуф 180) жили ТОЛЬКО в scout hard_checks, ядро их не знало (у ядра было ≤80) и кандидаты пуфа считали отступ по max-габариту (реальный гэп 61–80). Исправлено в одном источнике: occupancy.json pouf_table_max=60/sofa_pouf_max=180 + кандидаты по фактической полуширине. Сет 82: 2 нарушения → 0.
- А4: viz_qa.py — пост-QA финала (ΔRGB по instances-боксам + VLM gpt-5-mini, ~$0.006/сцену), вшит в run_pair, гейт exit 3; на сцене 21 поймал реальный брак у ОБЕИХ моделей (лишний стеллаж, перекрашенный пуф). 3 ретрая edit_gpt_raw (оба вызова) и fal_run (транзиенты, 4xx/FAILED не ретраим). shell-база ADR-0063 дефолт (ensure_shell с фолбэком на clay; возражение «мебель повисает» было про оболочки БЕЗ depth-условия). Приёмка блокирует batch_collage (pipefail). Ключи fal/OpenAI: tools/scout/.env (вне git) первым во всех цепочках (golden_label, viz_objects, viz_base, pipeline2, viz_frame, llm_planner).
- А5: SOFA_SLIVER hard 20–76 за спинкой дивана (+тест, 61 passed; фикстуры 2 шт исправлены — отплыв квантуется 0|76); sofa_to_wall_passage 70→76; дверь/окно вариативны от номера сета детерминированно (FIXED_OPENINGS=1 — отладка); ковёр привязан к дивану (передние ножки на ковре, край -25см от фронта) в scene_build, не к столику. Э6 partия — после финального перегона.
- А6: меш-фолбэк mesh_make(model=) Hunyuan3D (fal-ai/hunyuan3d/v2, {'input_image_url', 'textured_mesh'}) при провале самопроверки Trellis; доверенный фолбэк копируется в канон-путь. Живой тест: столик сцены 14/29 закрыт мешом (хвост 29: 3→2 брака; остаток — рендер меньше target_box, follow-up). viz_ab.py: A/B финала через fal (fal-ai/nano-banana-pro/edit СУЩЕСТВУЕТ, 50 с/лист, split по полосе ок); замер сцены 21: NB Pro чуть лучше по цвету (ΔRGB 1 роль против 2), баги состава те же — нужен батч на чистых коллажах. Промпт-сжатие отложено осознанно (каждый блок — документированное решение; резать без визуального A/B нельзя).
- А7: enrich_judge.py в enrich_wait: судья-сэмпл 30 terra, дрифт-бейзлайн enrich-drift.jsonl, копилка golden-candidates.jsonl; живой прогон на кашпо: роль 96.7%, подтип 90%, стиль 100%.
- Урок: утренний текстовый батч всего пула (4 части, ~$3, отправлен кроном 09:40 ДО фиксов) осознанно НЕ забираем: payload уже vision-обогащён, текстовый стиль хуже; деньги списаны — в утиль.
- Урок (харнесс): pkill -f ловит обёртку харнесса (exit 144) — уже было в авто-памяти, наступил снова; killить по PID.
