# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-07-31: Ресёрч дизайн-систем (awesome-design-systems): рекомендация топ-3 = shadcn/ui (фаворит), Mantine, Ant Design. Контекст: сейчас UI на ручном CSS (~99 токенов в globals.css, 17 компонентов), Tailwind нет. Решение владельцем НЕ принято — только рекомендация.
- 2026-07-31: Проверка всех ~160 систем из awesome-design-systems по GitHub API: топ-3 подтверждён данными (shadcn/ui 120k★ MIT, antd 98.9k★ MIT v6 React>=18, Mantine 31.5k★ MIT v9). Shopify Polaris React — архивирован; Kontur retail-ui — 270★, B2B-нишевый; корпоративные DS (Carbon/Fluent/Paste и пр.) отсеяны: код открыт, но дизайн под чужой бренд.
- 2026-07-31: Глубокий разбор Untitled UI React (untitleduico/react): MIT, живой (пуш 2026-07-22), 1.85k★, копипаст-модель как shadcn, но React Aria + Tailwind v4; бесплатно есть всё ядро (формы/таблицы/модалки/date-picker/charts); PRO $349 once (5k+ секций, 250+ страниц, Figma-sync). Входит в топ-3 вместо Mantine как «красивый shadcn» с мостом в Figma; риск — комьюнити в 65 раз меньше shadcn.
- 2026-07-31: Владелец выбрал Untitled UI React. Создан план plans/uui-migration.md (draft, подпланы U0–U4). Палитра-гибрид зафиксирована в плане: brand = кастом-терракота от #b06a4a (пресетов терракоты у UUI нет, orange отвергнут — ломает бренд под живой рекламой), нейтраль = stone, статусные = пресеты, шрифт Inter self-host, тёмная тема не включается, шалфей упраздняется. Клон репо изучен: theme.css 834 строки, 15 шкал, brand-оверрайд = 11 переменных, free-компонентов хватает, PRO не нужен.
