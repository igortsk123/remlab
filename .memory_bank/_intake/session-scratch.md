# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 2026-09-05 — свод выполнен (аудит памяти, план memory-bank-audit-2026-09 → completed): 27 записей блокнота
  разнесены (ADR-0187…0195, уроки 414–418, mesh-pool-ops, project-state), блокнот чист.
- 2026-09-05 09:30 UTC — ГОТОВО К ADR (следующий свободный номер, том decisions/adr-0151-0200.md).
  **Уборка транзита — упреждающая, на жёлтом пороге.** Решение: супервизор `ssh_run` при
  `dir_gb >= MESH_SINK_YELLOW_GB` (6) запускает `drain --keep` + `receiver_purge --apply` фоном,
  не останавливая раздачу; дроссель 600 с и флаг busy против наложений (`ssh_run.sink_relief`,
  коммит 2581d87, тест `case_sink_relief_before_red`).
  Почему: замер 05.09 за час 08:15–09:15 — ОДНА пауза 17.6 мин, все воркеры встали в 08:55 и
  пошли в 09:12 синхронно (цикл `while not _SINK['ok']`, защита ADR-0175). Приёмник рос +2.5 ГБ/ч
  и пересекал красную черту 7 ГБ около 08:56. Цена паузы — ~7 нодо-часов из 16, около 30 %
  стоимости мешей за час. Медиана генерации не менялась (199→205 с), доля прогрева даже упала
  (10→7 %): машины не тормозили, а СТОЯЛИ.
  Суть: защита была верной, реактивной была УБОРКА — она шла по таймеру разбора и по факту
  отказа, то есть уже после остановки пула. При 100+ мешах в час приёмник наполняется быстрее
  таймера. Альтернативы: поднять предел приёмника (не лечит, только отодвигает); чистить чаще по
  таймеру (не привязано к реальному темпу поступления).
  Влияет на: `ssh_run.py`, `tests_pool.py`, ADR-0175/0178.
- 2026-09-05 10:00 UTC — ДЕФЕКТ: УТЕЧКА ОСИРОТЕВШИХ `topview_render.py`. Шаг топ-вью в
  `batch_show.post_steps` завёрнут в `sh(...)` с таймаутом 2700 с. По таймауту убивается ОБЁРТКА
  (`/bin/sh -c for i in $(seq 0 5); ...`), а python-потомок остаётся жить с `ppid=1` — навсегда.
  Пост-разбор идёт каждые `MESH_POST_EVERY_S=900`, поэтому сироты копятся: на 05.09 в 10:00
  их было **20 штук, старейшая работала 21 ч 44 мин**. Последствия: съеденная память дев-машины
  (trimesh её не отдаёт, урок 391) и блокировка перезапуска — новый конвейер честно ждёт сирот
  (`wait_orphans`, до 1500 с), а ноды всё это время оплачиваются.
  Снял 19 процессов по PID. → ЧИНИТЬ: убивать группу процессов по таймауту (`start_new_session` +
  `os.killpg`), а не только обёртку. Плюс уже закоммиченный `TOPVIEW_DEADLINE_S` 2400→1800
  вступит в силу только с этого перезапуска.
- 2026-09-05 10:00 UTC — ПОТОЛОК БЫЛ В РАЗМЕРЕ ПАЧКИ. Замер 08:45–09:45: 27 нод сделали 107 мешей,
  полезных 6.5 нодо-часа из 22.1 оплаченных (занятость 29 %); простой МЕЖДУ заданиями на одной
  ноде — 12.8 нодо-часа, медиана паузы 59 с, но половина пауз >5 мин. Мешей ~110/час независимо
  от числа машин (12 → 16 → 24 нод): упирались не в мощность, а в то, что пачка из 200 заданий
  (~11 нодо-часов работы) кончается быстрее, чем конвейер готовит следующую. Решение владельца
  «идти потоком»: конвейер перезапущен с `--batch 2000` (при `MESH_MAX_JOBS=12000`), очередь
  11 704, курсор 1080. Перезапуск сделан по PID с сохранением групп — замер батча не прерван.- 2026-09-05 11:16 UTC — ИТОГ ЗАМЕРА БАТЧА В ВЫХОДНОЙ (суббота, 8 обходов 07:45–11:16).
  Батч в субботу ДЕШЕВЛЕ low примерно на треть — во всех окнах подряд:
  до окна (07:41–09:00): batch 24 меша / 2.3 опл.ч = 10.6 меш/нодо-час, **0.74 ₽/меш**;
  low-4 1.24, low-5 1.13.
  в окне (09:00–10:35): batch 49 / 8.3 = 5.9, **1.33 ₽**; low-4 2.47, low-5 2.48.
  всё утро: batch 92 / 14.5 = 6.3, **1.23 ₽**; low 2.04–2.15. Всего 400 мешей / 66.1 опл. нодо-часа.
  Удержание машин: батч получил 20/20 реплик в субботу утром, но в работу выводил стабильно
  меньше остальных (4–6 из 20 против 7–10 у low) — машины даёт охотно, доводит до работы хуже.
  ГЛАВНЫЙ ВЫВОД: меш/нодо-час у всех трёх групп в каждом окне ПРАКТИЧЕСКИ ОДИНАКОВ (10.0/10.6/11.0,
  затем 5.0/5.0/5.9) — тариф не меняет темп машины, разница в ₽/меш берётся целиком из ставки
  ($0.09/ч batch против $0.143/ч low, −37 %). Значит вопрос по батчу не «быстрее ли», а «удержит ли».
  Расходится с будним ADR-0177 (batch 1.86 против low 1.61) — в выходной картина обратная.
  ОГОВОРКА: окно в окне загрязнено — 09:00–10:35 включает деградацию из-за размера пачки и
  11-минутную остановку по красному приёмнику. Сравнивать тарифы между собой в одном окне честно,
  сравнивать окна между собой — нет.
