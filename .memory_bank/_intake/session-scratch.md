# Session scratch — блокнот захвата на ходу (append-only)

> **Зачем.** Захватывать durable-факт/решение в МОМЕНТ, когда он принят (там он ярче всего),
> а не раскапывать весь диалог на выдохе в конце сессии. `/memory-check` (Этап 1) консолидирует
> эти строки в `.memory_bank/` и очищает блокнот. Это амортизирует дорогой захват и лечит
> «согласованное замерзание» (частый провал: работал → не записал).

> **Как писать.** Принял ADR-значимое/durable (решение, миграция, контракт/API, интеграция,
> смена этапа, неочевидная грабля) → СРАЗУ допиши 1–2 строки СЮДА, не прерывая работу.
> Формат: `- ГГГГ-ММ-ДД — <что> — <почему/куда в память>`. Это НЕ канон, а сырьё для консолидации.

> **Что НЕ писать.** То, что и так в коде/git (структура, разовые правки), и то, что важно
> только для этого диалога. Значения секретов — НИКОГДА (только в `_secrets/`, вне git).

<!-- SCRATCH START — /memory-check переносит обработанное в банк и усекает до этой метки -->
- 01.09 ЦЕНА МЕША — корень найден: конвейер шёл с `--batch 5` при 10 нодах («заданий: 5 на
  10 нод» в логе) → занято 2 ноды из 10, КПД оплаченного времени 22%, цена $0.031/меш
  ($7.04 за 226 мешей по балансу владельца $11.99→$4.95; внутренний журнал считал $0.0075 —
  он видит только GPU-секунды заданий, а Salad берёт за всё время running). Формула Codex:
  цена ≈ $0.0075 / загрузка. Внедрено: перезапуск с `--batch 30` + GALLERY_OFF=1 (05:33).
  Дальше по вердикту Codex (`_intake/codex-idle-answer.md`): после стабильной волны batch=50
  (4-6 заданий на ноду), не звать ask_gpu в каждой пачке, не ждать RETRY_GRACE 300с при живой
  ноде, гасить группу сразу после последнего GPU-задания (а не после всей постобработки),
  уменьшать replicas на хвосте. Метрика ночи: sum(job_gpu_s)/sum(instance_running_s).
- 01.09 очередь мешей — ТОЧНЫЕ ЦИФРЫ (владелец поправил мою «11 631»): `mesh-pilot-sample.json`
  = **1465 заданий на 1461 товар** (126 сетов, 481 sku в них + 78 добавленных flat215, повторы
  с разными seed); пройдено 245. Сверка входа по ВСЕЙ очереди: HD-фото есть у 1230 заданий и
  во ВСЕХ 1230 в задании стоит именно HD (ADR-0136 соблюдён), у остальных 235 HD в базе нет.
- 01.09 постобработка съедала оплаченное время нод (шаги висели до таймаута 45 мин, пул
  простаивал): (1) `orient_worker.pending_meshes` считал sha256 КАЖДОЙ модели на каждом запуске
  (>1 ГБ чтения) → кэш по (mtime,size), отбор 5.7с→0.1с; (2) `topview_render` запускал 10
  процессов рендера на 8 ГБ машине → шаг убивало через ~70с, снижено до 4; (3) топ-вью также
  получил кэш готовых записей и проверку бюджета в начале цикла (раньше проверял только у
  рендера и умирал молча на анализе).
- 01.09 ремонт мешей ОТМЕНЁН владельцем (ADR-0143 соседней сессии) — проверено фактами:
  `model.repaired.glb` в дереве 0 (118 в `~/scout-scenes/mesh-repairs-parked/`), код копий не
  создаёт, галерея/топ-вью читают `model.glb`. Грабля: мой ночной перезапуск конвейера ОДИН РАЗ
  вернул отключённый ремонт (отключение жило только в процессе соседней сессии) — отключать
  такое надо в коде/файле-флаге, а не в env запуска.
- 01.09 ОБЪЁМ МЕШЕЙ (владелец поправил дважды): полный пул = **11 631 товар**
  (`plans/mesh-bulk-salad-hunyuan.md` — «по итогам пилота владелец решает про полный пул 11 631»,
  по нему считалась экономика своего образа против fal). ≈4 900 — ПОДМНОЖЕСТВО методики ADR-0131
  (роли слотов сетов), не главная цифра; мой пересчёт по базе на 01.09: в ролях слотов с
  габаритами и фото 7 862, всего нуждающихся в меше по стратегии 17 935 из 20 553 каталога.
- 01.09 приёмка: ОДНА проверка меша стоит 29с (замер) → ACCEPT_CAP 40 не влезал в таймаут шага;
  поставлен лимит 20 штук + бюджет 600с (`apply_repairs.py`). Волна переделок = очередь перегона
  забракованных с другим seed (сейчас 266), живёт в конвейере, отдельной страницы нет;
  `/test/mesh-color/` — ЭТО ДРУГОЕ: эксперимент соседней сессии по экспозиции входа (37 товаров).
- 01.09 демо flat215 идёт ПЕРВЫМ в очереди — подтверждено: позиции 0-78 из 1465, все пройдены,
  готово 66 мешей из 75 товаров набора (9 в волне переделок).
