# Канон mesh-eligible ролей и плиты у диванов

## Что случилось (30.08, пилот идёт)
1. КОВЁР попал в генерацию мешей. Правило «ковёр = плоскость с фото, меша не имеет» стояло
   в сетовом сэмплере, но товары из flat215-демо вливались в план ДРУГИМ путём и фильтр
   обошли. Я закрыл дыру: фильтр NON_MESH_ROLES перенесён в единственную точку отправки
   (ssh_run: jobs_from_sample + jobs_from_file), галерея страхуется тем же списком.
2. Плита-«пол» теперь и у ДИВАНОВ (114667:7701300897294268317). По твоему q26 автосрез у
   диванов запрещён (законные цоколи) — такие уходят в перегон seed+1. Но если плита
   seed-стабильна, перегон сожжёт 2 генерации и всё равно уйдёт в REPLACE_PRODUCT.

## Вопросы
1. Где ДОЛЖЕН жить канон mesh-eligible/non-mesh ролей, чтобы его нельзя было обойти новым
   источником заданий (сеты, flat215, будущая ежедневная очередь mesh_queue соседней сессии,
   ручные regen)? Вариант: files tools/scout/rules/mesh-roles.json + импорт в ssh_run,
   mesh_pilot, mesh_queue, галерею; или гейт в самом ВОРКЕРЕ (нода отклоняет non-mesh роль)?
   Что из этого канон, что страховка?
2. Диваны с плитой: есть ли БЕЗОПАСНЫЙ автоматический признак, отличающий артефактную плиту
   от законного цоколя? Кандидаты: плита выступает ЗА паспортную глубину d (цоколь всегда
   внутри габарита); плита тоньше X% при этом основная масса опирается на отдельные ножки
   (есть ≥3 контактных кластера выше плиты); цвет плиты вне палитры. Или не изобретать и
   принять политику: диваны только через reseed→replace?
3. Ковёр в планировщике: подтверди подход «плоскость с alpha-текстурой из вырезки» как
   канон для ковров/текстиля, или есть довод за меши и им?
Коротко, с приоритетами: что внедрить сейчас в идущий прогон, что после.
