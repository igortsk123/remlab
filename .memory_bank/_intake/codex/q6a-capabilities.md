Продолжаем (каталожная сессия). Q6a свода №13 (MASTER-zones-v7 §Q6a): capability-модель каталога — мой дизайн, прошу критику ДО реализации (кратко, с якорями).
Факты: products(params jsonb; у divan.ru есть «Посадочное место: Длина/Глубина/Высота посадочного места», «Наличие подлокотников», «Механизм трансформации»), product_enrichment.payload.specific (seat_height категориальная, arms, back…). В стоке: «Пуфы и банкетки» 218 (роль пуф; 15 шт 100–144×35–45), «Кушетки» 152 (роль диван; 13 шт 115–135×50–62), комод d≤40 & w 90–160: 190; тв-тумба d≤40 & h≤80: 361; обеденные с «раскладн|раздвиж|трансформ» в имени: 124. Владелец: кухонные уголки/столы-книжки НЕ нужны; cat_role не менять.
Дизайн: (1) `tools/scout/capabilities.py` → таблица `product_capabilities(shop_mid, external_id, source_role, caps jsonb, evidence jsonb, rules_version, computed_at)`, идемпотентная пересборка в refresh_daily после load3/enrich; правила — `tools/scout/rules/capabilities.json` с provenance. (2) caps: seat_length/depth/height_cm (params → evidence 'params'; иначе dims-эвристика с evidence 'dims'), has_back/has_arms (params/enrichment), wall_seat_capable (банкетка 100–144×35–45 | кушетка seat_length≥100 & d≤62), nominal_seats=floor(seat_length/55), dining_seat_capable (wall_seat_capable & seat_height 42–48 & has_back? нет), console_capable (комод/тв-тумба/стеллаж: d≤40, w 90–160, h≤85; финальная проверка h≤спинка+5 — при расстановке), foldable_dining (регекс; состояния closed/open неизвестны → sleeping). (3) планировочный слот `банкетка` для compose2 = SKU с wall_seat_capable (cat['банкетка'] строится из capabilities, cat_role не трогаем); `консоль` — не роль, а capability у комод/тв-тумба (Q6e). (4) тесты: provenance, счётчики на дев-БД (≥10 банкеток, ≥10 кушеток, ≥100 console_capable), инвариант «cat_role не изменился».
Вопросы: 1) пороги/эвристики — что поправить (seat_height для nook 42–48? кушетка без спинки как обеденная посадка — норма?); источники. 2) evidence per attribute — достаточно строки-источника или нужна структура {value, source, confidence}? 3) где хранить: отдельная таблица vs колонка в product_enrichment (дельта/версии)? 4) что ещё composer/солвер должны знать уже в Q6a, чтобы Q6b (edge_nook: банкетка спинкой к стене + стол + ≥2 стула) и Q6e не переделывали модель? 5) риски: фиды с params только у divan.ru — как честно помечать «нет данных» и не плодить ложные capability из dims-эвристики.
