КРИТИКА МЕТАПЛАНА до показа владельцу (правило проекта: новый метаплан — сперва на разбор).

ЗАДАЧА ВЛАДЕЛЬЦА (дословно): «человек заходит, кликает на планировке комнату, она увеличивается,
далее он может нажать подобрать из шаблона и мы ему генерим несколько моделей шаблонов под эту
планировку, и также он может сам добавлять диван или ковёр или двигать мебель или сбросить до
"стандарта". Далее кликает на модель дивана — подгружается лента из товаров с фотками, которые он
может на этот товар примерить. Размер модели автоматом уточняется под товар, заполняет
пространство, а по клику мы видим фотку товара и возможность её заменить. В любой момент человек
может отправить на рендер и увидеть комнату с 2 ракурсов только с тем, что он спланировал».
Референс — AlphaPlan (apps.alpha-vision.com/alphaplan): интерактивный план, режимы Room Planner /
Room Designer, поворот/масштаб, сохранение и шаринг.

МОЙ ЧЕРНОВИК МЕТАПЛАНА — `.memory_bank/plans/MASTER-interactive-planner.md` (прочитай его целиком).
Кратко: P0 контракт документа плана → P1 мгновенный отклик → P2 планировка квартиры →
P3 холст редактирования → P4 варианты из шаблонов → P5 примерка товара → P6 рендер 2 ракурса →
P7 проекты/шаринг, P8 сквозные гарантии.

ФАКТЫ ПРО ТЕКУЩЕЕ СОСТОЯНИЕ (проверь сам):
- солвер `services/planner-solver/`: экзамен 272/272, но время сцены медиана 47 с, p95 193 с,
  максимум 639 с (`tools/scout/acceptance-report-zoned.jsonl`);
- каталог 25 тыс. товаров с фото и габаритами, capability-модель, контракты подбора (`tools/scout/`);
- замер комнаты по фото — `services/room-measure/`;
- веб-приложение Next.js в проде (Stage 1), реф-монетизация;
- контракт позы для 3D/LLM — `tests/test_pose_contract.py`, экспорт `tools/scout/export_plans_ai.py`.

ВОПРОСЫ:
A. Верна ли декомпозиция и порядок пакетов? Что лишнее, чего не хватает, что стоит объединить?
B. Скорость: как ЧЕСТНО получить ≤300 мс на интерактивное действие, имея полный поиск в десятки
   секунд? Какие уровни (предпосчёт вариантов, инкрементальная валидация только затронутых правил,
   кэш, ограниченный локальный пересчёт зоны) реально дают этот бюджет, а какие — самообман?
   Что должно происходить, когда правка пользователя делает план невалидным: запрет, подсказка,
   авто-починка соседних предметов?
C. «Примерка товара»: как правильно устроить подмену SKU, чтобы не порвать канон (габарит меняется —
   меняются зазоры, ковёр, ось ТВ)? Нужен ли слой «слот» между каноном и товаром, и как в нём
   держать наши конверты и контракты? Что делать, когда выбранный товар не влезает?
D. Рендер «2 ракурса, ровно то, что спланировал»: наш AI-пайплайн или 3D? Как контролировать
   идентичность предметов в кадре и как выбирать камеры детерминированно из плана?
E. Документ плана: какая модель данных выдержит и UI, и солвер, и рендер, и историю правок
   (версии, источник предмета, откат к «стандарту»)? Где тут обычно ошибаются?
F. Риски: что в этой архитектуре чаще всего убивает продукт (производительность, рассинхрон
   контрактов, стоимость рендера, мобильный ввод)? Что проверить ДО начала работ (спайки)?
G. Что бы ты сделал первым спайком на 1–2 дня, чтобы снять главную неопределённость?

Файлы не изменяй. Верни: критику декомпозиции (что убрать/добавить/переставить), ответ по каждому
вопросу с обоснованием и ссылками на наш код, список рисков с ранней проверкой, и предложение
первого спайка.
