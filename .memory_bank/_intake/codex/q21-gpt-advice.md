Ты — независимый старший инженер-ревьюер. Разбери ВНЕШНИЙ совет (от GPT, передан владельцем) и
сверь его с нашим репозиторием. Не принимай совет на веру и не отвергай — проверь по коду и нашим
зафиксированным экспериментам.

СОВЕТ GPT (сжатая суть):
1. Главная схема: фото SKU → Trellis → грубый GLB (кэш, ~$0.02/SKU) → масштаб по W/D/H →
   расстановка нашим планировщиком → детерминированный 3D-рендер (правильная окклюзия) →
   в OpenAI на «красоту» вместе с фото SKU. «Первый кадр вообще не генеративный».
2. Альтернатива: примитивный рендер + depth + INSTANCE segmentation + отдельные reference на
   товар через masked IP-Adapter в fal-ai/flux-general/image-to-image (веса: depth 0.9–1.0,
   seg 0.8–1.0, IP 0.4–0.65, strength 0.65–0.8). Нормали из MVP убрать. Приоритет:
   instance mask > depth > rough RGB > normals > canny.
3. Белый лист эталонов на ПРОМЕЖУТОЧНОМ этапе не использовать (correspondence-задача);
   аппликация допустима только spatial, но врёт про ракурс.
4. SDXL-ControlNet-union не брать основным (перенос идентичности слабее FLUX).
5. FLUX.2/Qwen Edit для первого этапа не брать (semantic editing без жёсткой фиксации).

НАШИ ФАКТЫ ДЛЯ СВЕРКИ (проверь сам по репо и памяти):
- ADR-0060/0062/0063 (05.08): мы УЖЕ делали Trellis→GLB (mesh_make.ensure_mesh, кэш
  ~/scout-scenes/meshes, самопроверка силуэта IoU+цвет, фолбэк Hunyuan3D) и СВОЙ рендер
  mesh_render.py (numpy z-buffer, части по отдельности, 1.3 с/вид). Почему трек тогда не стал
  основным? Что в нём было болью (по коду/логам/ADR)?
- Сегодняшние эксперименты (28.08, scratchpad-скрипты rough_v4/v5/v6): SDXL-CN depth даёт
  фотореализм, но теряет окно/дверь и цвета; i2i strength 0.55 «замазывает» аппликацию, 0.8 —
  теряет мелкие предметы. Двухпроходная схема (наша композиция → gpt-image-2 «только улучшить»)
  держит места. Аппликация v3 «повёрнутая на видимую грань» лучше плоской, но фото не
  доворачивается.
- q19/q20: у fal flux-general регион-маски IP-Adapter под вопросом (Diffusers issue 13584 —
  маски принимаются и игнорируются), «only one controlnet», обязательные path/image_encoder_path.
  Совет GPT это не упоминает — оцени, меняет ли это его вариант 2.

ВОПРОСЫ:
1. Согласен ли ты, что вариант А (GLB+свой рендер → GPT) — главный? Что конкретно надо поднять из
   нашего старого кода и что дописать (сценовый рендер ВСЕХ мешей с окклюзией: есть ли он у нас,
   или было только повидовое вырезание в коллаж)? Оцени объём работ по файлам.
2. Слабые места варианта А по нашим же данным: доля брака Trellis по ролям (check-json в кэше),
   стоимость/время холодной генерации на каталог 3086 позиций банка, что делать с ролями-провалами.
3. По варианту 2: с учётом q19/q20 — жизнеспособен ли masked IP-Adapter в flux-general/i2i, или
   его надо сперва прогнать через тот же причинностный тест? EasyControl на fal — реально есть?
4. «Instance mask > depth > normals» — согласен? Наш опыт с normals (коробочные прокси) против
   свежих прокси-форм (стул с ножками) — нормали теперь полезнее или всё ещё вредны?
5. Что из совета отбросить как неприменимое к нам, что принять, в каком порядке. Дай короткий
   план эксперимента с гейтами (не более 2 экспериментов, как советует GPT, или иначе).

Файлы не меняй. Верни: вывод; сверку каждого пункта совета с нашим кодом/памятью (пути/строки);
риски; рекомендацию с порядком работ; что изменило бы вывод.
