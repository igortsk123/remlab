# Вопрос: отменяем обрезку ссылок mnogomebeli (наличие + реф-ссылки)

## Что изменилось с прошлого раза
Новый слой правды о наличии (31.08): `tools/scout/stock_truth.py` (единственный писатель
`products.in_stock`), `tools/scout/page_alive.py` (чистый классификатор ответа магазина),
`tools/scout/stock_check.py` (обход/наблюдения/гейты). План — `plans/stock-truth-page-verdict.md`.

## Найденный дефект (владелец: на демо показан мёртвый товар со ссылкой на раздел)
`tools/scout/reflink.py:29-36` для хоста mnogomebeli режет путь: `SPA_CUT` убирает `/!вариант/`,
затем `re.sub(r'/[^/]+/$','/')` убирает ещё один сегмент. Обоснование в комментарии (26.08):
«у mnogomebeli и карточка, и её родитель дают 404, живёт только страница серии».

Мои сетевые замеры 01.09 (curl, UA браузера):
- полная карточка `.../boss-sleep-160-velyur-royal/!divan-...-topaz/` → **200**, в теле
  `schema.org/InStock` (проверено `page_alive.schema_state` → positive);
- её родитель `.../boss-sleep-160-velyur-royal/` → **404**;
- `%21`-кодировка `!` → тоже 200 (то есть дело не в кодировке);
- `.../серия/&erid=...` (без `?`) → 301;
- выборка 30 случайных карточек магазина: 19 → 200, 11 → 404 (наши обрезанные ссылки: 30/30 → 200).

Следствия, которые я вижу:
1. Обрезка уничтожила единственный сигнал смерти: `stock_check` пробит `coalesce(direct_url,url)`
   = раздел → всегда 200 → контракт `mnogomebeli: evidence='series'` (`page_alive.py:49`) →
   вердикт `unknown` → `unknown` наличие не снимает → товар магазина нельзя снять автоматикой
   в принципе, пока он есть в фиде. `product_page_status` по mid 114667 — 0 строк.
2. `catalog_media.py:77` применяет `direct()` ПОВТОРНО к уже обрезанному `direct_url`
   (`direct()` неидемпотентен) → в банке ссылка ещё на уровень выше, чем в БД.
3. `in_stock` остаётся true → `sets_heal` не заменяет; `flat215_demo.py` наличие не смотрит вовсе.

## Что я собираюсь сделать
1. `reflink.py`: снять ветку обрезки для mnogomebeli целиком (оставить unquote `goto=` и починку
   разделителя `?erid`); добавить `--selftest`, включая проверку идемпотентности `direct(direct(u))==direct(u)`.
2. `page_alive.py`: контракт mnogomebeli `evidence:'series'` → `'sku'` + `signals:('schema',)`;
   обновить селфтест (200+InStock → alive, 404 → gone).
3. Бэкфилл `products.direct_url` для mid 114667 пересчётом из `url`.
4. Прогон `stock_check.py --shop mnogomebeli.com` (обход ~1092 карточек, пауза 2–5 с, затем
   подтверждающий второй голос через 15 мин) → снятие наличия у подтверждённо мёртвых →
   `catalog_media --apply` → `sets_incremental --heal --apply` → `--enforce-contracts --apply`
   → `--index` → пересборка и публикация демо.

## Вопросы, на которые нужен твой независимый разбор
1. Есть ли причина, по которой полную ссылку с `/!` хранить НЕЛЬЗЯ (партнёрская атрибуция,
   редиректы Гдеслона, кодировка `!` в `goto=`, поведение `xf.gdeslon.ru`)? Реф-метка `erid`
   в конце сохраняется при любом варианте — я это проверил, но интересует именно засчёт клика
   партнёркой: не ломается ли атрибуция, если мы ведём в карточку, а не в раздел.
2. Гейт первого прохода `GATE_SHARE_FIRST_RUN=0.60` (`stock_truth.py:45`). Ожидаю ~37%
   отрицательных на 1092 карточках — проходит. Но правильно ли вообще применять вердикты
   на ПЕРВОМ проходе после смены схемы ссылок? Аргумент против: смена ссылки — это ровно тот
   случай, когда `url_hash` меняется у всех сразу и история обнуляется. Стоит ли снять
   вручную ограниченной партией (например 200) и посмотреть, или гнать весь магазин?
3. Не пропускаю ли я других писателей/потребителей `direct_url`, которых сломает удлинение
   ссылки (кэши, ключи, отчёты, ширина колонок в БД, `url_key` в `page_alive`)?
4. `catalog_media.py:77` — двойное применение `direct()` станет безвредным после правки
   (веток обрезки не остаётся). Оставить как есть ради ветки `coalesce(..., p.url)` или убрать
   и требовать, чтобы `direct_url` был уже готовым?
5. Что ещё в этой цепочке (фид → ссылка → проверка карточки → наличие → лечение сетов → демо)
   стоит проверить тем же способом: есть ли ещё магазины, где мы проверяем не то, что показываем?

Ответ — по пунктам, с указанием файлов и строк. Если считаешь план неверным — скажи прямо, что
сделать вместо него.
