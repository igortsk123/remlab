Ты — независимый ревьюер. Владелец предложил конвейер канонической ориентации мешей (внешний
совет). Сверь с нашим кодом и оцени практичность на НАШЕЙ машине (DEV-VM без GPU).

СХЕМА ВЛАДЕЛЬЦА:
1. Hunyuan3D/Trellis → GLB. GLB→OBJ без изменения геометрии, нормализация масштаб/центр,
   папка+txt-индекс (utils/generate_index_file.py), two_stage_inference_script.py с pretrained
   orienter+flipper из репо github.com/cscarv/3d-orienter (установка poetry install).
2. GLB НЕ перезаписывать: orienter как сервис → вычислить применённую rotation → хранить только
   canonicalRotation (3 числа/кватернион) в БД; исходный textured GLB нетронут.
3. Проверка тремя слоями: (а) confidence orienter'а (flipper даёт вероятности, adaptive
   prediction set — большой набор = подозрительный объект); (б) НЕЗАВИСИМОЕ свидетельство —
   рендер GLB в 8 горизонтальных направлений, сравнение с каталожным фото через DINOv2/CLIP →
   с какого направления снято фото → согласуется ли с front orienter'а; (в) VLM ТОЛЬКО арбитр
   при конфликте 180°: «это chair, на каком изображении FRONT — куда направлены ноги сидящего»,
   выбор из двух геометрически валидных вариантов. Автоисправление +180° к canonicalRotation.
4. Усиленная проверка только для chair/armchair/sofa/bed/wardrobe; симметричное (круглый стол,
   табурет) — фронт фундаментально неоднозначен, это норма.

ВОПРОСЫ:
1. Репозиторий cscarv/3d-orienter: реален ли, что в нём (модель, зависимости, лицензия), пойдёт
   ли inference на CPU и сколько на 40 мешей? Наши ограничения: venv ~/venvs/scout, без GPU.
2. Слой (б) DINOv2/CLIP на CPU: чем заменить дешевле (наша цветовая сетка? qwen-эмбеддинг?),
   и не дублирует ли он наш mesh_front для сидячих?
3. Как схема стыкуется с уже сделанным: mesh_front (сиденье-классификатор, авторитет сидячих),
   symmetric_by_role (пуф/банкетка), qwen-фоллбэк с признаковыми промптами, манифесты mesh_gate,
   front_yaw в конвенции MR. canonicalRotation вместо front_yaw — что мигрировать?
4. Риски: OBJ-конвертация теряет что-то важное для orienter? Нормализация авторов совместима с
   нашей load_parts-нормализацией? Прогон 30k на CPU — реалистично или нужен прокат GPU
   (наш Salad-контур)?
5. Порядок работ на спайк 40 мешей с гейтами. Файлы не меняй. Верни правки/план списком.
