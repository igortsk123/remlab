Ты — независимый senior-ревьюер. Разбери задачу сам, по коду репозитория. Не считай ничьи
гипотезы верными.

## ЦЕЛЬ ВЛАДЕЛЬЦА (01.09.2026)
«Очередь мешей выстрой полную сейчас, в структуре товаров» и «выстрой приоритетность и запиши
регламент где-то в коде, как выстраивать приоритетность». То есть нужен ПОЛНЫЙ порядок
генерации 3D-мешей по каталогу и записанное в коде правило, по которому этот порядок строится.

## ПРОВЕРЕННЫЕ ФАКТЫ (запросы к боевой дев-БД и прогон счётчика, 01.09)
- Каталог 20 553 товара; в наличии 19 280; с фото 19 277 (из них **HD-фото 12 107**);
  обогащены и со стилями 19 213; **с габаритами 12 287** (без габаритов в работу не берут).
- `products.mesh_required` = **11 922** (ровно совпадает с независимым счётчиком
  `tools/scout/salad/mesh_demand.py`, поле `need_with_dims`). Готово (`mesh_status='ready'`,
  есть `mesh_uri` и `mesh_at`) = **241**, и это ровно те 241 SKU, у которых на диске есть
  `model.glb`.
- `need_slot_roles` = 7 847 (роли слотов сетов), осталось 7 606.
- Топ ролей по потребности: люстра 2439, диван 2308, бра 977, стул 835, шкаф 825, комод 679,
  ваза 541, кашпо 498, тв-тумба 442, кресло 416.
- `products.asset_strategy` (в наличии): hunyuan3d 17 379, cutout 1 484, procedural_plane 370,
  пусто 47 (у этих 47 нет `cat_role`).
- Таблица `mesh_demand` (создаётся в `tools/scout/mesh_queue.py`) содержит лишь **2500** строк:
  `priority` 1 «в сете», 2 «кандидат слота», 3 «резерв»; статусы wanted/not_required/superseded.
  Причина спроса отдельным полем НЕ хранится, только огрублённый `priority`.
- Текущая рабочая очередь — файл `tools/scout/mesh-pilot-sample.json`: 1465 SKU → 1503 задания
  (у 25 SKU по 3 seed), курсор `--skip` = 305.
- Производительность: медиана ~195 с/меш, ~8-9 нод RTX 3090, около $0.0075-0.03 за меш в
  зависимости от загрузки пула. То есть 11 681 оставшихся мешей — это порядка 100+ GPU-часов.
- Приёмка бракует часть форм (`flat_shape`): на диске 435 комплектов с моделью и 55 только с
  диагностикой (без `model.glb`).
- Схема пометки в карточке: `db/init/008-mesh-binding.sql` (`mesh_required`, `mesh_status`,
  `mesh_uri`, `mesh_at`, `mesh_revision_key`, `mesh_policy_version`, вью `product_mesh_state`).

## ВОПРОСЫ, НА КОТОРЫЕ НУЖЕН ТВОЙ САМОСТОЯТЕЛЬНЫЙ ОТВЕТ
1. Прочитай `tools/scout/mesh_queue.py` (функции `demand_from_*`, `ingest`, `run`, `export`),
   `tools/scout/salad/mesh_demand.py`, `db/init/008-mesh-binding.sql`, `tools/scout/salad/
   ssh_run.py` (`plan_jobs`, `jobs_from_sample`) и `tools/scout/salad/batch_show.py`.
   ГДЕ должна жить полная очередь и её порядок: в файле-снимке, в таблице `mesh_demand`,
   в `products`, или во вью? Что из этого уже есть и что дублируется?
2. Какие ЯРУСЫ приоритета обоснованы данными, а какие я бы придумал из головы? Нужен ли ярус
   вообще, или достаточно одного числового ключа?
3. Внутри яруса — какой ключ сортировки максимизирует пользу на потраченный GPU-час? Разбери
   конкретно гипотезу «сначала роли, где дефицит мешей блокирует больше всего сетов» против
   «сначала самые частые роли» — это разные вещи, и я не уверен, какая правильная.
4. Стоит ли поднимать в очереди товары с HD-фото (12 107 из 19 277)? С одной стороны, вход
   лучше и брака меньше; с другой — это систематически отодвигает часть каталога навсегда.
5. Как очередь должна переживать ПЕРЕСТРОЙКУ: сейчас позиция хранится курсором `--skip` по
   порядковому номеру в файле. Если порядок пересчитать, курсор станет бессмысленным. Что
   должно заменить курсор?
6. Что делать с 47 товарами без `cat_role` (нет стратегии, в очередь не попадают вообще) и с
   повторно забракованными формами (перегон их не лечит)?
7. Где физически записать РЕГЛАМЕНТ приоритизации так, чтобы он был единственной истиной и не
   разошёлся с кодом (владелец просит «запиши регламент где-то в коде»)?

Инспектируй репозиторий сам. Ничего не меняй, файлы не создавай и не удаляй.

Верни: 1) выводы; 2) доказательства с путями и строками; 3) риски и краевые случаи;
4) альтернативы; 5) что рекомендуешь и почему; 6) допущения; 7) что изменило бы твой вывод.
Будь критичен: ищи, где очевидное решение окажется неверным.
