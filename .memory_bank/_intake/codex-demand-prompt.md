Владелец 01.09 решил: свет (люстры 2439, бра 977) и вазы ВХОДЯТ в сеты, меши им нужны →
итоговая потребность **12 092** товаров (после гейтов: наличие → фото → обогащение → стили →
роль требует меша по `rules/asset-strategies.json` → габариты; плоскости 1628 вычтены).
Требование владельца дословно: «записывай это всё как очередь в меши только в нужном
приоритете не ломая приоритета существующего. Каждому товару должна жёстко записываться
ссылка на меш чтоб не было путаницы, в базу».

Что уже есть:
- `tools/scout/mesh_queue.py` (ADR-0131): таблица `mesh_demand(sku pk, role, priority, status,
  image_url, source_sha, dims, name, ...)`; приоритеты 1 = стоит в сете, 2 = top-K кандидат
  корзины, 3 = резерв роли; `MESH_EXCLUDE` (подушка/плед/ковёр/шторы/картина/зеркало/часы/полка);
  ворота: in_stock + живое фото + enrichment quality ≥0.65. Сейчас в таблице 2500 строк
  (wanted 1321) — то есть покрыт узкий спрос, а не весь пул 12 092.
- `mesh_jobs(job_key, sku, status, batch_id, attempts)` — 30 строк; `asset_revisions(revision_key,
  sku, glb_sha, status, origin, manifest)` — 254 строки (связь меша с товаром ЕСТЬ, но в
  `mesh_demand` ссылки на готовый меш НЕТ).
- Файловая очередь пилота `mesh-pilot-sample.json` (1465 заданий) — по ней сейчас идёт прогон,
  первыми 78 позиций демо flat215. Конвейер `salad/batch_show.py` читает именно файл.
- Я добавил `salad/mesh_demand.py` — счётчик потребности по базе (воронка + разрезы), он НИЧЕГО
  не пишет в БД, только считает.

Мой план (на критику): (1) расширить `mesh_queue.py` до полного пула: добавить приоритет 4
«остальные меш-роли до 12 092» (свет, вазы и пр.), НЕ трогая приоритеты 1-3 и порядок внутри
них; (2) в `mesh_demand` добавить колонки ссылки на готовый меш: `revision_key` (FK на
asset_revisions), `glb_path`/`glb_url`, `mesh_status`, `mesh_at` — заполняются приёмкой;
(3) конвейер продолжает работать по файловой очереди, но файл экспортируется из БД
(`mesh_queue.py --export`), чтобы источник истины был один.

Вопросы: 1) верна ли схема «ссылка на меш прямо в mesh_demand» или правильнее вью поверх
asset_revisions (риск рассинхрона vs удобство)? 2) как безопасно расширить до 12 092, не сломав
идущий прогон по файлу (1465, пройдено 245) и не потеряв приоритет flat215? 3) какие статусы
нужны товару, чтобы «не было путаницы»: wanted/queued/generating/ready/rejected/not_required —
достаточно ли, где хранить «есть меш, но брак»? 4) ворота quality ≥0.65 и MESH_EXCLUDE против
моей политики ролей `asset-strategies.json` — это два разных списка исключений, свести в один?
5) миграция: как накатывать (у нас `db/init/*.sql` идемпотентно) и что делать со старыми 2500
строками. Кратко, по-русски, с рисками для идущего ночного прогона.
