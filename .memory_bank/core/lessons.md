---
tier: 1
topic: lessons
scope: Уроки перед планированием — что пробовали и что НЕ сработало
tier2: ../anti-patterns.md
updated: 2026-08-09
importance: high
source: manual
---

# Lessons — что НЕ сработало и почему

> Только инкрементальные bullets; писать сразу, читать перед каждым планом.

(1–213 — в `../anti-patterns.md`, архив; вшито в код/правила. Свежайшие из архива:
204 пара правил может быть совместно невыполнимой — constraint-contract CI; 206 ambient —
в точке входа, детерминизм держать фаззером; 209 честный A/B — только фича-флагом на одном
коде/данных; 211 подложка вне free_space симметрично collisions; 212 дериватив-якоря
клампить в контур; 213 TIMEOUT приёмки лечится таймаутом 600, не числом воркеров, и во
время экзамена не гонять рядом тяжёлое — аварийные пустые записи неотличимы от регресса.)
- [2026-08-10, source-KB] Гейты поймали 3 бага; хинты ≠ positives; судья vs подпись →
  REVIEW; single-writer реестров. Детали — anti-patterns.md.

- 214 книжная РЕКОМЕНДАЦИЯ в hard = регресс: класс-гейт LAYER_STRENGTHS
  (`services/knowledge-db/kdb/export_rules.py`); 215 виновника регресса ищет конвейер —
  `tools/scout/acceptance_bisect.py`; 216 судья-LLM читает «клиренс» как статический зазор —
  глоссировать в SYS; 217 отчёт приёмки пишет ключ `scene`, не `id`.
- 218 снап носителя не должен ОБМЕНИВАТЬ hard — «никаких НОВЫХ видов»; 219 дроппер не
  разбирает hard-чистые зонные БЛОКИ (protected=fixed); 220 порядок зон:
  группа→МЕДИА→столовая→хранение→чтение; 221 визави несовместимо с прицелом ТВ — блок
  только без носителя; 222 `solver_run.py` гонять ИЗ `tools/scout`.
- 223 у КОВРОВ в фидах `w` — КОРОТКАЯ сторона: мерить `max(w,d)`; 224 ковёр многосторонней
  композиции — по ВНУТРЕННЕМУ контуру зоны («все ножки или ничьи»).
- 225 правки движка во время идущего ЭКЗАМЕНА смешивают две версии кода в отчёте — гейт-диф
  недействителен: правки только в `git worktree`, вкат после финиша, затем чистый прогон.
