---
tier: 1
topic: lessons
scope: Уроки перед планированием — что НЕ сработало
tier2: ../anti-patterns.md
updated: 2026-08-21
importance: high
source: manual
---

# Lessons — что НЕ сработало и почему

> Только инкрементальные bullets; писать сразу, читать перед каждым планом.

(Уроки 1–296 — в `../anti-patterns.md`: они вшиты в код, правила и тесты. Здесь только
свежие, ещё не «застывшие» в гардрейлах.)


Более ранние уроки — `../anti-patterns.md` § Архив уроков (перенесены 26.08: Tier 1 ≤3 КБ).

## Сессия 26–28.08 (демо-рендер; детали — ADR-0124..0130)

## Сессия 28.08 (ориентации/меши; детали — ADR-0129/0130)
- **Чинить по одной ошибке за цикл на медленной удалённой петле — дороже одной тщательной
  локальной проверки.** 7 перезакачек образа на Salad за два дня: py_compile и импорт-смоук
  не ловят умершие потоки (NameError на потерянном имени жил между двумя try). Перед выкаткой:
  pyflakes по ВСЕМ файлам + реальный запуск контейнера с прогоном старта (порт, прогрев,
  ошибка записана, gate работает).
- **Порог живучести обязан ПЕРЕКРЫВАТЬ время старта.** Проверка готовности Salad имеет
  потолок (задержка ≤1200с), а холодный старт ноды ~35 мин — инстанс при живом сервисе
  навсегда помечался неготовым, шлюз отдавал 503, нода 15 часов тарифицировалась впустую.
  Не подгонять пороги, а убирать зависимость: `/health` = «жив», гейт — в рабочем эндпоинте.
- **Облако тарифицирует СОСТОЯНИЕ, а не работу карты:** `running` идёт в счёт целиком,
  включая прогрев, докачку весов и простой. Скачивание образа — бесплатно, значит тяжёлый
  образ с весами дешевле лёгкого с докачкой (ADR-0135). Гасить группу сразу после прогона.
- **На exit-fi `INPUT DROP`:** контейнер не достучится до сервиса на шлюзе docker-сети. Сервис
  для контейнеров ставить контейнером в ту же сеть, а не на хост — iptables на машине с боевым
  VPN не трогаем.
