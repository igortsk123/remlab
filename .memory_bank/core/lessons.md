---
tier: 1
topic: lessons
scope: Перед планированием — уроки; что пробовали и что НЕ сработало, отброшенные подходы
tier2: ../anti-patterns.md
updated: 2026-07-31
importance: high
source: manual
---

# Lessons — плейбук уроков (что НЕ сработало и почему)

> **Протокол (ACE):** только инкрементальные bullets; писать сразу, читать перед каждым планом.

## Уроки

1. [2026-07-28] `./deploy.sh` (arm64/QEMU) OOM'ит DEV-VM → деплой только авто (push в main).
2. [2026-07-30..31] UI-мелочи, читающиеся как баг (скрытый скролл навигации; мерцание sticky-шапки;
   кнопка «в рамочке»; подсказка под формой) → правила и детект — `anti-patterns.md` §6.
3. [2026-07-30] «м 2» (разорванный `<sup>2</sup>`): классификатор не знал пробела → «за м²»
   падала в «за упак» → единицы тестить одной фикстурой.
4. [2026-07-30] ИИ-фолбэк молча отдавал `{}` при ошибке (= «не понадобился») → неуспех внешнего
   вызова всегда console.error.
5. [2026-07-30] Ozon: резид.+моб. пулы банятся по репутации IP (не JS-челлендж) → Ozon/WB только
   через анблокер (Bright Data/Zyte); сохранённую страницу владелец отверг.
6. [2026-07-30] Авто-коммит (`git add -A`) захватил чужой staged-код → параллельные агенты в
   отдельных worktree; перед commit — status.
7. [2026-07-31] Молчаливый дефолт (рулон 0,53 м) давал «2 рулона» до ввода данных → дефолт —
   фолбэк, НЕ источник показанного числа (ADR-0034).
8. [2026-07-31] ADR-0027 удалил проёмы без явного одобрения владельца → откат (ADR-0035) →
   удаление функциональности — только с явного «да» владельца, одобрение — в ADR.
9. [2026-07-31] Кнопка-пустышка (markSaved «сохраняла» уже сохранённое ради цели Метрики) →
   цель аналитики — на РЕАЛЬНОЕ действие, UI не изображает действие ради счётчика (ADR-0036).
10. [2026-07-31] deployment.md 10 дней врал «авто-деплой сломан» (починили молча) → живой факт
   (CI-джоба + health.version) wins, память обновлять при обнаружении.

**Tier 2:** ../anti-patterns.md (детект в diff; UI — §6–7)
