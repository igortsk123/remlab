---
tier: 1
topic: lessons
scope: Уроки перед планированием — что пробовали и что НЕ сработало
tier2: ../anti-patterns.md
updated: 2026-08-09
importance: high
source: manual
---

# Lessons — что НЕ сработало и почему

> Только инкрементальные bullets; писать сразу, читать перед каждым планом.

(1–207 — в `../anti-patterns.md`, архив; вшито в код/правила. Свежайшие из архива:
200 один лейбл движка — один путь, скрытые фолбэки контаминируют A/B (test_engine_purity);
201 «просадка» метрики ≠ регресс кода — сперва разложи провалы по причинам;
203 «completed» у Batch API ≠ успех — терминальность по результату; то же про пустой фид;
204 пара правил может быть совместно невыполнимой — пересечение допусков проверяет
constraint-contract CI; 205 на каждый тип якоря — smoke «≥1 позиция»;
206 глобал модуля заражает следующую сцену — ambient ставить в точке входа, детерминизм
держать фаззером; 207 групповой приор не перебивает per-item неправдоподобие, порог
неправдоподобия — ролезависимый (ROLE_MAX), не константа.)

- 208 joint-кандидат группы несравним по сырому fast-score с одиночным — вытесняет одиночные
  линии в срезе И в луче; раздельные квоты в обоих местах (`services/planner-solver/planner/beam.py`);
- 209 baseline из resume-строк разных дней непригоден для A/B (крон пересобирает сеты) —
  честный A/B только фича-флагом на одном коде/данных; 210 `git checkout <commit> -- path`
  СТЕЙДЖИТ — «docs-only» коммит унёс откат кода (пробы — через stash/`git restore --worktree`);
- 211 подложка исключается из free_space СИММЕТРИЧНО collisions, иначе роли поверх гибнут без
  следа (`services/planner-solver/planner/geometry.py`, ADR-0083); 212 дериватив-якоря клампить
  в контур (якорь у стены терял весь дериватив); 213 TIMEOUT/крэши приёмки dev-VM = контеншн
  ≥4 воркеров, не код (ACC_WORKERS=2; раннер пишет rc+stderr).
