---
tier: 1
topic: project-state
scope: Снимок «где проект сейчас» — точка ресинхронизации при /clear и resume
tier2: "changelog/project-history.md"
updated: 2026-09-02
importance: high
source: manual
status: working
source_of_truth: canonical
last_verified: 2026-08-31
---

# Project State — снимок состояния

> Это СНИМОК «где проект сейчас», НЕ журнал (держи ≤ ~8 KB). Обновляя,
> ПЕРЕПИСЫВАЙ разделы под текущее состояние, а не дописывай хронологию: история сессий/волн —
> append в `changelog/project-history.md`, завершённые планы — в `completed_plans/`. Снимок =
> что истинно СЕЙЧАС + ссылки. Первое, что читает агент при resume/`/clear`; обновил — `updated:`.

## Конвейер каталог→меши→сеты (29.08) — ГЛАВНОЕ
- Канонический порядок владельца работает сквозняком (`core/pipeline-order.md`, ADR-0133..0136):
  фиды+API → обогащение (Vercel) → стиль (luna) → отбор → хеши фото → [маски ждут Salad] →
  очередь → дневная партия 10 → сеты. Воронка: `pipeline_funnel.py`; 29.08: каталог 20 544 →
  отбор 16 546 → хеши 16 200 → вырезано 232 → сетов 126, готовых 0 (мешей ещё нет).
- 01.09 РЕМОНТ МЕШЕЙ ОТМЕНЁН (ADR-0143): везде оригинал `model.glb`, копии в
  `~/scout-scenes/mesh-repairs-parked/`. Цвет — ADR-0145 и [[mesh-color]]: диагноз есть, мерка
  сверена с 43 отметками владельца, ждёт пересборки образа (ножи снять + патч seed/CFG покраски
  + экспозиция входа) и опыта 4→12 заданий. Страница /test/mesh-color/.
- **02.09 СОСТОЯНИЕ ПРОГОНА.** Очередь полная и по регламенту (ADR-0153): 11 704 задания,
  ярусы владельца, файл `mesh-queue-v1.json`, курсор 30. Готово мешей 333. Демо flat215
  закрыто (9 позиций владельца сделаны 01.09). Сетов полностью обеспечено 14 из 126, до
  закрытия всех осталось ~216 мешей.
- Инфра: образы только digest; боевой — **слой кода поверх `cu124-baked` с ОТКЛЮЧЁННЫМИ
  гейтами** (ADR-0152/0154), группа **mesh-fresh-1** (batch, 60 ГБ диска, 12 ГБ ОЗУ,
  10 реплик, 4 класса карт по 24 ГБ). Прежние mesh-run10/mesh-gates-a удалены/остановлены.
  `batch_show.py`: разбор в фоне и по таймеру, спул повторов, автоочистка приёмника.
  Экономика: внутренний счёт ~$0.009/меш, ПО ФАКТУ ~$0.025 при загрузке пула 30% (ADR-0157).
- **02.09 УЗКОЕ МЕСТО — НЕ КОД, А МАШИНЫ.** Ночью пул набирается по 1–7 машин и осыпается;
  каждая тянет образ 20.7 ГБ по 0.25–1%/мин. Генерации нет с 01.09 ~20:00. Причины разобраны
  в ADR-0157: наши же пересадки (починено), исключения нод у группы, ёмкость тарифа `batch`.
  НЕ ПОВТОРЯТЬ: правка ресурсов группы перезапускает машины и обнуляет закачку.
- Качество: конвейер брака владельца (ADR-0138) работает конвейерно; цвет/свет к фото; фронт —
  mesh_orient → orientation_state; готовится вид сверху из мешей для планировщика
  (план topview-from-mesh, тест /test/topview-test/); архитектура фронта v2 (DINOv2 + Orient
  Anything V2 арбитром) — план orient-v2, на Codex-ревью.
- Тестовый сет flat215: мешей 27/63 готово, 39 в волне, 12 — плоскостями (ковры).

## Демо-планировщик (сводно)
- Демо живо: remont-lab.online/test/flat215-demo/ (план → расстановка → серверный scene3d-кадр
  из мешей БЕЗ GPT → заявка); 31.08: полное качество рендерит DEV через ssh-туннель с фолбэком
  на прод (~11с, ADR-0139); правила сцены и управления (камера внутри, ковёр-гомография, развод
  коллизий, тап-стрелки поворота) — ADR-0140; детали — [[demo-planner]] и history.
  Ждёт владельца: апгрейд VDS (4–8 ядер/8GB) для отказа от DEV-костыля.

## Расстановка мебели — текущий фокус (2026-08-21)

**Каноны, а не планы.** С 19.08 фокус — ситуационный канон (Q11/Q12, ADR-0109…0114): схема
выбирается под ЯКОРЬ комнаты; библиотека `/test/canons/` (65 карточек) рисуется боевым кодом
и проходит `validate()`; 12 зон / 60 схем, покрытие полное. Аудит Юли 21.08 отработан
(ADR-0115/0116, 13/15 принято; №13/№35 отклонены с пруфом). Сводка — `core/layout.md`.
**Заморозка (владелец 19.08):** планы/экзамен/галерею планов/экспорт НЕ пересобираем, пока каноны
не отработаны; тесты уровня планов (pose-контракт, focus_wall, dining watchdog) скипаются, а не падают.
**Экзамен (ночной крон 20.08, 03:28): 272/272 ok, TIMEOUT 0** (8 XL-таймаутов 17.08 закрыты);
открытый дефект — `test_route_is_walkable` на `set5-trapezoid` (маршрут 60 см при полу 70/75),
разбор отложен до снятия заморозки.
**Активный план:** `plans/q12-situational-canon.md` (открыты Q12-3 «гейт семантики якоря» и
Q12-5…7). Q11 завершён (`completed_plans/q11-canon-reference-contract.md`).
**Пайплайн и каталог — на паузе:** `plans/MASTER-pipeline-hardening.md` (сделаны А0+А1, А2–А7 нет)
и `plans/catalog-freshness-chain.md` (остались heal-замена выбывших и `/test/health.html`) —
статус `partial` с 21.08.
**Каталог/деньги:** OpenAI платные шаги ВЫКЛ (`openai.off`), лимит $5/день (`openai_budget.py`),
новый ключ (нет кредитов — пополнить), старые отозвать — владелец; nonton карантин — ждёт владельца.

### База движка (сводки; история — `changelog/project-history.md`)
Раскладка — ТОЛЬКО из шаблонов зон с паспортами/инвариантами (`services/planner-solver/rules/templates.json`, ADR-0088);
конверт −20/+10 только при подборе (`compose2.py`), солвер габарит SKU не меняет; порядок стадий
дизайнерский (ADR-0091); вторичная зона откатывается целиком при ухудшении маршрута/щелей/фокуса.
Модификаторы mode × shape × контур (ADR-0094); своды №8–12 (ADR-0098…0106): dining-каскад с
объяснимостью, MEDIA_MISSING/exactly-one, band=кап, functional claim, beam по гипотезам, паспорта=
runtime. Заполнение пола — диагностика, не цель. Приёмка: 272 сцены (`tools/scout/acceptance_run.py`,
№253+ свои проёмы), галерея `/test/acceptance-plans/` (номер плана стабилен).

## Каталог и стиль (2026-08-06)

Мебельный трек перешёл от «угадать по названию» к «взять из данных»:
- **Роль товара — из дерева категорий фида** (ADR-0070). База очищена: 87 672 → 25 034 товара,
  загрузчик берёт только 76 нужных категорий (дыры: кашпо 0, ковёр 26 — волна А2).
- **Обогащение построено и покрывает весь active-пул** (`MASTER-catalog-ai` К0–К3 + А0):
  `product_enrichment`, дельта по тексту/геометрии ТРИГГЕРИТ переобогащение (А1 закрыта,
  `load3.py:205`; смена ФОТО — не триггерит, дыра аудита 10.08), статусы,
  каскад на `gpt-5.6-luna`.
- **Стиль — сумма наблюдаемых признаков с рангами** (ADR-0071), честный ответ «стиля нет».
  Распределение: минимализм 49%, сканди 21%, современный 12%, неоклассика 11%, лофт 4%,
  джапанди 3%, нейтральных 11%.
- **Правило по деньгам** (ADR-0072): всё платное — сперва пробной партией; батчи — ADR-0073.

В очереди за мастер-планом: пересобрать комплекты на новых оценках, К5 (зоны, оценка набора).

## Что готово (детали — по ссылкам)
Bootstrap S1–S4 (`completed_plans/remlab-bootstrap.md`) · Stage 1 M0–M8
(`archive/plans/stage1-master-roadmap.md`) · Observability/PostHog (ADR-0012) · навигация+каркасы
(ADR-0017, `completed_plans/site-nav-and-scenarios.md`) · Калькулятор v2 в проде (ADR-0018,
`core/estimate.md`; К5/К6 ждут ключей) · трейсинг пайплайна в проде (ADR-0013,
`core/observability-tracing.md`). Хронология — `changelog/project-history.md`.

## Ключевые решения
Все ADR — `decisions.md` и `docs/DECISIONS.md` (дублировать список здесь не нужно).
Несущие: **0016 пивот v0.4 «Смета-first»**, 0001 self-host compose на exit-fi, 0013 трейсинг.
Стек: TS strict + Next.js + Drizzle + Zod + Inngest + внешний инференс (спека §1).

## Что НЕ делаем (вне scope v0.4)
Свой каталог бригад · точная смета работ (только грубо, отдельной строкой) · fit-движок ·
UK · кухня как вход (пока).

## Open questions / TODO
- Код под v0.4 — см. «Код-долг» в разделе Концепции.
- Прокси-анблокер (Bright Data/Zyte) для чтения Ozon/WB — решение/оплата владельца (ADR-0032).
- Auth: anonymous session id (интерим) vs GoTrue — Stage 1. Realtime job: polling vs self-host — Stage 2.

## Policies (как ведём разработку)
- План-first (`.claude/rules/agent-workflow.md`): код только после «деплой».
- Не ломать VPN-ноду на exit-fi: бэкап+rollback перед правками сервера, изоляция сети/лимиты.
- Секреты только в `.env` на сервере, не в git/памяти.
- Гипотезы, не аксиомы: отклонения → `docs/DECISIONS.md`.
- Migration-ready: приложение = compose + env + volume-dump + образ.
- **Память: durable — только в `.memory_bank/`.** Конец сессии — `/memory-check` (свод+гигиена);
  концепция — `guides/memory-automation.md`.
