# deps / build
node_modules/
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo

# env / secrets (никогда в git)
.env
.env.local
.env.*.local
*.pem
*.key

# test / coverage
coverage/
playwright-report/
test-results/
.vitest/

# os / editor
.DS_Store
*.log

# memory bank kit (не часть проекта)
_optional/
tools/*
# исключение: CI-воркфлоу memory-audit.yml гоняет этот скрипт → он ДОЛЖЕН быть в репо
# (zero-dep, без секретов). Остальной tools/ остаётся локальным.
!tools/memory-audit.mjs
_intake/_processed/

# docker artifacts
*.tar
*.tar.gz

# trace assets (картинки прогонов пишутся на диск/том, не в git)
.data/

# memory-bank start
.memory_bank/_secrets/*
!.memory_bank/_secrets/README.md
.memory_bank/changelog/reads.log
.claude/settings.local.json
.claude/settings.local.json.bak-*
.claude/settings.local.json.proposed
# memory-bank end
