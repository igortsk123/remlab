# venv: ~/venvs/kdb (pip только в venv — правило DEV-VM)
# Проверено на Python 3.14.4 (факт этой машины)
pydantic==2.13.4
jsonschema==4.26.0
rfc8785==0.1.4
pytest==9.1.1
# VEC-канал/эмбеддинги (optional — pairing деградирует с warning без них)
fastembed==0.8.0
onnxruntime==1.28.0
numpy>=1.26
