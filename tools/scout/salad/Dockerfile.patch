# Наращивание нашего кода поверх уже собранного образа.
#
# Зачем отдельный файл: базовый образ собирается час (CUDA-расширения, полсотни зависимостей
# Hunyuan), а наш код меняется по многу раз в день. Пересобирать всё ради трёх .py — потеря
# часа, и она уже случалась дважды: buildkit чистит свой кэш сам при нехватке диска, и тогда
# «инкрементальная» пересборка молча превращается в полную.
#
#   docker build -f Dockerfile.patch -t ghcr.io/igortsk123/mesh-hunyuan:cu124 .
ARG BASE=remlab/mesh-hunyuan:cu124-noweights
FROM ${BASE}
# Копируем ВСЕ модули воркера, а не список руками: соседняя сессия добавила collage.py и
# components.py, preprocess их импортирует — с ручным перечислением образ упал бы на старте.
# receiver.py и submit.py в контейнере не нужны (они наши, хостовые), но и не мешают.
# kornia нужен remote-коду BiRefNet (вырезка), у апстрима Hunyuan его нет. Ставим БЕЗ
# зависимостей: обычная установка потянула бы другой torch и молча сломала бы пайплайн.
RUN pip install --no-deps kornia==0.7.4 kornia_rs==0.1.8 &&     python -c "import kornia, torch; assert torch.__version__.startswith('2.5.1'), torch.__version__; print('kornia', kornia.__version__, '| torch не тронут')"
# Ускоритель закачки весов: платная фаза прогрева (веса на running-ноде) сжимается с
# 10–30 мин до ~3–5 (владелец 30.08: «сократить прогревы»). Полный BAKE_WEIGHTS=1
# требует ~60 ГБ диска на сборочной машине — отложен до расширения диска.
RUN pip install --no-deps hf_transfer==0.1.8 &&     python -c "import hf_transfer; print('hf_transfer ok')"
ENV HF_HUB_ENABLE_HF_TRANSFER=1
COPY *.py /app/
