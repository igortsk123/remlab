# ТОЛЬКО то, чего нет у апстрима. Ничего из его requirements.txt здесь быть не должно.
#
# Урок 28.08: сначала я продублировал сюда привычные пакеты со своими версиями — и молча
# переопределил ДЕВЯТЬ пинов Hunyuan, в том числе huggingface-hub 0.30.2 → 0.27.1,
# transformers 4.46.0 → 4.47.1, numpy 1.24.4 → 1.26.4, trimesh и pymeshlab. Образ при этом
# собирался зелёным, а разъехалось бы уже на ноде при загрузке весов — то есть за деньги и
# далеко от причины. Правило: список зависимостей апстрима — источник истины, мы ДОПОЛНЯЕМ.
#
# Уже есть у апстрима, не дублировать: fastapi, uvicorn, trimesh, transformers,
# huggingface-hub, numpy, pillow, opencv-python, pybind11, ninja, pygltflib, einops,
# omegaconf, scikit-image, pymeshlab, rembg.

boto3==1.35.90              # выгрузка результата в S3-совместимое хранилище (запасной путь;
                            # основной путь пилота — наш приёмник по HTTP, ему boto3 не нужен)
