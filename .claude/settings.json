{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_about": "Проектные настройки В GIT. Личное и разрешения — в .claude/settings.local.json (он в .gitignore, поэтому гарды держим здесь: иначе защита не переживёт клон репозитория).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v node >/dev/null 2>&1 && [ -f tools/hooks/guard-bash.mjs ]; then node tools/hooks/guard-bash.mjs; fi"
          }
        ]
      }
    ]
  }
}
